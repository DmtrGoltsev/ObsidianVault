---
id: "finance-investment-recommendations-qa-20260921"
тип: "доказательство"
статус: "активно"
проект: "Finance"
аудитория: "агент"
создано: "2026-09-21"
обновлено: "2026-09-23"
ссылки:
  - "[[Finance]]"
  - "[[Finance/01_Человеку/Архитектура/Инвестиционные_Рекомендации_20260921]]"
---

# QA инвестиционных рекомендаций — 2026-09-21

## Актуальный контур на 2026-09-23

Исторические локальные результаты ниже относятся к delivery-точке `a3d3368`. Текущая feature-вершина — `76363ca73756d4c279a115c48c29b53bf10ecef2`; [PR #9](https://github.com/DmtrGoltsev/finance/pull/9) открыт, не является релизным и имеет четыре успешные проверки: `delivery`, `android`, `backend-auth-and-migration-gates`, `build-and-test`. Успех CI не подтверждает установку на VPS, работу внешних провайдеров или production E2E.

| Шлюз приёмки | Наблюдение | Статус |
|---|---|---|
| Проверки feature-ветки | Четыре проверки PR #9 успешны для точного SHA `76363ca` | Пройдены для feature, не для production |
| Инвентаризация production | [Bootstrap `35799237155`](https://github.com/DmtrGoltsev/finance/actions/runs/35799237155) и [inventory `35799250576`](https://github.com/DmtrGoltsev/finance/actions/runs/35799250576) успешны; Alembic `20260822_0019`, `outbox_events=no`, свободно `3 615 506 432` B на общем разделе | Только чтение; готовность к установке не подтверждена |
| Ёмкость нативной установки | [Footprint `35802936517`](https://github.com/DmtrGoltsev/finance/actions/runs/35802936517): наблюдавшийся пик `6 735 093 760` B плюс резерв `1 227 784 192` B = минимум `7 962 877 952` B; недостаток `4 347 371 520` B | Блокирует выпуск; пиковая живая RAM неизвестна |
| Внешние секреты | В `backend.env` отсутствуют имена DeepSeek, FCM и delivery HMAC; иные хранилища инвентаризация не проверяла | Не подтверждено |
| Production и миграции | Новый контур не установлен; текущая ревизия БД `0019`, целевая `0025` | Не выполнялись |
| Защитный шлюз развёртывания | В feature-ветке `production-package-gate` отказывает до SSH с `DELIVERY_HOST_NATIVE_CAPACITY_UNVERIFIED` | Закрыт намеренно |

### Дополнительные сценарии перед выпуском

| ID | Сценарий | Критерий прохождения | Сейчас |
|---|---|---|---|
| INV-REC-017 | Повторная предварительная проверка только на чтение после плана ёмкости | Свежие факты VPS и утверждённые измеренные пороги для диска и RAM, включая запас, резервные копии и восстановление | Не проверено; дефицит диска доказан |
| INV-REC-018 | Миграция копии Finance DB `0019 -> 0025` | Данные сохранены; прежний backend работает на обновлённой копии; восстановление из проверенного дампа выполнено | Не проверено |
| INV-REC-019 | Нативные n8n/gateway/worker | Точные версии и контрольные суммы, отдельные роли/БД, loopback-порты, подписанный callback, три workflow и восстановление обеих новых БД подтверждены | Не проверено на VPS |
| INV-REC-020 | Полный сквозной путь | Обезличенный снимок каждого поддержанного брокера, реальный вызов DeepSeek, задание, callback, отчёт и безопасный повтор проходят в тестовом контуре | Не проверено |
| INV-REC-021 | FCM на устройстве | Тестовый Firebase-проект доставляет нейтральный push на Android; deep link открывает нужный отчёт, отзыв токена и дедупликация работают | Не проверено |
| INV-REC-022 | Откат после сбоя активации | Прежний backend восстановлен, новые службы остановлены, БД/ключи/дампы сохранены; дальнейшее восстановление определено оператором без автоматического downgrade | Не проверено |

Ни один из сценариев `017–022` не следует помечать пройденным на основании четырёх проверок PR. Значения паролей, токенов и ключей в доказательства не записывать.

## Исторический объект локальной проверки

- Репозиторий: `finance`.
- Ветка: `codex/investment-recommendations-delivery-20260920`.
- Локальная релизная точка: `a3d3368c726d7908066943072ae719035a7508c6`.
- Production, реальные аккаунты и реальные секреты не использовались.

## Обязательные тест-кейсы

| ID | Сценарий | Ожидаемый результат |
|---|---|---|
| INV-REC-001 | Два счёта СберИнвестиций в одном портфеле | Видны оба профиля, объединённая сумма и число счетов |
| INV-REC-002 | История готовой рекомендации после перезапуска | Видны профили счетов, краткий итог и ссылка на тот же отчёт |
| INV-REC-003 | Развёрнутый готовый отчёт | Видны допущения и горизонт анализа |
| INV-REC-004 | Потерян ACK после готового анализа | Повтор использует тот же job/event/idempotency key, второй анализ не создаётся |
| INV-REC-005 | Повтор идентичного ready callback | Возвращается сохранённый отчёт независимо от возраста данных и последующего изменения политики |
| INV-REC-006 | Отличающийся ready callback | Callback отклоняется, сохранённый отчёт не меняется |
| INV-REC-007 | Legacy-отчёт с `callback_hash=NULL` | Принимается только полностью идентичный результат, после чего сохраняется канонический хеш |
| INV-REC-008 | Исчерпаны попытки outbox | Job получает явную ошибку доставки, UI разрешает безопасный повтор той же доставки |
| INV-REC-009 | Ready приходит при `pending` или `processing` outbox | Событие терминально гасится и worker не вызывает n8n повторно |
| INV-REC-010 | Одновременный повтор callback | Сохраняется один отчёт и один ready event |
| INV-REC-011 | Регистрация, refresh и revoke FCM token | Токен привязан к владельцу и сессии; logout отзывает его; invalid token очищается |
| INV-REC-012 | Повтор ready push | На одно событие и устройство существует не более одной доставки |
| INV-REC-013 | Обновление каталога MOEX | Периодический и ручной запуск не создают параллельные одинаковые задания |
| INV-REC-014 | Debug без Firebase-конфига | Сборка проходит с выключенным push и не требует `google-services.json` |
| INV-REC-015 | Docker build context | В gateway попадают только `package*.json` и `gateway/**`; `.env`, credentials, Git и кэши исключены |
| INV-REC-016 | Цикл миграций `0024 -> 0025 -> 0024 -> 0025` | Legacy report сохраняется; после повторного upgrade `callback_hash` снова `NULL` до безопасного закрепления |

## Фактические результаты

| Проверка | Результат |
|---|---|
| Полный backend исходной delivery-точки | `416 passed, 6 skipped, 15 warnings`, 93.14 с |
| Полный backend после финальной strict preexisting validation | `418 passed, 25 skipped, 16 warnings`, 76.95 с; PASS |
| Три ранее падавших mocked FCM-кейса | `3 passed, 28 deselected, 3 warnings`, 1.50 с; PASS |
| Delivery, OpenAPI и production workflow contracts | `53 passed, 3 warnings`, 19.47 с; PASS |
| OpenAPI/ручной контракт | `18 passed, 2 warnings`, 6.20 с |
| Миграции `0023–0025`, включая SQLite legacy-cycle | `3 passed, 58 deselected, 2 warnings`, 0.90 с |
| Ruff lint по backend/tests/миграциям | `All checks passed` |
| n8n/gateway Node tests | `32 passed, 0 failed`, включая HMAC parity с реальным backend-модулем |
| n8n workflow validation | `3` workflow: PASS |
| Реальный import/export n8n `2.39.8` | PASS: `3` стабильных ID, isolated SQLite profile, workflow неактивны |
| Android unit | `198 tests, 0 failures, 0 errors, 0 skipped` в `29` XML-файлах |
| Android build | `testDebugUnitTest`, `assembleDebug`, `compileDebugAndroidTestKotlin`, `lintDebug`: BUILD SUCCESSFUL, `67` задач |
| Android instrumentation на `emulator-5554` | `3 passed, 0 failed`: объединённый портфель, история/профили, допущения отчёта |
| Android lint | задача успешна; `19 warning`, `5 information`, ошибок нет |
| GitHub Actions YAML | `4` файла успешно разобраны; production workflow contract `4 passed` |
| Compose config | n8n и delivery: PASS с тестовыми внешними путями/примером env |
| Live PostgreSQL `18.3` fresh upgrade | PASS: чистая база прошла всю цепочку до `20260921_0025`; `outbox_events=1`, индексов `6` |
| Live PostgreSQL legacy-cycle | PASS: `0024 -> 0025 -> 0024 -> 0025`, отчёт сохранён, финальный `callback_hash IS NULL=true` |
| Gateway durable dedupe после рестарта PostgreSQL | PASS: повтор `event_id=0` вставок, повтор `job_id+attempt=0`, итоговая строка `1`, nonce `2` |
| Cleanup временного PostgreSQL | PASS: сервер остановлен, порт `58802` не отвечает, проверенный temp-каталог из `2340` элементов удалён |
| Live PostgreSQL strict outbox regression | `14 passed, 3 warnings`, 73.15 с: ownership, semantic contract, four negative cases, controlled downgrade, legacy report и offline paths |
| Финальный cleanup strict-validation кластера | PASS: test DB отсутствуют, сервер остановлен, порт `53505` закрыт, temp-каталог из `1002` элементов удалён |
| Live PostgreSQL final schema/predicate/downgrade regression | `20 passed, 3 warnings`, 76.05 с: wrong FK schema, wrong `ON DELETE`, equivalent/false/other predicates, explicit `0020`, relative `-1`, `base` и explicit below-`0020` |
| PostgreSQL `pg_get_expr` canonical predicate | PASS: `NOT (deduplication_key IS NULL)` канонизирован сервером как `NOT deduplication_key IS NULL` и принят; `WHERE false` и `IS NULL` отклонены до DDL |
| Offline SQL final | PASS: сгенерированы upgrade `0020 -> 0021` и downgrade `0021 -> 0020` для PostgreSQL |
| Финальная очистка PostgreSQL | PASS: `finance_migration_*` отсутствуют, сервер остановлен, порт `50669` закрыт, root PID завершён, temp-каталог из `1003` элементов удалён |
| Secret scan | эвристический scan новых commit/diff: `0` совпадений, `0` подозрительных файлов; `gitleaks` локально отсутствует |
| `git diff --check` | PASS |

Тестовый APK без production Firebase push:

`C:/Users/style/Documents/Codex/finance-investment-delivery-20260920/apps/android/app/build/outputs/apk/debug/app-debug.apk`

Размер: `57 037 416` байт (`54,4 MiB`). Это локальный debug/no-push артефакт, а не production-релиз.

## Ограничения проверки

- Docker Desktop `4.88.1` и Docker CLI `29.7.2` доступны, но daemon не поднялся. Backend завершился на stale socket `C:/Users/style/AppData/Local/Docker/run/sailor-ingest.sock`: Windows вернула `The file cannot be accessed by the system`. После полной остановки `0` Docker-процессов; socket размером `0` и с атрибутом `ReparsePoint` безопасно удалить не удалось. Reset, Docker data, images и volumes не затрагивались; повторный запуск после неудачного удаления не выполнялся.
- Вследствие этого Docker build/smoke и контейнерный вариант gateway не выполнены. Проверка `docker compose config` для n8n и delivery остаётся успешной; PostgreSQL migration/dedupe закрыты отдельным временным Windows-кластером без Docker.
- Существующий PostgreSQL на `localhost:5432` не использовался. Disposable-кластер работал только на `127.0.0.1:58802` и после проверки полностью удалён.
- Причина трёх прежних FCM-падений подтверждена как неполное локальное QA-окружение: `google-auth[requests]>=2.40,<3.0` уже объявлен в `apps/backend/pyproject.toml`, а CI устанавливает `pip install -e "apps/backend[dev]" pyyaml`. В новом изолированном окружении этой штатной командой установлен `google-auth 2.58.0`; `pip check`, полный suite и три FCM-кейса прошли. Код проекта и dependency manifest не менялись.
- Файл применённой ревизии `20260919_0021` изменён; Alembic checksum не проверяет и уже применённую ревизию повторно не запускает. Legacy-база без ownership-реестра проходит обычный upgrade до head, но её downgrade явно блокируется до DDL. Совместимая preexisting outbox может откатиться до `0020`; ниже `0020` требуется отдельное эксплуатационное решение по принадлежащим ей FK. Offline SQL поддерживается для точного fresh-среза `0020 -> 0021`; preexisting outbox намеренно требует online Alembic и отклоняется до DDL.
- Финальная проверка preexisting outbox использует эффективную схему `current_schema()` и серверный `pg_get_expr`, а не имена объектов или необработанную строку Inspector. Relative `-1` разрешён только как прямой переход `0021 -> 0020`; `base`, явная ревизия ниже `0020` и более широкие цели сохраняют ownership guard.
- `gitleaks` не установлен; выполнен локальный эвристический scan без вывода возможных значений секретов.
- Не выполнялись реальные OCR fixtures Синары, СберИнвестиций и Финама, live DeepSeek, live FCM, production backend и production deploy.
- Для live push нужны внешний Firebase project ID, Android `google-services.json` и service account с правом FCM HTTP v1; эти данные не хранятся в базе знаний и Git.

## Критерий следующего E2E

Использовать только отдельный тестовый контур: тестовый Firebase-проект, тестовый DeepSeek credential и обезличенные реальные скриншоты трёх брокеров. После прогона добавить отдельное доказательство с датой, версиями и результатами, не записывая секреты.
