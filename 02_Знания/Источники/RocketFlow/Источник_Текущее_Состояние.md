---
id: "src-current-state"
тип: "источник"
статус: "активно"
проект: "RocketFlow"
владелец: "rocketflow-team"
создано: "2026-05-31"
обновлено: "2026-06-07"
уверенность: "высокая"
источники: ["docs/33-current-state-summary.md"]
доказательства: ["Док_Cleanup_Manifest", "Док_Backend_Verification", "Док_Web_Verification", "Док_Android_Verification", "Док_Prod_Deploy_State"]
теги: ["документация", "статус", "handoff"]
---

# Источник: Текущее состояние

## Путь

`docs/33-current-state-summary.md`

## Роль в проекте

Краткий handoff-отчёт о текущем состоянии проекта. Точка входа для новой сессии без переноса полной истории.

## Краткое содержание

Статус: три волны (A, B, C) завершены, Wave C.1 завершён, текущая стадия — MVP3 упрощение и полировка. Активная ветка: `MVP3`, tracking `origin/MVP3`, HEAD `9825a40f6d2e4b909b785ef228aacc5393acec0b`, latest short `9825a40`; `git rev-list --count MVP2..MVP3` = 21. Финальные post-cleanup проверки зелёные: backend `mvn --batch-mode --no-transfer-progress test` прошёл (`63/0/0/0`, `BUILD SUCCESS`, total `03:47`), web `npm run build` прошёл (`tsc -b && vite build`, 1792 modules transformed, built in `2.09s`, assets `index-B4KB3O2m.css` / `index-BZSQwOSw.js`), Android `.\gradlew.bat :app:testDebugUnitTest :app:assembleDebug :app:lintDebug --no-daemon` прошёл с exit code `0`. Audit inventory: backend 16 test files / 63 tests, Android unit 8 files / 49 tests, Android instrumented 2 files / 7 tests, web test scripts отсутствуют. Android local setup исправлен, SDK `C:\Users\style\AppData\Local\Android\Sdk`, `android/local.properties` ignored by git, emulator `emulator-5554` видим. Cleanup invariants healthy: `git ls-files -- test-artifacts` empty; `git diff --cached --name-status -- test-artifacts` показывает 517 staged deletions; physical `screenshots/`, `tmp-collapse-window.xml`, root `rocketflow-web-sha-*.tar.gz`, `test-artifacts/` отсутствуют; `.gitignore` покрывает generated paths и `android/local.properties`. Git status после verification: staged 517 `test-artifacts` deletions; modified `.gitignore`, `docs/33-current-state-summary.md`, `docs/60-hexcore-prod-runbook.md`; untracked `docs/65-prod-db-backup-local-runbook.md`, `scripts/Invoke-ProdPostgresBackupDownload.ps1`, `scripts/Set-ProdBackupDownloadEnv.example.ps1`; generated outputs только ignored. Production model: jar/systemd backend + web static на HexCore; deploy workflow `backend-hexcore-prod-deploy.yml`. Docker/GHCR остаётся open gate, GHCR workflow отсутствует или требует восстановления.

## Статус актуальности

Актуален на 2026-06-07 после cleanup/repo audit и финального verifier. Build health зелёный для backend/web/android; GHCR/staging notification/security/prod gates остаются отдельными открытыми гейтами.

## Связанные заметки

- [[RocketFlow]] — главная заметка проекта
- [[MVP3_Упрощение]] — текущая стадия
- [[Wave_A]], [[Wave_B]], [[Wave_C]] — завершённые волны
- [[Док_Cleanup_Manifest]] — evidence и cleanup manifest
- [[Источник_Android_Local_Setup]] — Android local setup
- [[Док_Prod_Deploy_State]] — production deploy status
