---
id: "evidence-n8nagents-verification-matrix-20260829"
тип: "доказательство"
статус: "историческое"
проект: "N8NAgents"
владелец: "style"
создано: "2026-08-29"
обновлено: "2026-08-29"
уверенность: "высокая"
источники:
  - "Git N8NAgents docs/verification-matrix.md @ 09824a6e16e479d2283ddbd4fb5125a50bda5113; tree 5eb0df96c8ab908ba45cdd18c8286ce683528135"
доказательства: []
source_path: "docs/verification-matrix.md"
source_base: "09824a6e16e479d2283ddbd4fb5125a50bda5113"
source_tree: "5eb0df96c8ab908ba45cdd18c8286ce683528135"
source_snapshot: "working tree at import; includes 12 added lines beyond source_base"
source_file_sha256: "26156d8c1038a93fa276646866c76a63c87fbac936d96f95e0646f40d76eb59"
imported_date: "2026-08-29"
source_status: "source verification snapshot; status cells are point-in-time evidence"
проверка_редакции: "PASS — secret/PII values absent; identifiers are placeholders or redacted source facts"
каноничность: "canonical vault location for this imported human-readable source document; CURRENT_STATE and the full architecture note win for runtime facts"
теги: ["n8n", "source-import", "obsidian-only-docs"]
---

> [!important] Canonical placement and source status
> Полный human-readable source document перенесён в canonical Obsidian vault. Source path указан только как provenance и может быть удалён из repository. Current verified runtime state: [[CURRENT_STATE_N8NAgents_2026-08-29]].
>

# Verification matrix

Statuses: `PASS`, `FAIL`, `MANUAL-PASS`, `BLOCKED-LOCAL`, `BLOCKED-EXTERNAL`.

Foundation/Phase A rows retain their original initial-deployment meaning. Dated production rows report only the separately observed current state; a scoped production PASS does not retroactively satisfy unrelated initial or negative-test gates.

| Check | Command / evidence | Current status |
|---|---|---|
| K4R sealed authority | external `MANIFEST` SHA-256/size plus `GO-O4-ATTEMPT1-QUORUM-v2` | must match before any edit |
| K4R capability/containment | `test-phase-a-release-gate-linux.sh capability` | `BLOCKED-LOCAL` unless UID 0, private mount namespace, loopback-only network namespace, real exact/nested/same-device binds, immutable bit, read-only repo/Vault and unchanged outside sentinel all pass |
| Actual production runner matrix | 66 exact required cases in canonical 17-column TSV | required in contained Linux; every production label and isolated root/shared/current UID/GID/mode check is reached, no row is skipped and no caller-supplied total is trusted |
| Fault shims | exact 34-row canonical 10-column manifest; generated only inside disposable namespace | every target fixed/absolute, original and shim hashes bound, call counts exact; no shim copied into repository/package |
| Runner TOCTOU and publication | source/staging/final tuple+hash mutation canaries | exact failure label/RC, zero prohibited later steps |
| Rollback/signals/cleanup | prior target, prior absence, rollback RC 78, HUP/INT/TERM pre/post, cleanup-primary RC | all required runner cases must pass |
| Mount boundary | exact root, nested and same-device binds in runner/governance/compose | all three validators must reject from decoded mountinfo |
| Root/shared/current matrix | numeric owner/group, exact modes, raw+canonical current target | runner, governance and compose rows required |
| Windows false-PASS canaries | child prints PASS+nonzero; child prints BLOCKED+zero | both must be rejected |
| External static evidence | `verify-static.ps1 -MatrixPath ... -ExecutionMatrixPath ... -LinuxStaticMatrixPath ...` | canonical matrix derived only after child and Linux matrices pass |
| Deterministic packaging | `test-package-reviewed-release.ps1` | candidate/final byte equality, two-build equality, stale/tampered external evidence rejection and isolated Git objects |
| K4R remote authority | separate owner-confirmed `K4R-REMOTE-1` execution-GO | forbidden during offline gate; `OFFLINE_READY` alone is insufficient |
| JSON parse + schema references | `scripts/verify-static.ps1` | run during local handoff |
| Draft 2020-12 metaschema + 5 positive/5 negative tool fixtures | `scripts/verify-contracts.py` with `jsonschema` | must report `PASS`; otherwise explicitly `BLOCKED-LOCAL`, never semantic PASS |
| DeepSeek fallback discriminator + exact arguments, 6 positive/3 negative fixtures | `scripts/verify-contracts.py` with `jsonschema` | must report `PASS`; otherwise explicitly `BLOCKED-LOCAL` |
| Migration sentinel equals SHA-256 of `002_assistant_schema.sql` and cannot overwrite mismatch | `scripts/verify-migration-sentinel.py` | run during local handoff |
| Compose parses without interpolation | `docker compose ... config --no-interpolate --quiet` | run if local Docker CLI supports it |
| Static topology: only Caddy ports, internal data network, no latest/runner | verification scripts | run during local handoff |
| Phase A default excludes Caddy; bootstrap publishes only `127.0.0.1:5678` and applies resource limits | verification scripts + rendered Compose | static check locally; rendered Compose required before deployment |
| Exact Caddyfile validates | `docker run --rm ... caddy:2.11.4-alpine caddy validate ...` | `BLOCKED-LOCAL` unless safe daemon/image available |
| Shell syntax | `bash -n` / `sh -n` through scripts | run if shell available |
| Secret-like material scan | verification scripts plus `git diff --cached` review | run during local handoff |
| PostgreSQL sentinel, cross-DB/TEMP denial, fixed API grants, direct-DML denial, audit immutability, idempotency and fault rollback | `scripts/verify-postgres.ps1` disposable project | `BLOCKED-LOCAL` unless safe daemon available |
| A2 redacted host discovery | `docs/server-discovery.md` evidence | `PASS` after reboot; no deployment claim |
| n8n 2.36.7 trusted memory node contract | production A/B evidence plus credential-free structural contract | `PASS` for the exact memory-node parameters and mandatory `ai_memory` edge; no credential-bearing full export is committed |
| DeepSeek V4 native node and fallback decision | live spike matrix | `BLOCKED-EXTERNAL` |
| Memory LangChain DDL/add/get/clear parity | PostgreSQL 17.11 disposable direct-login test after idempotent grants rerun | `PASS`; exact `memory_runtime`, `search_path`, grants, DDL, ordered read and clear-by-session |
| Production single-session recall | sanitized A/B evidence dated 2026-08-29 | `PASS`; update/execution/app/memory/outbound counters and user confirmation recorded |
| Memory persistence across restart | controlled n8n/PostgreSQL restart | `UNKNOWN — not tested` |
| Memory isolation between two session keys | two-session execution evidence | `UNKNOWN — not tested` |
| Header auth → normalize → numeric user+chat allowlist before any DB/effect/LLM/tool | execution evidence/counters | `BLOCKED-EXTERNAL` |
| Exact JSON webhook POST, 1 MB limit, spoofed XFF, wrong content type and fallback 404 | HTTPS route tests | `BLOCKED-EXTERNAL` |
| Localhost-only SSH-tunnel owner bootstrap; public editor denied until owner+2FA | listener/UI/external test | `BLOCKED-EXTERNAL` |
| Success/error execution persistence contains no headers/body/prompts/tool/provider payloads | redacted execution/log fixtures | `BLOCKED-EXTERNAL` |
| External IPv4/IPv6 only approved SSH/80/443; no 5678/5432/API | listener, inspect, provider and external scans | `BLOCKED-EXTERNAL` |
| Duplicate/retry/crash-window and reminder concurrency | workflow/database fixtures | `BLOCKED-EXTERNAL` |
| Restart/reboot persistence | VPS controlled restart | `BLOCKED-EXTERNAL` |
| Exclusive quiesced backup, signed BOM, immutable upload, retention cleanup, timer/journal/failure path | backup evidence | `BLOCKED-EXTERNAL` |
| Tampered encrypted archive rejected before decrypt/extract | `test-backup-tamper.sh` | `BLOCKED-LOCAL` without Bash/minisign fixture |
| Authenticated captured-digest PostgreSQL-only restore | restore drill | `BLOCKED-EXTERNAL` |
| Credential decryption without decrypted export | supported-method evidence | `BLOCKED-EXTERNAL`; no supported non-export method confirmed |

Static PASS means only that the local draft is internally consistent. It never upgrades deployment or product acceptance to SUCCESS.
