# Реестр дефектов исполнения объединённой QA-матрицы

Дата начала прогона: 2026-09-10. Телефон исключён. Записи не закрываются без повторного прогона затронутых case ID на точном артефакте.

## Зафиксированный baseline

| Контур | Ревизия | Рабочее состояние |
|---|---|---|
| Poruchik | `b4840f159f9eeb67d007b22491402966ddc970f9` | tracked diff SHA-256 `d766c1fe55c97bd84150c8268c8fe8c6774795983204d1931728754ad5ac3e82`; 7 Android conflict/rebase WIP files; QA docs untracked; pre-existing `settings-gradle.lockfile` |
| N8NAgents | `8ba7250ef0e030f41ec884241d207fb3998d8802` | tracked diff SHA-256 `c4f1674284f502bf9fccefa3a299aa15f2e2927dd5a837ed7873bb0c16907b77`; существующие чужие WIP/artifacts сохранены |
| Android app contract | `applicationId=com.poruchik.app`, `versionName=0.2.0`, `versionCode=2` | Для AVD evidence нужен APK, собранный из указанного exact WIP snapshot |
| Production | TaskCore `sha256:3225da…92a9d9`, Gateway `sha256:2fe87b…2b694f4`, OpenClaw/relay `sha256:878972…72c8ac`; plugin `0.2.3`; Flyway V1–V9 success | Read-only B1: Gateway, TaskCore, Action Executor, n8n, OpenClaw, relay, Caddy и обе PostgreSQL healthy; public live/ready=200. Active workflow IDs зафиксированы. Все 43 SECURITY DEFINER функции имеют закреплённые owner/search_path/ACL, PUBLIC=0 |

## Открытые продуктовые дефекты

| ID | Case ID | Состояние | Сигнатура | Влияние | Владелец следующего пакета | Критерий закрытия |
|---|---|---|---|---|---|---|
| DEF-001 | SYNC-16, SYNC-17, SYNC-19, E2E-05 | RETEST_PASS | Старая реализация теряла несохранённый draft при compare/retry. `SYNC19-final-proof.json` теперь подтверждает два настоящих CONFLICT6/7, force-stop/PIN/reopen, сохранённый TITLE draft/dirty, explicit compare и final retry APPLIED8 при сохранённом server DESCRIPTION | Риск потери пользовательского черновика или неявной перезаписи закрыт на production HTTP+Android UI/Room | Завершено APK11/current client | `sync19-final-v8-oracle.json`: три singleton HTTP200/audit5→8; `sync19-cleanup-final-oracle.json`: cleanup APPLIED9, baseline business hash восстановлен, conflict rows0. Независимый официальный API writer создаёт ту же авторитетную конфликтную версию; второй AVD не добавляет продуктовый oracle |
| DEF-002 | TASK-13, ENV-08 | OPEN, воспроизведён на AVD | В редакторе задачи третий переключатель TEAM сжат в высокую пустую капсулу; надпись отсутствует при обычных 1080×2400, density420 | Пользователь не может осознанно выбрать командную видимость | Mobile общий пакет | На штатном и увеличенном масштабе все три подписи видимы и выбираются; UI/API/ACL переходов согласованы. Evidence: mobile-20260910/TASK-visibility-overflow.png, bounds TEAM [922,943][1048,1356] |
| DEF-003 | EX-045 | RETEST_PASS: весь набор локально и на production PG17; четыре workflow совпадают с проверенными исходниками | Telegram 429 с retry_after_seconds=3600 передаётся классификатором, но reminders_finalize_delivery назначает повтор через 120 секунд | Преждевременные повторы при ограничении Telegram | N8N общий пакет | Повтор не раньше provider retry_after; отрицательные и большие значения обработаны безопасно, quiet-hours соблюдены; PG17 перед выпуском. scheduler-boundaries.sql/.log в каталоге QA_EXECUTION_20260910 |
| DEF-004 | FOC-01 | RETEST_PASS | Android empty focus имел version0 при канонической начальной версии1, поэтому первый save отклонялся | Backend и Mobile согласованы; пустой snapshot сохраняет версию после перезапуска | Backend/Mobile | DEF004-empty-reopen-version.json: версия4/entries0; последующее заполнение подтверждено XML и task17-focus-oracle.txt: версия7/шесть FOCUS_REPLACE. Отдельный DEF008 порядка строк остаётся открыт |
| DEF-005 | OPS-01, EX-042 | CLOSED_LOCAL_AND_PROD_METADATA: чистый bootstrap исправлен, productionpgcrypto уже существовало до012 | В init/migrations отсутствует установка pgcrypto; internal_scheduler_authorize вызывает public.hmac и падает SQL error | Новый scheduler-контур не авторизует запросы после документированного чистого развёртывания | N8N общий пакет | Канонический bootstrap создаёт нужную зависимость с правильной схемой/ACL; valid signature проходит, invalid/replay отклоняются без SQL error; подтвердитьPG17. scheduler-auth-direct.log |
| DEF-006 | TASK-18, SYNC-03 | RETEST_PASS: восстановление прежнего текста и однократный retry на AVD/API; TASK-18 полностью PASS | После status APPLIEDv3 сохранение результата отправляет stale detail v2; получает CONFLICTcurrent3, сервер result=null, редактор результата очищается | Пользователь теряет доступ к несохранённому тексту результата в редакторе | Mobile общий пакет | После status→result в том же detail актуальная версия; конфликт сохраняет текст для compare/retry, reopen/offline не теряют его. TASK18-result-conflict-reopen-empty.xml; op2535bb74; Room proof: payloadretained=true, stateCONFLICT; потерян доступ через UI, не текст в БД |
| DEF-007 | DOC-13, DOC-12 частично | RETEST_PASS реализованных путей: 16 локальных проверок и AVD GET404/офлайн; официальный отзыв назначения NOT_IMPLEMENTED | После проверенного онлайн-содержимого ответы API403/404 возвращают прежний закрытый Markdown как Success(fromCache=true); отдельный IOException корректно использует кеш | Документ остаётся доступен после явного отказа сервера в доступе | Mobile следующий пакет, текущий APK не изменять | 403/404 не раскрывают кеш; удаление метаданных при отзыве доступа инвалидирует содержимое; сетевой сбой сохраняет проверенную офлайн-копию. DocumentCacheAuthorizationTest: 4 проверки, 3 FAIL (403/404/DELETE), 1 PASS (IOException) |

| DEF-008 | FOC-01 | RETEST_PASS, APK4 | Кнопки ↑/↓ меняют выбранный порядок, но строки редактора остаются в исходном порядке A/B/C; после сохранения отображается правильный C/B/A | Во время редактирования пользователь не видит результат перестановки | Mobile, следующий пакет | Строки или явные номера немедленно отражают порядок; после сохранения и повторного открытия UI/Room/API совпадают. FOC01-three-selected.xml, FOC01-reordered-before-save.xml, FOC01-reordered-saved.xml |
| DEF-009 | CAL-04 | RETEST_PASS | Карточка календаря открывала точную задачу, но Android Back возвращал список задач вместо календаря | Третий пакет сохраняет исходный маршрут и положение календаря | Mobile | batch3-CAL-scroll-proof.json: кнопка/system Back/пересоздание сохраняют координаты03–06; warm counter>1 и manual tab reset проверены на неизменной APK3 |

| DEF-010 | EX-003 | RETEST_PASS | Публичный DELETE /v1/devices/{deviceId} зарегистрирован и разрешён Gateway, но отсутствовал в mobile OpenAPI; внутренние три relay dispatch маршрута также не были описаны | Контракт дополнен в c8a0710 без изменения доступности маршрутов | Backend, третий пакет | ex003-routes-batch3.json: mobile30/handlers30, internal13/handlers13, оба набора расхождений пусты; SERVICE_OPENCLAW dispatch остаётся внутренним |

| DEF-011 | INB-02, EX-036 связанный параметр | RETEST_PASS, c8a0710 | GET /v1/inbox?afterSequence=-1 возвращает200 и все события вопреки OpenAPI minimum0 | Некорректный курсор принимается вместо отказа | Backend | afterSequence0/exact/beyond дают точный хвост; negative/нечисло/переполнение отвергнуты без изменений. inbox-api-batch.json, inbox-api-db-oracle.txt |
| DEF-012 | INB-07 | RETEST_PASS | MARK_INBOX_READ события9ad2ffd8/version1/sequence30 отправлял expectedVersion30 | Версия исправлена; сохранённый конфликт восстанавливается явно и без повторного эффекта | Mobile | Новый8e2087ea: expected1/PENDING→offline force-stop/reopen та жеop→APPLIED/server READv2/audit1. Старыйecc3 superseded, новый2a8cc5dc APPLIED, server READv2/audit1. batch3-INB-offline-proof.json, batch3-INB-retry-proof.json, batch3-generic-read-oracle.txt |

| DEF-013 | INB-03, INB-04, INB-05, APP-03 | RETEST_PASS | После ACCEPTED/DECLINED assignment и APPROVED/REJECTED approval связанные события оставались UNREADv1 | Сервер c8a0710 и APK3 исправлены; все четыре свежих решения подтверждены на проме и UI | Backend/Mobile | batch3-decision-post-oracle.txt и batch3-assignment-post-oracle.txt + Room/UI: terminal/Inboxv2, resolved_at, audit1/change2; ACCEPT даёт доступ, DECLINE нет. Дополнительные три proposal исхода проверены локальным IT, не заявлены как prod UI |
| DEF-014 | EX-038; общая ветвь 13 typed tools | LOCAL_RETEST_PASS, 2e30dde; DEPLOY_BLOCKED после3попыток оснастки | Реальный plugin повторяет один toolCallId с тем же operationId, но новым correlationId; контроллер считает запрос изменённым и возвращает409 | После потери ответа повтор не восстанавливает исходный успех; дубль эффекта не создаётся | Backend/plugin, общий пакет | replay-correlation-proof.json: все поля кроме correlation равны; write-tools-local-batch3.log: первый approval singleton, повтор different request409. Канонические n8n и Action Executor передают intent неизменным. Повтор на проме не запускался; после fix same args replay успешен, changed args same key всё ещё409 |
| DEF-033 | EX-025 | RETEST_PASS, APK12 | Controlled SQLite write failure при offline CREATE выходил как необработанный `SQLiteConstraintException qa-internal-storage-detail` из `TaskDao_Impl.upsertAll`→`RoomTaskRepository.createOffline`; PoruchikApp закрывал dialog и очищал title до завершения suspend-вызова | При отказе локальной записи пользователь терял draft и не получал контролируемого результата; возможен ложный вид успеха/закрытия формы | Mobile | `OfflineRepositories.createOffline` преобразует исключение записи в санитизированный `TASK_CREATE_SAVE_FAILED`, повторно бросает cancellation и сообщает queued только после commit; UI сохраняет dialog/title до Success и блокирует повторное нажатие. `APK12-create-write-ui.log`:1/1 PASS за9,129с, baseline task/outbox неизменён через второй file Room handle, один явный retry создал ровно1 CREATE PENDING; `APK12-update-proof.json`: exact APK SHA`75f9572…d3b4a`, прежний signer`b567…aca0`, обе AVD сохранили PIN и14 Room-таблиц |
| DEF-034 | EX-019 | RETEST_PASS, APK13 source | Edit Save не имел общего in-flight guard: после actual Room commit, пока repository reply удерживался latch, второй pointer tap запускал второй UPDATE; одна recreation не отменяла дубль | Один пользовательский save мог создать две durable операции и два серверных эффекта | Mobile, текущая дефектная партия; подписанный release candidate не перезаписывался | `TasksScreen` ставит `savingTask` синхронно до launch, повторный tap no-op/control disabled, reset в finally. `APK13-double-save-ui.log`: тот же test1/1 PASS5,542с, actual Room commit, held first reply, calls1/pending1, второй pointer tap no-op, одна recreation до release, draft сохранён и Save снова enabled. Failure/cancellation проходят тот же finally; отдельный зеркальный тест не добавляет риска |
| DEF-035 | ENV-09 | RETEST_PASS, APK14 | Две видимые delegation CheckBox имели пустые text/content-desc при checkable/clickable/focusable=true; отдельные sibling Text не давали имени интерактивным целям | TalkBack/другой accessibility service объявлял две безымянные цели | Mobile | `APK14-delegation.xml`: обе checkable цели имеют exact русские content-desc, NAF отсутствует, state/role/clickability/focusability прежние. Остальные поверхности не повторялись; voice review сведен с прежним UI3PASS+source semantics. `APK14-final-proof.json`: SHA`8c9cd709…cf9fda`, signer`b567…`, main18/57/7,member3/0/0,prod mutations0 |
| DEF-036 | OPS-06 | RETEST_PASS | Если worker исчезает после пятого claim и не вызывает fail(), lease истекает, но `claim()` исключал attempts5, а DLQ создавался только через fail(); delivery навсегда LEASED, run RUNNING | Выполняющаяся команда зависала без terminal/DLQ; оператор не получал конечного состояния | Backend TaskCore, одна общая lease-партия | Before:`ops06-lost-lease-red.log`1/1 FAIL rollback. Fix actual-PG3/3 PASS. Scoped production release: commit`993c4f2`, image`be1a9a9d…b9b09`, JAR`7196a556…b56b89ac`; predicate pre/prewrite/post0, readinessUP, exact2networks/config unchanged, global claim/fixture0; validated dump SHA`9d4288ee…5862e0`, rollback не понадобился. `def036-release.log`, `def036-release-exit.json` |
| DEF-037 | SYNC-22 | RETEST_PASS | Permanent REJECTED сохранялся в Room с безопасным `errorCode`, но normal PoruchikApp summary не использовал `summary.rejected`, а TasksScreen не читал REJECTED/errorCode | Пользователь видел только общий итог синхронизации и не знал, какая операция окончательно отклонена | Mobile, `PoruchikDao.kt`+`PoruchikApp.kt` | Before targeted FAIL3,313с. APK15 same test PASS6,038с: persisted/reopen notice, no resend/cursor31/later change; ack watermark `(createdAt,operationId)` скрывает старые без изменения outbox; invalid raw code→`OPERATION_REJECTED`, payload hidden. `APK15-final-proof.json`: SHA`09072131…bfee1d`, signer прежний, обе AVD14таблиц exact, PIN сохранён |
| DEF-038 | EX-015 | RETEST_PASS | Official `RevokeDevice(A)` отзывал device/SSH/session/challenge, но не отключал push registration; recipient и READY candidate SQL не исключали revoked device | Отозванное устройство больше не проходит auth и не выбирается для новой push-доставки; device B не затронут | Backend Gateway/TaskCore FCM selection | `ex015-revoke-push-red.log` → `ex015-revoke-push-green.log`: actual-PG1/1, A registration/recipient/candidate false, A-only DLQ, A+B READY, attempts unchanged, B exact. DEF038 attempt2 `RELEASE_VERIFIED`: exact live JAR/ELF и images, healthy/readiness/restarts0, env/networks/config/counts unchanged. Attempt1 дал ложный ENV_CHANGED из-за порядка Docker Env и read-only подтвердил exact OLD rollback; SENDING, прошедшая pre-send guard до observed revoke, остаётся честной in-flight границей. |
| DEF-039 | EX-062 | RETEST_PASS | После исходного protected401 и definitive `AUTH_INVALID` на refresh challenge клиент сохранял `Authenticated`, показывал кешированный защищённый интерфейс и не предлагал переподключение | Отозванный клиент не загружал изменения, но оставался в ложном авторизованном состоянии | Android `RefreshingMobileApi` / `AuthCoordinator` / `AuthGate` | Red: `M/EX062-recovery-red-final.log`. Green: `M/DEF039-recovery-green.log`, state, `M/APK16-final-proof.json`: terminalSignal1/challenge4011/upload0/blockedRepeat2, durable reopen, cancel preserves, explicit confirmation wipe1, failed enrollment no cached access; unit covers IOException/503/initial401-success/challenge+refresh401. APK16 exact both AVD, signer same,14 Room tables/PIN preserved. |
| DEF-040 | ASN-08, ASN-09 | RETEST_PASS | Получатель принятого назначения с TASK_ASSIGN обходил canRedelegate=false и передавал корень вне исходной документной ветки | Риск несанкционированного переназначения и расширения доступа закрыт | TaskCore, коммит56c9bca, выпуск принят | `redelegation-gap-red.log`:2 FAIL → `assignment-acl-gaps-final.log`:5/5 PASS/0 skip,5 rollback0/provider0. `def040-release-result.json`: RELEASE_VERIFIED, exact image/JAR, backup verified, healthy/readinessUP/restart0, Env/networks/config/schema unchanged, rollback not needed. Подписанный APK не изменялся. |

## Дефекты тестовой оснастки

| ID | Case ID | Состояние | Сигнатура | Почему не product FAIL | Следующая проверка |
|---|---|---|---|---|---|
| H-001 | NET-12 | CLOSED_LOCAL, общий Android gate | `OkHttpMobileApiTest`: read-timeout expected `RESPONSE_HEADERS_START`, actual `REQUEST_HEADERS_START` | MockWebServer `setBodyDelay(500)` задерживает момент до выдачи headers; passive listener корректно хранит последнюю фактически достигнутую фазу | В пакетной правке сделать fixture, где headers доказанно выданы до задержки body; затем проверить allowlisted `READ_TIMEOUT`, отсутствие message/URL/headers/body и неизменность запроса |
| H-002 | ENV-07, NAV-01, UI smoke dependency | BLOCKED_FIXTURE | На authenticated API35 AVD `NavigationSmokeTest` 3/3 не увидел tabs; сразу после runner пакет отсутствовал, а explicit MainActivity start вернул `Error type 3` | Подтверждено: UTP после instrumentation удаляет tested package и вместе с ним authenticated fixture; это жизненный цикл тестовой оснастки, а не потеря данных продуктом. Mobile разделяет disposable instrumentation AVD и persistent E2E AVD | Исправить harness так, чтобы он устанавливал exact APK и создавал/сохранял isolated authenticated fixture на время трёх smoke cases; после teardown проверить отсутствие влияния на другие AVD |
| H-003 | OPS-03, EX-059, release governance | CLOSED_LOCAL, bee1e4a | `python scripts/verify-source-hygiene.py .` завершается exit 1: запрещёнными считает пять tracked `.md` (`PORUCHIK_INTEGRATION.md`, `MIGRATION_PROVENANCE.md`, `ROLLOUT.md`, `MEMORY.md`, `USER.md`) | Скрипт применяет явную политику «никаких tracked human-readable файлов» ко всему subtree, но актуальный репозиторий намеренно содержит эксплуатационную документацию; это конфликт политики/harness, а не доказанный runtime-дефект | bee1e4a: точные5документов,4runtimeJS,8закреплённыхархивов; архивные regular-file/path ограничения. test-source-hygiene.py5/5 и exact verify-source-hygiene.py PASS107machine/5docs. Полный wrapper не пройден: его frozen-attempt/WIP gate отдельный от H003 |

## Блокеры среды

| ID | Case ID | Состояние | Причина | Попытка | Ближайшая альтернатива |
|---|---|---|---|---|---|
| B-001 | NET-02, NET-03, NET-04, NET-05, NET-06, NET-07, NET-09, EX-003, EX-005..008 | RESOLVED_ENV | Найден официальный portable Go 1.25.1: `.e2e/toolchains/go1.25.1/go/bin/go.exe`; Gateway и Action Executor `go test ./...` завершились PASS | Вторая попытка имела новый вход — точный GOROOT/GOPATH; Docker engine для Go suite не нужен | Case PASS ставится только после mapping test name→oracle; EX-005..008 переведены из BLOCKED в NOT_RUN |
| B-002 | P3 FCM/biometric cases | NOT_RUN_CAPABILITY | Play API37/API35 AVD есть, но token delivery и biometric prompt success/cancel ещё не доказаны | Только preflight boot/GMS/connectivity | Отдельный Play AVD после M1, без физического телефона |

## Закрытые результаты текущего прогона

| Case ID | Статус | Evidence | Ограничение |
|---|---|---|---|
| EX-063 | PASS | `python scripts/test-scheduler-artifacts.py` 5/5; runtime artifact shape | Не заменяет живые scheduler lease/cadence/delivery cases |
| EX-064 | PASS | Тот же прогон; renderer create/reschedule/cancel и Moscow copy | Не доказывает domain transition/TTL |
| EX-065 | PASS | `node --test local/tests/bridge-core.test.mjs` 5/5 | Локальный bridge; не заменяет OpenClaw/Telegram E2E |
| EX-066 | PASS | `node --test openclaw/test-static.mjs` 5/5 | Static allowlist/config/pinning; не заменяет schema parity EX-004 и domain tool cases |
| NET-13 | PASS | Current-tree `OkHttpMobileApiTest`: sink containment и sanitization | Локальный oracle; network phase E2E остаётся `NOT_RUN` |
| SYNC-18 | PASS | Current-tree `OfflineRepositoriesTest`: replacement enqueue failure откатывает supersede | Локальная Room transaction; AVD path отдельно |
| SYNC-20 | PASS | Current-tree `OfflineRepositoriesTest`: wrong-task conflict ID не supersede/enqueue | Локальная Room transaction; AVD path отдельно |
| EX-067 | PASS | `pwsh -NoProfile -File local/tests/run-static.ps1`: bridge 5/5, compose/CLI static contracts terminal PASS | Static local topology; Docker runtime и Telegram provider отдельно |
| OPS-03 | PASS | Production catalog query: 43/43 SECURITY DEFINER functions; owner/search_path/volatility/parallel/ACL matrix exact, PUBLIC grantee отсутствует | Read-only метаданные; domain behavior функций покрывается отдельно |
| OPS-04 | PASS | Production container inventory: все обязательные сервисы healthy; selected runtime restart0; Flyway V1–V9 success; image/plugin/workflow fingerprints recorded | Read-only snapshot; rollback/recreate case OPS-05/EX-060 отдельно |

## Частичные доказательства, не закрывающие кейс целиком

| Case ID | Evidence | Почему статус не изменён |
|---|---|---|
| OPS-03 / EX-059 | `python scripts/verify-yaml.py .`: exit 0, 7 YAML/YML файлов непустые и без duplicate keys | Это только YAML-подмножество release governance; signer/SBOM/locks и полный function ACL oracle не проверены |
| OPS-06 / EX-059 | `python scripts/verify-migration-sentinel.py .`: exit 0; sentinel `20260826.1`, SHA-256 `a356e03c…c36119` совпадает | Подтверждён только закреплённый sentinel одной миграции; clean bootstrap, drift и negative preflight остаются `NOT_RUN` |
| EX-059 | `python -B scripts/test-k4r-evidence.py`: 7/7 PASS; `python -B scripts/test-swap-name-gate.py .`: PASS | Подтверждены strict evidence schemas/derived status/fail-closed coverage и exact swap-name parser; полный release set, signer/SBOM и Linux gate ещё не проверены |

## Правила обновления

- Один продуктовый симптом с общей причиной — одна запись, даже если падает несколько case ID.
- Независимые кейсы продолжаются; останавливается только зависимая ветвь или общий ресурс.
- Исправления объединяются по владельцу/компоненту, затем одна сборка и повтор всех затронутых case ID.
- Suite-level число тестов не превращается в число PASS кейсов без прямого соответствия oracle.

| DEF-015 | PUSH-14 | RETEST_PASS, APK5 | Неизвестное поле content проходит в сохраняемый WorkManager input, хотя parser его игнорирует | Текст из push сохраняется вопреки требованию отбросить лишние поля | Mobile | PUSH14-privacy-family.log: actual WorkSpec.input содержит content, 2 проверки/1 отказ; canonicalData исправление проходит локальные проверки, проверка новой APK ожидается |

| DEF-016 | EX-028, EX-029, EX-031, PRT-03 | SERVER_PROD_RETEST_PASS; plugin deployment BLOCKED | Typed update resultText50001 принят и сохранён вопреки agent-intents.schema maxLength50000; plugin parameter не содержит maxLength, internal DTO не валидируется как mobile @Valid | Разные входные каналы применяют разные ограничения результата | Backend/tools | update-tool-boundaries-null-preserved.log; PG18 workspace46e51c56-f924-476a-b729-df7838dcef4b/task1c8b4d5e-30be-46dc-8996-10e442452dc6 v4 length50001; исправление должно сохранить50000 и отклонить50001 безэффектов |

| DEF-017 | NAV-03 | RETEST_PASS, APK6 | Task link на недоступную задачу после завершённого sync0/0 остаётся в MissingRequestedTask с бесконечным spinner; назад работает, чужого содержимого нет | Пользователь не получает конечного состояния недоступности | Mobile | NAV03-private-link-settled.xml, NAV03-private-refresh.xml; docs/MVP0_QA_MATRIX.md TASK13 прямо требует отсутствие бесконечного ожидания. Прямой GET404 не заявлен, поскольку запрашивается общий sync |

### Проверка пакета DEF-016 и DEF-017

DEF-016: кандидат TaskCore b21e570 проверяет исходные JSON-типы до преобразования DTO. Независимый журнал `def016-numeric-final.log`: 5/5, ошибок и пропусков 0. Строковая и дробная версия, переполнение курсора отклонены без изменения задачи; допустимые операции Inbox, UPDATE, ASSIGN и серверная ветвь AGENT_DERIVED сохранены. Предыдущие четыре нарушения ограничений проверены в общей группе 18/18. Локальные EX-028/029/031 закрыты; это не подтверждение развёртывания plugin в production. Четвёртая попытка запрещена, DEF-014 остаётся блокированным на развёртывании.

DEF-017: APK6 SHA256 `57E08BAA34DA9C0EA4CA8E709F9FBFA92F3F21D1513E5C8252DAF32F2C01A6FC`; `batch6-recovery-ui.log` 4/4 и реальные XML `batch6-NAV03-private-terminal`, `batch6-NAV03-private-retry`, `batch6-NAV03-private-back`, переход с A на отсутствующую B, повтор без сети и `batch6-NAV01-existing-positive.xml`. Недоступная задача завершает ожидание, повтор и возврат работают, прежняя деталь не появляется. При обновлении сохранены 14 задач, 26 операций и 6 сообщений. Прямой GET404 этим не проверен: NAV-03 остаётся частично выполненным.

### Следующий общий пакет доступности интерфейса

| Дефект | Кейсы | Статус | Подтверждённое нарушение | Исполнитель | Доказательства и приёмка |
|---|---|---|---|---|---|
| DEF-018 | ENV-08 | RETEST_PASS, APK7 | При шрифте 2.0 поле сообщения Agent сжато до нечитаемого обрезка; подпись перекрывает соседнюю карточку с клавиатурой. В Inbox пустое состояние обрезано до белой карточки, прокрутки нет | Mobile | ENV08-agent-font2.png/XML, ENV08-agent-keyboard.png/XML, ENV08-inbox-font2.png/XML. Текст ввода и подписи читаемы, кнопки доступны; пустое состояние Inbox доступно целиком через допустимую прокрутку. Один пакет без изменения логики функций |

TaskCore DEF-016 развёрнут отдельно: b21e570, тег `mvp0-typed-validation-20260910-1745`; `def016-prod-oracle.txt` подтверждает допустимое изменение 200 с единственными аудитом/изменением/операцией и строковую версию 400 INTENT_INVALID без дополнительных эффектов. Сервисы healthy, restart0. Plugin не менялся и остаётся блокированным.

NAV-03 закрыт совокупностью APK6 и `TOOLS-5a5af623/nav03-cross-principal-api404.json`: реальный GET чужой PRIVATE c6ccf1a1-06a6-494b-98f9-7c38cb0a3032 от MEMBER d924f0b5-7568-49c2-b95e-6beb0d56d938 вернул 404 без содержимого. API и UI проверены на разных явно изолированных объектах; это не заявление, что UI выполнял этот прямой GET.

TASK-13 остаётся неполным: DEF-002 визуально исправлен, но цепь PRIVATE→TEAM→ASSIGNED с can_read второго участника ещё не выполнена. DOC-13 требует именно отзыва назначения выбранного корня у получателя; уже выполненные GET404 и локальные проверки кеша не заменяют этот сценарий.

### Отзыв доступа: разделение реализованных границ

DOC-13 не является текущим продуктовым FAIL: V9 can_read_document допускает владельца, ACCEPTED assignment с выбранным корнем либо DOCUMENT_BRANCH grant. TEAM не даёт тело документа, PRIVATE не отменяет ACCEPTED assignment. Публичного или внутреннего revoke/complete assignment, а также producer/revoke grant нет. Поэтому исходный сценарий отзыва назначения помечен NOT_IMPLEMENTED и не засчитывается как PASS; новые API ради него не создаются. DEF-007 исправлен в реализованных 403/404, удалении кеша и защите поздних ответов: 16 локальных тестов и настоящий GET404 с последующим открытием без сети.

Независимый `task-access-loss-local.log` подтвердил иной реализованный риск: TEAM task+metadata ранее получены MEMBER, владелец штатно меняет visibility PRIVATE, прямой GET запрещён, следующий sync возвращает прежний cursor550 и changes[]. Область 73813134-5f4f-42e5-943d-aaadc236901e, задача 01ac9c16-9681-496b-a98e-96fe8a17e9c0, получатель 8cd2322d-57ba-434f-a189-bc8fe4c3e389. Это остаток TASK-13/синхронизации, а не доказательство прежнего доступа к телу документа. AVD проверка и граница общего исправления согласуются отдельно.

## Пауза по просьбе пользователя — 10 сентября 2026

Проверки, сборки и сеть остановлены. Каноническая точка продолжения: [PROJECT_HANDOFF.md]([[PROJECT_HANDOFF]]). Срез94 PASS /2 FAIL /202 NOT_RUN, всего298. APK7 8/8 и реальные Agent/Inbox empty положительные; реальные решения font2 ещё не сведены, ENV08 пока FAIL. TASK13 UI не начат, local access-loss regression красный сохранён. TaskCore DEF016 production исправлен, plugin deployment после трёх откатов BLOCKED; попытка4 запрещена. Документация сохранена локально, push/deploy не выполнялись.

## Дефекты, уточнённые после возобновления

| ID | Кейсы | Статус | Нарушение | Доказательство |
|---|---|---|---|---|
| DEF-019 | TASK-13, EX-027 | LOCAL_RETEST_PASS; production/AVD ожидаются | После TEAM→PRIVATE sync не удаляет ранее полученную задачу/метаданные и не продвигает курсор | task-access-loss-local.log красный; task-access-loss-family-final.log 4/4 с V10, без содержимого в targeted TASK DELETE, защита получателей и сохранённых прав |
| DEF-020 | REM-17 | RETEST_PASS, локальные шаблоны и тот же код на production | Общая обрезка длинной сводки удаляет обязательную строку количества объединённых напоминаний; все элементы при этом остаются членами доставки | rem17-digest-boundaries.json, 6/8 PASS, оба materializer варианта с 25 длинными заголовками FAIL, без реальных отправок |

DEF-018 окончательно закрыт: APK7, реальные auth/detail/inbox/agent при font_scale2.0, изображения и XML перечислены в журнале исполнения. Мобильные решения не нажимались; исходный масштаб восстановлен.

| DEF-021 | REM-17, REM-18 | RETEST_PASS, три конкурентных режима на production | Две транзакции проходят один и тот же rate/storm: 6>5, общий delivery+confirmation 6>5, storm21>20 | rem17-rate-concurrency.json; rem17-rate-mixed-concurrency.json; rem17-rate-storm-concurrency.json; PostgreSQL18 assistant_runtime, providerCalls0 |

DEF-020 расширен до всей семьи четырёх workflow: rem17-digest-boundaries.json содержит 16 случаев, 12 PASS/4 FAIL длинной сводки. В одной партии исправления требуется сохранить число объединённых напоминаний в пределах существующего ограничения 4000, максимум5 сообщений и взаимно однозначное включение members.

| DEF-022 | PRT-07 | LOCAL_REVIEW_PASS, ca601c3; production BLOCKED | Plugin client возвращает произвольный upstream error/code и сообщение ошибки транспорта в Error.message | prt07-error-sanitization.mjs/json: 2 новых синтетических варианта FAIL, по1dispatch и businessSuccess=false. Реальные секреты не использовались; показ через фактический SDK/UI этим тестом не проверен |

Кандидат DEF022: только openclaw/plugin/dist/poruchik.js и test/error-sanitization.test.mjs, владение письменно согласовано с backend. Допускаются конечный список известных кодов, нейтральная ошибка транспорта и очищенные AbortError/TimeoutError без исходного cause. def022-local-tests.log: 15/15 (5 новых и 10 затронутых Poruchik), без пропусков. Независимая приёмка передана backend, автор исправления не объявляет его принятым. Ранее пройденные четыре PRT07 fault-проверки отдельно не повторялись. Развёртывание plugin по-прежнему запрещено после трёх попыток.

| DEF-023 | TASK-13 | OPEN, общий feed commit-order | Курсор уходит вперёд за ещё незакоммиченное событие другой транзакции; последующий committed DELETE становится навсегда невидимым клиенту | task-access-commit-order.log, cursor894→899→899, changes[], GET запрещён; local PG18, 2 теста/1FAIL/0skip. Нужна общая защита порядка публикации, не повторное выполнение операции |

Обновление 11 сентября: DEF023 локальный кандидат независимо принят по коду и `task-access-commit-order-fixed.log` (3/3 PASS,0 пропусков). Ранний lock рабочего пространства предотвращает обгон транзакции, освобождается после отмены, не блокирует чтение без изменений и другие пространства. Один дополнительный тест обратного порядка задач в конкурентных пакетах передан в общий регресс. До выпуска и проверки production дефект не закрыт.

DEF020/021: локальный пакет независимо принят. `rem17-digest-legal-fixed.json`24/24 PASS с допустимыми заголовками до300, включая переводы строк и отдельно учтённые объединённые/ожидающие элементы. Три `rem17-rate-*-fixed.json` подтверждают лимиты5/5/20 и ожидание конкурирующей транзакции, реальных вызовов провайдера0. Изменены только бюджет сводки и блокировка политики получателя; периодичность и тихие часы сохранены. Статус production остаётся неподтверждённым.

| DEF-024 | NET-04 | OPEN, пакет неоднозначных заголовков | Два разных Idempotency-Key в одном авторизованном PATCH приняты с200 и изменяют задачу вместо400 без эффектов | `net04-06-fixed.json`, точная QA taskc9202ea5-a0a1-44ac-8e53-2b2177088fc6:v1→v2, аудит/события выросли; raw HTTP, application/merge-patch+json. Остальные варианты собираются до одного исправления gateway |

DEF019 и DEF023: RETEST_PASS по разделённым доказательствам. Последовательный production TASK13 на APK8 подтвердил потерю TASK/metadata и возврат той же версии через назначение; локальные настоящие COMMIT/ROLLBACK гонки проверили порядок транзакций; immutable ee5e19b развёрнут и отдельный `task-feed-live-fence.json` подтвердил реальную раннюю блокировку рабочего пространства. PATCH ждал, read-sync200 за0,174с не ждал, после finally release применилось ровно одно изменениеv9→v10 и одно TASK-событие. Это не объявляется production-гонкой порядка COMMIT. Full TASK13 PASS.

| DEF-025 | EX-008 | OPEN, общий пакет Gateway | Синтетический upstream302 с внешним Location прозрачно возвращён вызывающему клиенту вместо нейтральной служебной ошибки | gateway-def024-build-test.log, TestQAProxyUpstreamFailureAndRedirect: status302, outside.invalid с syntheticcanary. На production этот ответ не воспроизводился; реальные секреты не использовались |
| DEF-026 | ENV-04 | OPEN, сбор мобильного семейства | Черновик Агента исчезает после поворота экрана и повторного PIN, хотя раздел Агент сохраняется | ENV04-agent-draft-input.log: QA_INPUT_EXACT chars44/PASS; ENV04-agent-landscape.xml: rotation1, EditText пустой. Причина в реализации ещё не установлена; видимость кнопок и остальные разделы собираются в тот же пакет |

| DEF-027 | EX-007 | OPEN, тот же пакет Gateway | Подставленные X-Forwarded и X-Action достигают upstream с исходным значением | gateway-def024-025-fixed-build.log, TestQAProxySpoofedHeaderFamily. Синтетическая проверка HTTP proxy; принятие чужих прав на production не утверждается. Исправление должно удалять фактические ключи независимо от регистра перед добавлением доверенных полей |

DEF024/025/027: LOCAL_RETEST_PASS в едином `gateway-header-redirect-sanitize-build.log`. Дублированные заголовки не достигают upstream, redirect нейтрализован, spoof-семейства в двух регистрах удаляются до доверенной идентичности. Локальные EX007/008 закрыты согласно своей среде; production-исправление ещё ожидает выпуска и соответствующего оракула. NET04 остаётся FAIL.

DEF024: RETEST_PASS, `net-header-family-retest.json`13/13 и `net-header-valid-control.json`200/v10, нулевые новые эффекты. DEF025/027: RETEST_PASS на локальных синтетических оракулах с подтверждённым развёртыванием того же immutable Gateway1fabb863…76ad46. Уровни доказательства не смешиваются: synthetic redirect/spoof на production не создавались. Все три дефекта исправлены одной сборкой и одним выпуском.

DEF026: RETEST_PASS на final APK9 27385639…D93A. Текст/выбор переживают настоящий rotation→PIN; реальные landscape/portrait IME PNG независимо подтверждают видимость обеих кнопок, панели возвращаются после закрытия клавиатуры. Compose14/14 дополняет, но не заменяет эту проверку. ENV04 принят по всем пяти разделам; телефон не использовался.

DEF020/021: окончательный пакет принят11 сентября.24 локальные проверки реального jsCode и production source alignment подтверждают сводки; rem17-prod-delivery/mixed/storm.log подтверждают5/5/20 с наблюдаемым PG Lock и отказом превышения. rem17-prod-cleanup.json:33 точные QA-задачи отменены,0 доступных отправок/ожидающих подтверждений,3 политики выключены,0 собственных соединений. Полная приёмка REM17 quiet→digest отдельно не заявляется.


| DEF-028 | NET-07 | OPEN, общий пакет public DTO validation | PATCH принимает неизвестное поле и числовой title200 вопреки additionalProperties:false/string | net07-json-boundaries.json: QA c920… v10→11 от неизвестного поля, v11→12/title123 от числа; audit/outbox+1 и change+2 каждый. Контракт mobile.openapi.yaml updateTask. Typed intent DEF016 — другой вход, не повторный дефект его исправления |

Остальные четыре проверки семейства вернули400 без изменений: malformed JSON, объект description, число dueAt, корневой массив. Заголовок восстановлен корректным merge-patch200/v13. Первая restore415 вызвана двумя Content-Type в тестовом helper; результат известен и доменных эффектов нет. Использована только официальная QA-задача. Исходные красные доказательства сохранены; пакет передан backend после независимого REM05.

DEF028: финальная независимая локальная приёмка завершена 11 сентября после возобновления. 21 уникальный тест PASS, 0 пропусков; реальный MVC/core/PostgreSQL тест и полноценная sync Mutation проверены по сохранённым журналам и исходникам, без повторного прогона. Scoped public mapper не изменяет внутренний или сохранённый JSON. Backend получил gate для одного выпуска; production-статус до canary остаётся OPEN.

DEF028: RETEST_PASS на production, один выпуск7417e95/образ071afd9770…7d26f. def028-public-canary.json10/10:8 ошибочных запросов400 без эффектов, один valid PATCHv13→14, replay без второго эффекта. ReadinessUP/restart0/обе сети подтверждены; JSON-пакет закрыт.

| DEF-029 | SYNC-13 / FOC-04 | RETEST_PASS, APK10 + production/API/UI | Поздний HTTPv2 и старый feed перезаписывают новый Focusv3; HTTP ответ скрывает локальный PENDING-выбор | QueueRoomRecoveryTest:3 красных теста, включая4 UPSERT/DELETE×empty/nonempty ветви. Файловый Room/reopen; transport controlled, не production. Task/cursor защищены. Локальная операция не потеряна, но отображаемый выбор откатывается |

Связанные пути собраны до исправления: RoomSyncStore.replaceFocus, applyFocus и перекрытие локального RoomFocusRepository.replaceOffline. Требуется одно согласованное исправление с сохранением pending/conflict-намерения, новых версий и пустого снимка; один APK после текущей consumer цепи. Три независимых теста файловой очереди зелёные, их не повторять без затрагивающего изменения.
DEF029: итоговое единое семейство подтверждено queue-room-focus-conflict-final.xml:8/8 без пропусков, включая сохранение CONFLICT-выбора после reopen, монотонность версий и ABORT/IGNORE enqueue без повреждения очереди. Одна APK10 собрана mobile; полный статус SYNC13/FOC04 не повышен до её API/UI приёмки.

### DEF029 закрыт одной APK10 и общим Focus-сценарием

SYNC13 и FOC04 PASS; **298 = 152 PASS + 2 FAIL + 144 NOT_RUN**, FAIL REM17/PRT07. APK10 SHA256C4717801313A4D1A149BBBA6CE724C1E8515F01CA534962DCD1449F49CCD1E57, прежний signerb567…aca0. `APK10-update-proof.json`: schema6, пять таблиц сохранены точно, PENDING1/CONFLICT2,18tasks/52outbox/6messages, локальный PIN сохранён. Это успешное обновление debug-кандидата; исходные ENV02/EX018 release и pending CREATE/UPDATE границы автоматически не закрываются.

`FOC04-APK10-proof.json`, conflict/reopened/retry XML и `foc04-final-oracle.jsonl`: независимый официальный клиент A того же владельца применил ea6441b2…v15→16; Android B817337c8… получил CONFLICT, сохранил исходный payload и видимые9300f7f9…/6bf22116…. Извещение о конфликте и выбор пережили настоящий force-stop/PIN. Версия16 получена авторитетным GET; currentVersion в первоначальном конфликте отсутствовала, его не выдумываем. Явный UI retry02cf2f12…expected16 принят один раз какAPPLIED17, исходная операция SUPERSEDED с прежним payload. A/retry имеют поодной idempotency/audit записи; прежний конфликт не создал доменный эффект. Это не трассировка каждого HTTP-повтора, а доказательство точных состояний/эффектов; запрет автоматического повторения terminal дополнительно подтверждают локальные тесты. Оба AVD теперь APK10, main18/53/6, Focus17, сеть восстановлена. Телефон не использовался.
| DEF-030 | AUTH-03; соседний AUTH-18 пока не проверен | RETEST_PASS, APK11 | После локального отказа padded приглашения экран содержал только ошибку и не давал вставить другое приглашение или повторить | `APK11-auth-task-ui.log`: исправленный `AuthEnrollmentRecoveryUiTest` PASS; malformed input не вызывает bootstrap/redeem, состояние неизменно, новая вставка снова доступна. Общий `APK11-final-proof.json`:2/2,8,775с |
| DEF-031 | SYNC-17; повторный production conflict остаётся в SYNC-19 | RETEST_PASS, APK11 | Явное сравнение старого конфликта объявляло актуальнуюv2, но поля оставались прежним черновиком | `APK11-auth-task-ui.log`: исправленный legacy comparison/repository UI PASS; fresh data переносится в draft до явного retry. Общий `APK11-final-proof.json`:2/2,8,775с |

Mobile — единственный исполнитель продуктовых PoruchikAuthGate.kt и TasksScreen.kt; QA меняет отдельный AuthEnrollmentRecoveryUiTest. Существующие AVD сессии не очищаются. Один общий выпуск после сбора семейства и точечной повторной проверки; успешные repository/старые voice/FCM тесты без затрагивающих изменений не повторять.

| DEF-032 | NET-08; общий публичный контракт ошибок MVP0_QA_MATRIX:59 | RETEST_PASS, production proxy commit`689b2c8` | Управляемый HTTP front proxy возвращал plain429 без code/trace | 21-case original/candidate differential сохранил весь live contract, кроме целевой формы429; unit sliding/problem PASS, reproducible binary SHA`71b15b10…79846d9`. `def032-release-result.json`: `RELEASE_VERIFIED`, active/readiness/hash, unit hash unchanged, exact old backup. `def032-canary-result.json`:59 loopback200→429 `application/json`, code`RATE_LIMITED`, traceId=`X-Correlation-Id`. NET08 также закрыт композицией independent-source differential и exact Gateway principal-key/limiter isolation; Gateway120 не является критерием этого исправления |

Порог60 перед Gateway120 сам по себе не признан ошибкой. Исправлен только формат429/trace, с сохранением обеих квот, origin и маршрутизации. NET08 закрыт композицией: live front boundary+429, differential другого source/spoof same source и exact Gateway principal-key/limiter isolation tests. Прямой published121 законно скрыт front60 и не добавляет продуктового oracle.

### Повторная критика необходимости остатка — 11 сентября 2026

Приняты сокращения без ослабления подтверждённых API/UI/OS/production-границ. TGL04 закрыт контрактом без target principal, сохранённым scoped binding lifecycle и NET03; новый spoof query/header прогон отменён. ENV06, PRT04–06 и OPS08 сведены с более точными существующими доказательствами. VOI02/06/07 закрыты композицией настоящего review/edit UI, общего production text-submit и source/storage proof; реальная акустика VOI01/08 не заявлена.

NET12/EX022/NET14 больше не образуют cross-product сетевых вариантов: исполняются только отказ до отправки, неопределённый read-timeout после возможной отправки и offline→restore. EX025 ограничен уникальным low-storage/write-failure oracle; EX058 — одним владельцем состояния на класс ошибки. EX049 actual n8n restart, EX057 OEM/work-profile и EX060 rollback конкретного кандидата исключены из обязательных активных прогонов, но не получили PASS по отмене. Для EX049 durable lease recovery подтверждён новыми SQL-клиентами и историей без дублей; startup/in-flight process crash, OEM profile и конкретный rollback остаются явно непроверенными.

Итоговый hash/diff/secret scan для EX001/EX059/OPS10 выполняется один раз после последнего release-кандидата. Plugin PRT07 остаётся воспроизводимым FAIL на live runtime и одновременно BLOCKED после трёх попыток выпуска исправленного кандидата; четвёртый deploy запрещён. DEF032 и NET08 закрыты. REM17 после исправления DEF020/021 не считается открытым дефектом: actual quiet00–06→digest остаётся NOT_RUN.


### PRT07 — повторная проверка успешна
DEF014/016/022 в части плагина приняты: один проверенный production-выпуск архива1d22bd9f…, installed sanitizer2/2 и SDK identity/HMAC/replay PASS. Доказательства: prt07-resume-release.json, prt07-installed-canary-stdin-result.json, prt01-installed-sdk-result.json. Предыдущие запреты/откаты выше являются историей. Текущий срез256 PASS /0 FAIL /42 NOT_RUN; непроверенные сценарии не объявлены успешными.



| Дефект | Кейс | Статус | Подтверждение |
|---|---|---|---|
| DEF-041 | AGT-06 | RETEST_PASS, production | agent-cancel-effect-red.log: отменённый trusted run выполняет новый CREATE; actual client/controller/PG, rollback |
| DEF-042 | AGT-16 | RETEST_PASS, production | agent-relay-false-success-red.log: actual relay при controlled CLI boundary после client403 формирует SUCCEEDED/toolInvocations=[]; network/provider0 |

Одна связанная пачка, архитектурный обзор принят: signed trustedRunId/atomic effect gate + attempt-scoped outcome journal с разрешением успешного повтора, без текста/аргументов/секретов.



### DEF041/042 — производственная повторная проверка успешна
Принят final script48b182…2a47b и def041042-final-final-readonly.json: точные новые Core/Executor, архив5c594… из7 файлов, relay4c2a…, здоровые службы/сеть/неизменные настройки и DeepSeek; активная работа0. def041042-final-api-run.json: подписанный CREATE с отсутствующим trustedRunId→403 RUN_CONTEXT_INVALID, задачи/аудит неизменны; прежний GET_FOCUS→200 APPLIED, только штатный audit+1. def041042-final-module-run.json: фактические установленные relay/plugin8/8 PASS, сеть/провайдер0. Вместе с четырьмя actual PostgreSQL проверками отмены/идентичности/двух порядков блокировки, Linux forwarding и совместимостью старого request hash это закрывает AGT06/DEF041 и AGT16/DEF042. Удалённый abort уже запущенного провайдера не обещается. Первые два процедурных отказа и точные восстановления сохранены в истории; общий регресс не повторялся. Срез277 PASS /0 FAIL /21 NOT_RUN. Подписанный APK16 не менялся.
