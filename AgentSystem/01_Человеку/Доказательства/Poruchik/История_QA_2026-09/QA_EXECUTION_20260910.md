# Промежуточный прогон QA, 10 сентября 2026

Телефон не используется. Объединённая матрица содержит 298 кейсов. Число модульных тестов не подменяет число закрытых сценариев.

## Локальная проверка PostgreSQL

Поскольку Docker daemon недоступен, создан отдельный одноразовый кластер установленного PostgreSQL 18 на `127.0.0.1:55438`. Production и существующая служба PostgreSQL не затрагивались. После проверки кластер остановлен. Данные оснастки сохранены в локальном каталоге доказательств.

Успешно выполнены:

- Канонические миграции N8NAgents `001`–`011` с нуля.
- Повтор миграций `005`–`011` в предусмотренном порядке.
- `001_contract_tests.sql`, `004_reminders_v2.sql`, `005_reminders_list_contract.sql`.
- `003_memory_langchain_parity.sql` от роли `memory_runtime`.
- Запрет подключения `assistant_runtime` и `memory_runtime` к `n8n_metadata`.
- Ожидаемая ошибка `002_fault_atomicity.sql`: тестовая таблица отсутствует после отката, запись sentinel ровно одна.

В тесте `005` контейнерные пути `\i /migrations/` заменены только в копии оснастки на относительные `infra/postgres/migrations/`. Исходные SQL не изменялись. Первая попытка выявила несовместимость абсолютного контейнерного пути, вторая — обработку кириллицы в абсолютном пути `psql`; относительный путь устранил обе проблемы.

Это частичные доказательства OPS-01/02: они не подтверждают Gateway/TaskCore bootstrap и не заменяют прогон на production-версии PostgreSQL 17.11. Последовательные вызовы claim в существующем SQL не считаются настоящим конкурентным тестом EX-043. Проверки provider 429/400/403 и полного цикла повторной отправки также не закрыты этим набором.

Доказательства: `C:/Users/style/Documents/ChatGPT/Агенты/.e2e/qa-pg18-20260910/` — отдельные журналы миграций, повторов, тестов и остановленного кластера. В нём только локальные тестовые сущности; production-секреты не использовались.

## Защита жизненного цикла агентных запусков

`npm test` в `N8NAgents/openclaw/plugin` завершился с 20 PASS, 0 FAIL, 0 skipped. Проверены текущие файлы рабочего дерева; `plugin-manifest.json` связывает журналы с SHA-256 реализации и тестов.

| Кейс | Статус | Точное доказательство |
|---|---|---|
| REM-10 | PASS | `authoritative reminder selection closes the current tool context after ACTION_REJECTED`: после ошибки прекращаются повторная мутация и посторонний инструмент; ожидаемая последовательность сетевых вызовов проверяется точно; следующий запуск работает |
| REM-11 | PASS | `late ACTION_REJECTED from an older run cannot close the newer run`: отложенный ответ A и позднее завершение A не закрывают B; завершённые A/B запрещают дальнейшие вызовы, последовательность вызовов проверяется точно |
| REM-14 | Частично, статус матрицы не повышен | Успех доменного действия сохраняет `handled: true` при ошибках edit/clear/reply; проверяется один вызов API. Сам SDK dispatcher и повтор callback в данном локальном наборе не исполняются |

Прочие 17 успешных тестов не повышают автоматически составные tool/Telegram E2E кейсы. В частности, production-плагин и локальный текст подтверждения могут отличаться.

## Оснастка выпуска

`verify-contracts.py .` в N8NAgents прошёл, включая отрицательные мутации схем и контракта выпуска. H-003 воспроизведён: `verify-source-hygiene.py` запрещает пять намеренно отслеживаемых документов. Исправление не выполнено по одному дефекту: оно остаётся в общем пакете оснастки.

H-002 уточнён: UTP удаляет пакет после instrumentation и уничтожает предварительно подготовленную авторизацию. Для последующих проверок выделяются разные AVD для instrumentation и длительного пользовательского E2E. Это не доказательство потери данных приложением.

## Следующий независимый пакет scheduler

На том же одноразовом PostgreSQL 18 выполнены новые целевые SQL без внешних отправок. Каждая группа ограничена транзакцией с откатом, включая неуспешный сценарий.

| Кейс | Результат | Доказательство |
|---|---|---|
| EX-041 | PASS | `scheduler-boundaries.sql/.log`: count=3 и until_date включительно создают ровно три occurrence, cursor exhausted=true, повтор materialize не меняет число |
| EX-044 | PASS | `scheduler-terminal.sql/.log`: истёкшая pre-send lease получает новый token, attempt_count=2 и ровно одну запись истории; следующий claim не захватывает её повторно |
| EX-045 | FAIL, DEF-003 | `scheduler-boundaries.sql/.log`: provider retry_after_seconds=3600, фактический retry_at через 120 секунд |
| EX-046/047 | Частично | `scheduler-terminal.sql/.log`: dead_letter/uncertain конечны, три последующих claim не возвращают delivery, повтор finalize отклоняется, history1/attempt1/lease NULL. Классификатор ответа провайдера пока отдельно не исполнен |

Основание DEF-003 — официальный [Telegram Bot API, ResponseParameters](https://core.telegram.org/bots/api#responseparameters): `retry_after` задаёт оставшееся время ожидания до допустимого повторения запроса. Проверено 10 сентября 2026. Это не произвольный порог теста. Классификатор workflow сохраняет значение в метаданных, SQL его игнорирует. Проверка production PostgreSQL 17.11 до выпуска исправления ещё требуется.

OPS-05 повышен в PASS по read-only доказательству backend: `ops05-binding.txt` в `.e2e/runs/MVP0-QA-20260910-TOOLS-1ebebe44`, SHA-256 `bbb9c3b8e4bc2e947e1257cdc3efba22586f659d6e021bf612fe95918034cfa4`. NetworkMode relay совпадает с текущим контейнером OpenClaw, оба healthy/restart0.

Дополнительные результаты того же независимого пакета:

- EX-043 PASS: `scheduler-concurrency.py/.json` — два одновременных PostgreSQL-сеанса с барьером начала и пересекающимися двухсекундными транзакциями получили 10+10 непересекающихся delivery, у всех attempt_count=1.
- EX-046 и EX-047 PASS: `scheduler-classifier.mjs/.log` исполняет неизменённый код workflow-классификатора, ответы 400/403 переходят в dead_letter, неопределённый timeout после отправки — uncertain. Вместе с SQL terminal-проверкой подтверждены отсутствие повторного claim, одна попытка/запись истории и освобождение lease. Внешних Telegram-вызовов нет.
- EX-049 остаётся частичным: после завершения первых SQL-клиентов и истечения lease новые клиенты восстановили ровно 20 delivery один раз, итог attempts=2/history=20. Перезапуск настоящего n8n worker ещё не выполнен.
- OPS-01 FAIL, DEF-005: канонический bootstrap не создаёт `pgcrypto`, хотя SQL авторизации использует `public.hmac`. Прямой вызов `internal_scheduler_authorize` с неверной подписью падает отсутствующей функцией вместо контролируемого отказа. `scheduler-auth-direct.log` подтверждает ошибку внутри продуктовой функции. Поиск по init, migrations, scripts и документации N8NAgents не обнаружил отдельного предусмотренного шага установки расширения. Production не изменялся.
- FOC-01 FAIL, DEF-004: свежий principal не может сохранить фокус; сервер возвращает embedded VERSION_CONFLICT при отсутствующем snapshot. Доказательство backend `backend-oracle-batch1.txt`, SHA-256 `94b50ffb9e1f6f490375e936891953148638488f66e88b230c818186132c40f4`.

Присланный backend результат обычного создания задачи не закрывает TASK-02 полностью: в каноническом кейсе требуется проверить границы 1 и 300 Unicode-символов. Настройка срока и один переход PLANNED→IN_PROGRESS также не закрывают оставшиеся подшаги TASK-11/15.

Поворот экрана дважды вызывает запрос PIN, но сохраняет раздел и несохранённый черновик. Явного требования не блокироваться при смене конфигурации не найдено: пока это наблюдение удобства использования, не отдельный дефект и не основание менять политику безопасности в текущем пакете.

EX-048 PASS подтверждён `scheduler-completion.sql/.log`: исходная доставка и ровно два разрешённых повтора фиксируются как sent; следующий повтор не создаётся. Подтверждённый snooze создаёт одну отложенную доставку, complete отменяет её, следующая occurrence периодического напоминания остаётся открытой. Повторная материализация не создаёт дублей. Использованы только локальные тестовые данные и фиктивные provider message IDs, сетевой отправки не было.

DEF-006/TASK-18 FAIL подтверждён совместным AVD/API наблюдением: status применён с версией3, сохранение результата отправляет устаревшую версию2 и получает CONFLICTcurrent3. Серверный result остаётся NULL, редактор результата очищен и после повторного открытия. `TASK18-result-conflict-reopen-empty.xml` относится к операции `2535bb74…`. Сохранность payload в outbox ещё проверяется; удаление данных Room этим наблюдением не утверждается.

Уточнение DEF-001: баннер сравнения появился после двух отдельных сохранений срока: первое применилось, второе получило настоящий конфликт. Ошибка здесь — потеря изменённых полей при compare, а не ложное распознавание единственного собственного сохранения как внешнего изменения.

В конце локального пакета PostgreSQL-кластер снова остановлен. Активных локальных проверок или внешних отправок не осталось. Срез матрицы: 298 кейсов, PASS19 / FAIL6 / NOT_RUN273. Дальнейшие статусы обновляются по готовым доказательствам коллег, без ожидания конца всей матрицы.

Кандидат ошибки offline-документа снят до регистрации дефекта. После сетевой ошибки уведомление сдвинуло дерево, и координатное нажатие открыло другой, ранее не закешированный root. Повтор по фактическим bounds дочернего документа показал точный текст и «Сохранённая копия» (`DOC-child-offline-verified.xml`, онлайн — `DOC-child-online.xml`). Это ошибка оснастки, не отсутствие кеша в продукте. Присваивание полного DOC-09/10 PASS требует дополнительно связать ID и checksum строки кеша с сервером.

Первый пакет исправлений ограничен DEF-001–006 и затронутыми ими регрессиями. Поворот с сохранённым черновиком и отображение Markdown как исходного текста не добавляются как улучшения без нарушения принятого контракта. Общая матрица 298 не расширяется.

## Исправление H-003 в том же пакете

Коммит N8NAgents `bee1e4a` меняет только `scripts/verify-source-hygiene.py` и добавляет его узкий CLI-регресс `scripts/test-source-hygiene.py`.

Разрешены точные пять эксплуатационных документов. После устранения первого отказа обнаружились ранее скрытые исключения для четырёх отслеживаемых JavaScript-файлов плагина и восьми закреплённых npm-архивов установки/отката. Добавлены только точные пути этих артефактов. Для архивов проверяются обычные файлы допустимой структуры npm-пакета; ссылки, обход пути и посторонние файлы запрещены. Это не сканер содержимого на секреты; прежняя классификация запрещённых путей сохранена.

Пять CLI-тестов прошли: допустимый набор, посторонние документы, несовпадающий регистр/каталог, `.env`/`.pem`/посторонний архив, допустимые и опасные архивные элементы. Рабочий `verify-source-hygiene.py .` теперь PASS: 107 машинных файлов, 5 документов. Чужие файлы не добавлялись в коммит и не откатывались.

Это закрывает локальную ошибку H-003, но не весь EX-059: общий legacy wrapper дополнительно требует отдельного замороженного списка изменений, точного release-коммита и Linux-доказательств. Эти ворота не ослаблялись.

Целостное ревью Android-пакета перед сборкой передало три связанных замечания исполнителю одновременно: сравнение должно завершиться успешной загрузкой нужной версии до разрешения Save; сбой сохранения result-retry должен вернуть ошибку с откатом вместо необработанного исключения; миграция Room5→6 требует проверки существующих данных, pending outbox, пустой/непустой версии фокуса и очистки при смене пользователя. Политика vault не меняется.

Повторное целостное ревью подтвердило устранение этих замечаний. Общий gate tasks/sync/network/database/focus прошёл, включая файл БД до/после миграции, повторное открытие, откат замены конфликтной операции и наложение только изменённых полей. H-001 исправлен увеличением интервалов оснастки до body delay1500ms/read timeout500ms; ожидание фактической фазы и проверки очистки телеметрии сохранены. Приёмка production-дефектов требует следующего AVD-прогона единого APK и не заменяется этим ревью.

DOC-09 и DOC-10 повышены в PASS: онлайн/офлайн XML связаны с `mobile-20260910/room-proof.json`. Для дочернего документа `b5effc94…` checksum сервера и фактического UTF-8 содержимого кеша совпадают: `9e87115d5d9ff8d33ccbd0f2d8bc69900cae0da9bc8dd9bf704849455e71bd74`; офлайн показаны точный текст и «Сохранённая копия». Тот же proof подтверждает, что результат операции `2535bb74-e17f-4cb1-a63a-7b2d0c63e7fc` остаётся в outbox со stateCONFLICT: DEF-006 касается восстановления редактора, а не удаления payload из БД.

## Повторная проверка первого пакета и следующая ветка

Независимый чистый PostgreSQL18 на127.0.0.1:55438: миграции001–012 и повтор005–012 прошли. Прошли канонические001/004, новый006 (Telegram retry_after7200 и неверный формат), проверки подписи/отрицательных вариантов/replay, границ повторений, аренды, окончательных ошибок, snooze/complete. `reminders_finalize_delivery` сохранила владельцаassistant_owner, фиксированный search_path и исполнение только owner/runtime. Расширениеpgcrypto установлено вpublic владельцемpostgres; runtime не имеет CREATE вpublic. Доказательства: `.e2e/qa-pg18-batch1-20260910`. Это локальныйPG18, не подтверждениеэквивалентностиproductionPG17.

Следующая независимая ветка выявила DEF-007/DOC-13: `DocumentCacheAuthorizationTest` выполнил три проверки существующего Room-репозитория. ВременныйIOException вернул проверенный кеш (PASS); явныеAPI403 и404 также вернули прежний Markdown какSuccess вместо ошибки (дваFAIL). Тестовый файл добавлен отдельно; продуктовый код и единый APK первого пакета не менялись. Это отличается от прежнего ошибочного нажатия на другой документ: здесь сервер явно запрещает доступ, а не недоступен по сети. Связанный путь DELETE метаданных требует проверки инвалидирования содержимого при отзыве назначения. Источник oracle — существующие DOC12/13, новые кейсы не добавлялись. Общий срез298: PASS21 / FAIL7 / NOT_RUN270.

Связанный сценарий DOC-13 подтверждён отдельно: после онлайн-загрузки `RoomSyncStore.applyChanges(DocumentMetadataChangeDto DELETE)` удаляет метаданные, но оставляет строку `document_content`. Итог узкого теста — четыре проверки: IOException PASS; явные403/404 и инвалидирование содержимого после DELETE FAIL. XML и журнал сохранены в текущем каталоге исполнения. Это один общий дефект отзыва доступа, не три отдельных дефекта.

Дополнительно после012 прошли005 list-contract,003 memory-runtime, повторная проверка source-hygiene и пять её тестов; два параллельных SQL-клиента разделили20 доставок10+10 без пересечений, затем после истечения аренды восстановили20 ровно по одному разу. Это SQL-часть EX-049; перезапуск настоящего n8n-worker ею не подтверждается. Локальный кластер55438 остановлен по завершении проверок.

REM-09 закрыт независимым локальным исполнением настоящего plugin-кода: `rem09-selection.mjs/.json` проверяет cancel и reschedule без списка и с pending_action_id вместо task_id (четыре сочетания). В каждом случае получен REMINDER_SELECTION_REQUIRED, дальнейшие list/reschedule того же запуска заблокированы; после отказа ни одного HTTP-вызова, запросов изменения в Action API нет. База не затрагивалась, сетевые отправки отсутствовали; нулевой эффект следует из недостижимости API изменения, а не из подменённого ответа БД. Общая матрица298: PASS22 / FAIL7 / NOT_RUN269.

## Сквозной повтор DEF-001 и DEF-006 на первом APK

`backend-def001-def006-proof.json` связан с `DEF001-compare-v4-preserved.xml`, `DEF006-before-retry.xml` и `DEF001-006-room-retest.json`. Подписанное внешнее изменение подняло задачуv3→4 (audit282). Явное сравнение сохранило локальныеtitle/TEAM, получило серверdescription и сохранилоdue/status; одна новая операция примениласьv4→5 (audit284), прежняя конфликтная операция сталаSUPERSEDED. SYNC-16 функционально PASS. Пояснение сравнения всё ещё использует старую conflict.currentVersion; DEF-001 остаётся частично исправленным до исправления текста во втором пакете. Legacy-конфликт, повторный конфликт и полный двухэмуляторныйE2E этим результатом не закрыты.

Старый результат E2E_UI_RESULT_A восстановился после обновления APK; явный retry применилась ровно одна операцияv5→6 (audit285), исходная2535… сталаSUPERSEDED, новая18ec… APPLIED. Остальные поля сохранены, редактор очистился после применения. Основной DEF-006 повторно проверен; TASK-18 переведён из исходногоFAIL вNOT_RUN с частичным доказательством, поскольку варианты пустого результата/50001символ ещё не выполнены. Текущий срез298: PASS23 / FAIL5 / NOT_RUN270.

AUTH-01 закрыт после read-only сверки семейства сессий исполнителем backend. `auth-agent-oracle.sql` и `auth01-fingerprint.sql` задают точные запросы: приглашениеdee38462… потреблено один раз для OWNER332bed94…/principal7b53cf53…; fingerprint указан, hash устройства приглашения совпадает с единственным active device076cf48e…; семейство одно, поколения1–4 отражают ротацию, активная сессия одна. Исходное AUTH_SESSION=0 было неверным критерием промежуточного снимка, а не отсутствием успешного redeem. UI входа и последующие authenticated операции уже сохранены. AGT-01 имеет доказательства taskless run431a72c5…/conversationa6c2dc0d…, SUCCEEDEDv3/attempt1, dispatch1 ACCEPTED, audit1 и трёх штатных lifecycle outbox-событий; однако отправленный UI prompt был ASCII, отдельный вариант не-ASCII ещё не подтверждён.

REM-15 прошёл в локальном SQL и настоящих Code nodes workflow: отправка подтверждения увеличивает счётчик ровно один раз, next_confirmation_at сохраняет cadence30мин с quiet-hours, раннийclaim пуст, TTL и max-count терминальны. Русские предложения create/reschedule/cancel содержат11.09.2026 16:45МСК и действия исходных кнопок, не содержат callback UUID, кодовR-… или сырогоreschedule. Старое расписание явно сохраняется до подтверждения. `confirmation-boundaries.sql/.log` и `confirmation-workflow.mjs/.json` сохранены в `.e2e/qa-pg18-batch1-20260910`.

Общий локальный gate Android второго пакета:16 проверок отзыва доступа (включая first/known GET/LIST, DELETE,403/404, omission полного списка, повторное открытие, сетевые ошибки, соседнюю задачу) +3 прежнихdocument-теста +7 тестов текста/состояния задачи прошли. Общий счётчик in-flight ответов привязан к объекту RoomБД; проверка и инвалидирование выполняются в транзакции, миграции не добавлялись. Это локальная проверка; DOC-13 остаётся FAIL до AVD/API повтора отзыва назначения на APK2. Текущий срез298: PASS25 / FAIL5 / NOT_RUN268.

OPS-01 PASS: в чистой локальной БД последовательно применены TaskCoreV1–V9 с schemas=poruchik/default-schema=poruchik из application.yml, затем GatewayidentityV3. До них в отдельной чистой БД применены N8N001–013. Сохранены журналы каждой миграции и metadata/ACL. Установка через отдельныеpsql-соединения сначала не воспроизводила search_path Flyway и остановилась наV6; это исправлено параметром окружения в новой пустойБД, без измененияSQL. DEF-005 закрывает дефект чистогоbootstrap: вproductionpgcrypto уже было установлено раньше, поэтому не утверждается, что production ранеебыл неисправен по этой причине.

TASK-18 PASS после связывания трёх ветвей: пустой результат отключает родительскуюкнопку Save (`TASK18-B-result-empty.xml`),50001символ отклонены самим приложением сRESULT_TOO_LONG (`TASK18-result-50001-rejected.xml`), текст сохранён, TaskBv1/outbox15 не изменились (`TASK18-after-50001.json`, до/после наблюдения исполнителя). Устаревшая версия/восстановление/retry подтверждены прежними DEF006 UI/Room/API доказательствами. Accessibility обрезает длинный echo до5000; это не принималось за длину фактического ввода.

Срез298: PASS27 / FAIL4 / NOT_RUN267. Оставшиеся FAIL в матрице относятся к составным TASK13, FOC01, DOC13 и EX045; их локальные исправления/частичные ретесты не подменяют ещё не выполненные сквозные шаги.


## Контрольный срез второго пакета

После окончательной правки диапазона retry_after чистые N8N001–013 и канонические тесты001/004/006/007 прошли на локальном PostgreSQL18. Значение1000000000 не обрезается SQL-парсером; для подтверждения срок ограничивает его TTL, не ранний повтор. Восемь сочетаний настоящих классификаторов и SQL-финализаторов (четыре workflow, одиночная отправка и сводка) сохранили7200 секунд. Это локальные проверки без отправки Telegram; производство013 и импорт workflow ими не подтверждены. Артефакты: qa-pg18-batch2-20260910/final-*.log, pipeline-429-result.json.

Поддерживаемый повтор N8N002–013 и GatewayV3 прошёл без изменения схемы, ACL или данных21/33таблиц. ops02-result.json хранит SHA-256 схемы и отсортированных строк до/после. В pg_dump исключены только случайные служебные restrict-токены. Для полного OPS-02 отдельно сверяется повтор Flyway по его истории; сырые неидемпотентные CREATE TABLE не запускаются повторно.

DOC007 на втором APK: после удаления только разрешённого тестового дочернего документа реальный GET404 очистил его метаданные и содержимое, сохранив родительский документ (0/0 против1/1). После повторного открытия офлайн закрытый дочерний текст не показан. DOC007-GET404.xml, GET404-purge-proof.json и DOC007-offline-reopen-after-404.xml. Это подготовка данных через контролируемое удаление, не доказательство пользовательской функции удаления или отзыва назначения. Событие DELETE202 пока не было получено синхронизацией (клиент201), поэтому эта ветвь не объявлена PASS. Проверенная сетевая ошибка до отзыва использует точную копию: DOC007-offline-child-reopened.xml; старое наблюдение без смены выбранного документа не считается новым HTTP-запросом.

Для следующего общего пакета зарегистрированы два воспроизведённых нарушения: FOC-01 не показывает перестановку строк в редакторе, хотя сохранённый порядок верен; CAL-04 возвращает из детали календаря в список задач. Текущий APK не пересобирался ради отдельных находок. Срез298: PASS27 / FAIL5 / NOT_RUN266.

EX-004 PASS: ex004-tools.mjs/.json выполняет реальные TypeBox-описания и callback регистрации. Точное множество18 имён совпадает с конфигурацией allow; каждое зарегистрировано один раз, все поля схем типизированы, required ссылается на существующие поля, additionalProperties запрещены. Точное множество13 intent совпадает с Action Executor; выключенный флаг оставляет5 reminder-инструментов. SDK-обёртка tool заменена регистратором без побочных действий; это проверка регистрации и схем, не диспетчера Telegram. Срез298: PASS28 / FAIL5 / NOT_RUN265.
REM-14: два существующих теста обработчика прошли (rem14-handler.log): ошибка edit вызывает clear и один ответ; при отказе всех UI-операций возвращается handled:true после одного Action API. Фактическая дедупликация SDK ещё не проверена; локального SDK в доступных каталогах не найдено. Название теста само по себе не считается доказательством повторного callback.

OPS-02 PASS дополнен существующими Surefire-журналами TaskCore: Flyway проверил10 миграций, текущая версия9, No migration necessary в нескольких контекстах интеграционных тестов. Сохранены только безопасные строки в ops02-flyway-existing.log. Они подтверждают штатный повтор через историю, а независимый повтор SQL для поддерживаемых N8N/Gateway подтверждён неизменными hash схемы/данных. Новый запуск полного Maven не требовался. Срез298: PASS29 / FAIL5 / NOT_RUN264.

NAV-01/05/08 PASS: cold task link после PIN открывает точную кешированную задачу; ссылка старого диалога показывает только его два сообщения; оба возврата из обычной детали ведут в список. XML NAV01-cold-exact-task, NAV05-old-history-explicit, NAV08-system-back/button-back связаны с прежними ID/Room.

DEF-003 и EX-045 PASS: source equality всех четырёх production workflow совпала с проверенными исходниками; PG17 BEGIN/ROLLBACK проверил delivery/confirmation7200/1000000000/неверный формат/TTL без отправки. Три функции сохраняют owner assistant_owner, SECURITY DEFINER, search_path pg_catalog,app, EXECUTE только owner/runtime. Артефакты n8n013-workflow-source-equality.txt и n8n013-prod-rollback.sql/.log. Новые повторные scheduler-прогоны не нужны.

EX-003 FAIL по отсутствующему публичному DELETE devices в OpenAPI. Состав маршрутов получен машинно из OpenAPI/Go mux/Java annotations; внутренние relay claim/complete/fail также вынесены в реестр расхождений.

Срез: {"PASS": 33, "FAIL": 5, "NOT_RUN": 260, "BLOCKED": 0}; частичных NOT_RUN 36. Подробности в QA_PARTIAL_COVERAGE_20260910.md.

AUTH-13 и TASK-19 PASS по api-case-mapping.json и связанным API/DB журналам отдельного workspace8012ec66: истёкший access401→challenge201→ES256refresh200→защищённый200, семья одна/поколения1–2/active1; create/update replay не создаёт повторов, изменённый payload того же ключа отвергнут.
TASK-17 PASS: точный файл50000UTF16=49999codepoints=99979bytes, SHA256 a4c3b961e311f1248287e8b083fb078139d49da4c92683a88ab8225dca23ee1c совпал с production; resultv2→DONEv3, аудиты/outbox по одному на изменение, связанных TaskRun0, старый независимый taskless run неизменен.
AGT-01/13 PASS: Unicode-команда породила один run6433ce24/conversationf37f27c9, SUCCEEDEDv3/attempt1/dispatch1/audit1/три штатных lifecycle events. Ровно один ответ E2E_UNICODE_ЁЖИК🙂 в XML, SHA256 совпал с Room. Старый и новый диалоги имеют по два сообщения; после явного выбора старого, force-stop/plain launch/PIN восстановлен новый. task17-agent-unicode-oracle.txt + AGT13-history-proof.json + AGT13-latest-after-old-selection.xml.
VOI-03/05 PASS: реальная отмена Google recognizer сохраняет черновик/Room без отправки; три instrumentation UI проверки на disposable5562 (voice-ui-test-proof.json) дополнительно проверяют пустой результат и отмену, отсутствие review/send и точный сохранённый текст. AcousticRecognitionVerified=false: реальные русские фразы VOI01/08 этим не подтверждены. VOI02 остаётся частичным: fake repository подтвердил один отредактированный submit, но не production run после этого review.
Срез298: PASS40 / FAIL5 / NOT_RUN253. Proxy EX005–008 только скомпилированы: запуск exe отклонён Windows Application Control; перенос для обхода политики не выполнялся, продуктовый отказ не заявлен. Локальный PostgreSQL55438 остановлен после завершения scheduler/OPS проверок.

TASK-14 PASS после отдельных MEMBER PATCH description/due/visibility404, GET404/list exclusion и spoofed owner/workspace headers: задачаv2/private/due, audit2/outbox2 неизменны, can_read=false, MEMBER idempotency0. member-acl-batch.json и member-acl-db-oracle.txt в workspace8012ec66. INB-01 и INB-08 пока частичные: API порядок/решения/изоляция есть, UI/ссылки полностью не проверены. INB-02/07 воспроизведены как DEF011/012 и входят в третий общий пакет.

Независимые read-tools: ReadToolContractIntegrationTest —5/5 PASS,0 skipped, native PG18 в отдельной qa_read_tools; V1–V9 автоматически применены Flyway. read-tool-intent.mjs вызывает настоящий plugin client с подменённым транспортом; его реальный intent передаётся подписанному typed controller, далее настоящему доменному сервису и БД. Проверены LIST_TASKS фильтры/owner-member scope/неверная дата, GET_FOCUS пустой/упорядоченный, GET_CALENDAR московский полуоткрытый интервал/неверная дата, GET_INBOX порядок/продолжение/изоляция, READ_DOCUMENTS точный Unicode/checksum/чужая ветвь. Снимок доменных данных до/после одинаков; аудиты чтения/nonce/idempotency не принимаются за изменение пользовательских данных. Пять workspace UUID записаны в read-tools-native-fixtures.json, журнал и summary рядом. Это локальная интеграция, не production/provider E2E; оставшиеся варианты составных EX030/033/035/036/037 ещё не выполнены.

Срез298: PASS41 / FAIL7 / NOT_RUN250. Новые отказы Inbox собираются с FOC/CAL/контрактами до общей сборки, не отдельными выпусками.

DEF-013 подтверждает нарушение прямого oracle INB03/04/05 и APP03: доменные решения применены, но связанный Inbox не RESOLVED. Оба assignment завершилисьv2 с audit DECIDE2, changeASSIGNMENT4; ACCEPT дал TASK UPSERT1 и can_read=true, DECLINE оставил can_read=false. Их inbox всё ещё UNREADv1. Для approve/reject та же сигнатура в mobile-inbox-decisions-oracle.txt основного прогона1ebebe44. Кейс ASN-03 самого DECLINE и ASN-04 wrong actor404/no accepted effect прошли отдельно; API-успех решения не подменяет провал разрешения Входящих.

Общий третий пакет включает FOC feedback, возврат календаря, контракт маршрутов, проверку afterSequence, offline READ/version/recovery, атомарное разрешение Входящих после решений. Новый GET inbox/id не добавляется: текущий контракт имеет только PATCH; для ремонта используется существующий авторитетный список и exactID.

Срез298: PASS43 / FAIL11 / NOT_RUN244. Все пользовательские данные вне точных QA fixtures сохранены.

Повторное ревью третьего пакета завершено отдельно для Android и сервера. В Android проверены защита маршрута от запоздавшего восстановления истории, сохранение счётчика входящих переходов при пересоздании Activity и скрытие кнопок решений для RESOLVED. Разрешена одна сборка APK3; семь подготовленных проверок интерфейса ещё должны исполниться на ней. Холодная блокировка PIN не ослаблялась.

На сервере проверено атомарное разрешение связанных Входящих для семи исходов assignment/approval/proposal: исходная транзакция, блокировка доменной строки, точное ограничение workspace/recipient/kind/subject, повышение версии только для ещё не разрешённых событий, одна запись изменения на событие. Отрицательный afterSequence отклоняется с кодом INBOX_SEQUENCE_INVALID. Локальные результаты исполнителя: 4 целевых и 38 полных интеграционных проверок на чистой PostgreSQL 18, без пропусков. Независимое ревью разрешает обновление TaskCore; успешное обновление и повторные проверки на проме пока не заявлены. Небольшое уточнение схемы nullable/blank errorCode в закрытом dispatch API передано исполнителю и не затрагивает серверный код.

Сопоставление частичного покрытия обновлено: из 244 NOT_RUN у 50 имеются конкретные частичные доказательства, у 194 пока нет сопоставленного результата текущего прогона. Частичное покрытие не повышает статус до PASS.

EX-003 и DEF-010 закрыты после машинной проверки обновлённых спецификаций: 30 публичных и 13 внутренних маршрутов точно совпадают с обработчиками. Доказательство ex003-routes-batch3.json сохранено отдельно от исходного отказа. Уточнения nullable/blank errorCode для обоих закрытых dispatch запросов вошли в c8a0710.

EX-036 закрыт локальным интеграционным прогоном существующего ReadToolContractIntegrationTest: начало с нуля, продолжение по последовательности, изоляция двух получателей, отрицательное значение с точным HTTP400/INBOX_SEQUENCE_INVALID и неизменность доменного снимка. Прогон 1/1 без ошибок и пропусков записан в read-inbox-native-batch3.log. Создана только локальная тестовая область 377fd2a6-7f54-4e92-8c0d-39e10dd87ca8 с тремя событиями; её участники 1e54ae79-6a53-47c2-897c-4c46a6751f14 и 4e65ea12-248b-4c4c-80df-dbc6295d82d6. Использован реальный plugin intent и подписанный контроллер с настоящей PostgreSQL 18, но транспорт plugin подменён; проверка не доказывает production/provider E2E.

Срез 298: PASS45 / FAIL10 / NOT_RUN243. Среди NOT_RUN 49 частичных и 194 без сопоставленного существенного результата текущего прогона.

Следующая целевая группа существующих read-tools тестов завершилась 3/3 без пропусков: read-tools-boundaries-batch3.log. EX-030 и EX-035 закрыты по всем указанным вариантам. Неизвестный строковый status и обратный диапазон дают пустой доступный набор; схема не требует для них отказа, поэтому новый дефект валидации не придумывается. Неверная дата отклоняется, доменный снимок неизменён. Дополнительно GET_FOCUS с несуществующим участником отклонён точным 403/PRINCIPAL_SCOPE_INVALID; malformed context остаётся отдельным непроверенным вариантом EX-033. Локальные области этого запуска: 28d877e3-eab4-4a10-93dc-a477a1a94ba5, 8cf5c160-f6da-432a-b86f-54e25728a055, 0e5e3fbe-e116-4e4b-b5a7-0fa53e843181. Прод и провайдеры в запуске не использовались.

Срез 298: PASS47 / FAIL10 / NOT_RUN241; 47 частичных и 194 без привязанных результатов текущего прогона.

EX-033 закрыт после дополнительного malformed identity варианта: read-focus-malformed-context.json подтверждает PORUCHIK_RESOLVED_IDENTITY_INVALID, один запрос разрешения личности и ноль отправленных intent. Совместно с предыдущими проверками пустой/непустой версии, порядка, двух участников и несуществующего участника это покрывает полный локальный контракт кейса.

Свежие решения approval на c8a0710 и APK3 закрывают INB-05 и APP-03. События aba0f71f-8602-4564-975f-e73ff3757914 и d48da5a9-fede-412c-9106-0125e7dfdbe0 стали RESOLVEDv2; решения 2a7cb1af-30b5-4413-84c1-76592fe474e5 и a8d91ac1-0513-41c4-8ccd-5a235f90d36a — APPROVED/REJECTEDv2, каждое с одним аудитом и двумя записями изменения события, включая создание. Доказательства batch3-decision-post-oracle.txt, batch3-INB-retry-proof.json и XML batch3-INB-approve-A-after/batch3-INB-reject-C. Кнопки решений исчезли. Эти артефакты не подменяют assignment кейсы INB-03/04.

CAL-04 и DEF-009 закрыты по реальному ADB-прогону APK3: возврат кнопкой, системной кнопкой и после поворотов с повторной разблокировкой сохраняет точные координаты строк календаря03–06. batch3-CAL-scroll-proof.json сопоставляет исходный экран и три возврата. Проверен счётчик после нескольких внешних переходов и сброс маршрута при ручной смене вкладки. Ошибки отдельной instrumentation-оснастки не использованы как доказательство продуктового отказа.

Уточнение EX-037 основано на реализованном контракте, а не на желании получить PASS: TaskCoreService.writeDocument вычисляет SHA256 официально записанного Markdown; Models.DocumentView содержит content/checksum/readOnlyForMobile, но не содержит checksumVerified. Контракт чтения возвращает хранимую сумму для потребителя, повторный пересчёт на сервере не обещан. Поэтому прежняя формулировка «verified Markdown» была чрезмерной, а требование серверного отказа после искусственного повреждения строки БД снято. Первоначальный read-doc-integrity-batch3.log сохранён как отклонённое ожидание теста, не продуктовый дефект. Контрольная сумма локальной строки восстановлена в finally.

Исправленный тест read-doc-contract-batch3.log прошёл1/1: media_type application/pdf запрещён ограничением БД; плохой UUID отклонён; официальная запись возвращает точный текст и SHA256, полное и целевое чтение соблюдают ACL; чтения не изменяют доменный снимок. Повреждённый хранимый hash передаётся как есть — это явно зафиксированная граница, проверка потребителем относится к отдельным DOC кейсам. EX-037 закрыт в локальной интеграции, без утверждения production/provider E2E.

Срез 298: PASS52 / FAIL7 / NOT_RUN239; 45 частичных и 194 без привязанных результатов текущего прогона.

EX-034 SET_FOCUS прошёл весь локальный набор:0/1/20 элементов, отказ для21/повторов/чужой задачи/устаревшей версии, точный порядок и число строк при независимом чтении snapshot. Отказы не изменили доменные данные, аудит и журнал изменений. Доступная CANCELLED задача допустима текущим контрактом и сохранилась как единственная запись версии5; запрет терминальных задач не придумывался. focus-write-contract-batch3.log —1/1 без пропусков.

INB-07 закрыт по batch3-INB-offline-proof.json и batch3-generic-read-oracle.txt: событие8e2087ea/sequence38 получило одну операцию e8866014 с expectedVersion1, сохранило её после отключения процесса без сети, затем стало READv2 с одним аудитом. Старый конфликт9ad2ffd8/ecc3a4a0 отдельно восстановлен: исходная операция SUPERSEDED, новая2a8cc5dc APPLIED, серверный аудит также один. DEF-012 закрыт.

INB-03/04 и DEF-013 закрыты свежими реальными решениями assignment после серверного исправления: e41a583f ACCEPTEDv2/Inbox6ff38684 RESOLVEDv2/доступ есть/задача открыта;359d87e6 DECLINEDv2/Inbox56e23e7b RESOLVEDv2/доступа и локальной задачи нет. У каждого один аудит и две записи изменений assignment и Inbox, включая создание. Доказательства batch3-assignment-post-oracle.txt и batch3-INB-assignments-proof.json. Устаревшая отметка OPEN для DEF-004 также исправлена: сохранённая пустая версия4 и последующие версия7/шесть аудитов доказывают устранение первого сохранения; отдельный дефект порядка интерфейса DEF-008 остаётся.

Обнаружен DEF-014 в общей ветви повторного вызова typed tools. Actual plugin при одинаковом toolCallId сохраняет operationId, но генерирует новый correlationId; канонические n8n и Action Executor передают его контроллеру неизменным. Идемпотентность корректно отклоняет изменившееся тело с409, поэтому повтор не получает исходный успех. replay-correlation-proof.json подтверждает равенство остальных полей, а write-tools-local-batch3.log воспроизводит отказ на REQUEST_APPROVAL. Первый эффект singleton, дубликата нет. Production повтор не запускался; статус OPEN_LOCAL_REPRO. Все шесть разрешённых capabilities и отрицательные scope/target/capability варианты отдельно прошли в approval-request-capabilities-batch3.log. Ошибочный replay выделен в отдельный regression test, остальные группы не заблокированы.

Срез 298: PASS56 / FAIL5 / NOT_RUN237; 45 частичных и 192 без привязанных результатов текущего прогона. Точные локальные UUID и количества fixtures записаны в local-tools-fixture-ledger.json.

Следующая локальная ветвь EX-040 проверена без повторения известного блокера: approval-decision-race-batch3.log —1/1. Реальный plugin/controller/PG18 подтвердили APPROVE/REJECT, отказ чужому участнику, два конкурирующих решения с одним победителем и одним APPROVAL_ALREADY_DECIDED. Три связанных Inbox стали RESOLVEDv2, ровно три аудита решений, версия задачи осталась1. Проверка разрешённого эффекта пропускает только APPROVED. Полный EX-040 остаётся частичным до устранения общей ошибки повторного toolCallId DEF-014. Теперь46 частичных и191 без привязок, канонические статусы56/5/237 не менялись.

PUSH-14 уточнён по существующим требованиям: PROJECT_HANDOFF описывает нейтральный FCM и авторитетную загрузку HTTP; MVP0_QA_MATRIX PUSH03 ограничивает серверный payload; PushEnvelopeTest намеренно проверяет отбрасывание content/responseText на клиенте. Явного требования отклонять весь сигнал из-за неизвестного дополнительного поля нет. Ошибочные обязательные поля и конфликтующие идентификаторы по-прежнему должны давать ноль работы/запросов/уведомлений. Лишние поля допустимо игнорировать, но нельзя сохранять, отображать, пересылать или использовать для изменения маршрута/запроса. Это не ослабляет отдельную проверку allowlist серверного payload. PUSH-14 пока не повышен: реальный прогон исполнителя продолжается.

EX-039 дополнен реальным локальным plugin/controller/PG18 прогоном proposal-decision-local-batch3.log —1/1: ACCEPT/REJECT/DELEGATE дают ровно две задачи и одно назначение, три аудита решений и три разрешённых Inbox. DELEGATE без assignee откатывает уже начатое создание задачи и все доменные/аудитные изменения; чужой actor отклонён. Race и общий повтор toolCallId ещё не проверены полностью, поэтому статус кейса остаётся NOT_RUN с частичным покрытием. Все запущенные Maven процессы этой группы завершены; ресурс освобождён перед следующими производственными правками.


### Четвёртый пакет: повтор инструментов и сохранение порядка фокуса

Коммит plugin 2e30dde: независимые 13 инструментов × 2 доверенных контекста прошли проверку стабильного повторного запроса и изоляции запуска (replay-family-regression.json). Реальный plugin → подписанный контроллер → PostgreSQL18: 12/12, без пропусков (batch4-tools-native-normalized.log). Проверены повтор с тем же результатом без второго эффекта, изменённое действие с тем же ключом →409, решения согласований/предложений и гонки. Это локальная интеграция, не повтор через production provider. Первые четыре отказа в batch4-tools-native.log относятся к сравнению внутренних числовых JsonNode разных классов; нормализация через JSON сохраняет поля, числовые значения и порядок массивов. Исходный журнал сохранён. EX038/039/040 закрыты в разрешённом локальном режиме.

FOC01: APK4 5531D7B9…A7855A, фактически видимые перестановки без ручной прокрутки и сохранение/повторное открытие; batch4-FOC-proof.json + batch4-focus-anchor-oracle.txt подтверждают один op ce181a4f, v9→10, точный порядок A/07/08 и один аудит. INB02: inbox-after-sequence-negative-post-c8.json подтверждает production400 на -1, неизменный положительный список до/после; предыдущие границы курсора сохранены.

PUSH08/09: настоящая доставка FCM на APK3, точные INBOX_EVENT e9ca3237 и свежий APPROVAL_DECIDED 2c3c83af, один получатель/одна доставка, разрешённые поля маршрутизации, нейтральное уведомление и переход к точной задаче. Артефакты push-inbox-stage2-oracle.txt, push-approval-fresh-stage3-oracle.txt и мобильные PUSH-inbox-proof.json/PUSH-approval-fresh-proof.json. Предыдущий повтор APPROVAL v2 был корректно подавлен и не использован как положительная проверка.

PUSH14: обнаружен DEF015 — лишний content сохранялся в WorkManager input. Исходный отрицательный результат сохранён; исправление входит в пятый пакет, до проверки новой APK дефект открыт.


EX032 закрыт локально: assignment-tool-local.log, actual plugin/controller/PG18. ACCEPT/DECLINE возвращают v2 и replay тот же результат; wrong actor не меняет audit/change; конкурентные решения дают одного победителя; PRIVATE доступ появляется только после ACCEPT; три задания, три аудита решений, три InboxRESOLVEDv2.

PUSH06/10/11 закрыты на APK4: push-permission-deny-grant-oracle.txt + PUSH-deny/grant-proof.json, permission/notification файлы. Без разрешения событие f259af63 принято и задача обновлена до v3, уведомлений0. После разрешения старое уведомление не появилось; новое событие8adf5cd0 обновило доv4 и создало нейтральное уведомление. PUSH-grant-tapped-before-unlock.xml показывает PIN, PUSH-grant-unlocked-target.xml — точную задачу после разблокировки. Это соответствует background TASK_CHANGED, не подменяет отдельную foreground TASK_CREATED проверку.

PUSH17 закрыт по ранее выполненному чистому PostgreSQL18 gate: batch3-backend-local-gate.txt, FcmPushPublisherIntegrationTest 4/4 в составе38/38. migrationCutoffDoesNotSeedHistoricalBacklog исключает событие до cutoff, seedsExactlyOncePublishesOnlyAfterAcceptanceAndPayloadIsDataOnly подтверждает новое событие, один delivery и published только после принятия. Исходный кейс разрешает локальный контрактный режим; повтор не запускался.

PUSH13 и DEF015: batch5-worker-family.log, 3/3 actual WorkManager на disposable APK5 37338D44…AF24EF. v3→v2/v3 не меняют sync work/уведомление, v4 принят; полный завершённый event replay не создаёт повторный sync/notification. Неизвестный content отсутствует в WorkSpec input, notification и следующем sync input. Реальный FCM TASK_CREATED на основной AVD подтверждает canonical WorkSpec ровно7ключей (PUSH-created-proof.json). Полный PUSH14 ожидает отдельные malformed eventId/entityId, исправление самого DEF015 принято.

PUSH14 закрыт после двух дополнительных malformed UUID: PUSH14-malformed-final.log,1/1 на неизменённой APK5; вместе с batch5-worker-family3/3 покрыты неверные eventId/entityId/route/type/version и отброс лишних данных. Product APK повторно не собиралась.

EX028 закрыт по двум явным границам: create-tool-boundaries-final.log проверяет настоящий typed client→controller (human Unicode/300/replay и invalid zero-effect); derived-create-server-branch-final.log проверяет реализованную документированную серверную ветвь AGENT_DERIVED напрямую подписанным запросом. Результат PROPOSAL_PENDING, tasks0/proposals1/inbox1/auditAGENT_INTENT_CREATE_TASK1, повтор тот же. Plugin splitIntent всегда HUMAN_COMMAND; автоматический producer AGENT_DERIVED отсутствует (NOT_IMPLEMENTED, вне реализованного объёма), не имитировался как действующий путь. PRT03 schema-before-HTTP остаётся отдельной проверкой. Invalid301 direct-call отклонён ограничением БД; HTTP классификация этого вне-schema запроса не проверена.


Развёртывание DEF014: три попытки исчерпаны, четвёртая не запускается. Последняя остановилась после init до recreate из-за неверного предзапускового требования exact-one generation: старое и новое поколения сохраняются одновременно до очистки при старте gateway. QA review ошибочно одобрил это условие. Это блокер процедуры развёртывания, не доказательство дефекта plugin; локальный12/12 и13×2 остаются действительными. Candidate не объявлен действующим на проме; production replay не выполнялся. Backend выполняет/проверяет откат к старому пакету, итоговая проверка состояния ожидается.

EX029 FAIL: title/description/due/null clear прошли после исправления оснастки (Jackson NON_NULL удалял null из входных args, теперь input ALWAYS; это не productbug). Stale/status/date отклонены; resultText50001 принят, authoritative PostgreSQL18 len50001/v4. DEF016 отражает существующий schema max50000, не новый лимит. Последующие terminal/approval подварианты ещё не исполнены, полный case не закрыт.

EX031 закрыт локальным actual plugin/controller/PG18: assign-tool-boundaries.log. Approved TASK_ASSIGN с корнем документа текущей задачи и canRedelegate=true даёт одинPENDING; replay тотже. Missingapproval/stale/документдругойзадачи/несуществующийmember отвергнуты сaudit/change0. Послеуспехаassignment1/Inbox1/ASSIGNMENT_CREATEaudit1, задачаv1, PRIVATEдоступполучателя доaccept отсутствует.

DEF016 расширен как единая существующая семья ограничений, без новых кейсов: typed-validation-family.log выполняет6вариантов заодинпрогон. CREATEdescription20001, UPDATEresult50001, UPDATEбезизменяемыхполей, ASSIGNповторяющийсяdocumentRoot приняты сэффектами вопреки существующим maxLength/minProperties/uniqueItems. UPDATEtitle301 иdescription20001 отклонены атомарно. Поэтому ранее закрытые EX028/031 снова FAIL; ихположительные результаты сохраняются, нонеподменяют отрицательные границы. Исправлениясобираютсяоднимпакетом.

DEF014 последняяпопытка: def014-attempt3-rollback-oracle.txt подтверждает ROLLBACK_VERIFIED, старыйpackage d4139077/source a8e3c08c, activegeneration g-c1c09636d909c7f1, OpenClaw/relay/n8n/TaskCore healthyrestart0, relayпривязанктекущемуOpenClaw. Четвёртойпопыткинесовершалось.

PUSH05 закрыт: foreground TASK_CREATED на APK5, push-task-created-foreground-oracle.txt + PUSH-created-proof.json и UI. Exact event5d28ed6f, taskf45a3c2e v1, publisher/delivery singleton, actual WorkSpec с7canonicalключами, accepted1, нейтральное уведомление и точная задача после нажатия.

Авторизация на реальных AVD: AUTH07-boundaries-proof.json подтверждает шесть UI границ (3/4/12/13 цифр, несовпадение, буквы); сохранение нового PIN не требовалось исходным кейсом. AUTH08-12-proof.json и wrong/correct-pin XML: неверный PIN оставляет экран заблокированным и очищает поле, верный открывает данные; на устройстве без strong enrollment кнопка биометрии отсутствует. AUTH09-reboot-proof.json + reboot-locked XML/reboot-current.png: после настоящей перезагрузки требуется PIN и сохраняются те же13 задач/25 операций/3 записифокуса/6 сообщений. AUTH07/08/09/12 закрыты.

EX020 закрыт после полного дополнения: EX20-phase-proof.json и ex20-doze-server-oracle.txt подтверждают одну операцию после offline→реальной перезагрузки→Doze→автоматического восстановления. EX20-work-proof.json: единственная работа poruchik-on-demand-tunnel-sync, stateSUCCEEDED, runAttemptCount1, EXPONENTIAL/10000ms, незавершённых0. Наличие CONNECTED constraint подтверждено существующим scheduler. Исходные сеть/зарядка/Doze восстановлены; повторного прогона ради чисел не было.

AUTH10/11 закрыты реальным Android BiometricPrompt на disposable AVD: зарегистрированный finger1 открывает Tasks, finger2 даёт Not recognized без доступа, отмена возвращает locked с PIN. Артефакты AUTH10-enrollment-oracle.txt, AUTH10-biometric-prompt/success.xml и AUTH11-biometric-wrong/cancel.xml. Прежний ранний touch до готовности prompt не считался отказом продукта. NAV03 занесён как DEF017 согласно существующему контракту recovery; новую функцию не добавляем.

PRT02 закрыт новым независимым локальным прогоном реального createPoruchikClient: prt02-context-boundaries.json,9контекстов (неверный канал, неowner/нетowner, другойactor/chat, четыре malformedAndroidполя) отвергнуты до сети, network0. PRT08 закрыт уже выполненным replay-family-regression.json13×2: стабильныеoperation/correlation, изменённыйbusinessbody не скрывается, разныеrun изолированы; контроллерный replay/409 доказан batch4-tools-native-normalized.log. Это локальные режимы исходныхкейсов, не currentproductionfix.

EX042 закрыт на разрешённом исходным кейсом локальном контрактном уровне: qa-pg18-batch2-20260910/scheduler-auth.sql.log, канонический internal_scheduler_authorize. Badkey/signature/bodyhash/timestamp/job отвергаются без расходаnonce; корректнаяподписьпринимаетсяодинраз, replayREPLAY_DETECTED, domain/lease delta0, ROLLBACK. Production HTTP/webhook не проверялся и не заявляется. Повтор не запускался.

AUTH16 закрыт в согласованной границе реализованного coordinator: AUTH16-real-coordinator.log1/1, настоящие Android Keystore/session/PIN/Room и best-effort APIlogout; AUTH16-after-logout-ui.xml SignedOut. disposable-logout-server-oracle.txt подтверждает active sessions0/revoked2 однойсемьи, основная AVD осталасьactive. Device row и сервернаяpushregistration остаются; это logout сессии, не device revoke. MainActivity callback очищает локальный pushTokenStore. UI кнопкавыхода не реализована и не добавлялась; AUTH17 повторстарымbearer здесьнепроверен.

ENV05 закрыт реальным OS lifecycle, ENV05-disposable-proof.json и XML/screenshot: HOME→Recents→текущаякарточка сохраняетPID10415 и Tasks безповторногоPIN; послеforce-stop/launch новыйPID11139 и locked. СнимокRecents нераскрываетданные. Это замещает прежний неверно классифицированный MAIN-intent, а не изменение политики блокировки.

## Проверка числовых границ и APK6

Независимый узкий запуск `def016-numeric-final.log` на PG18: 5/5 без ошибок и пропусков, TaskCore b21e570. Красные случаи преобразования версии и переполнения курсора устранены; допустимые UPDATE, ASSIGN, Inbox и серверный AGENT_DERIVED сохранены. EX-028/029/031 закрыты на локальном контрактном уровне. Production plugin остаётся прежним после подтверждённого отката трёх попыток.

AUTH-02 закрыт совокупностью реального ввода приглашения из буфера, экранов после повторного открытия и `disposable-member-redeem-oracle.txt`: приглашение 6c19767d-5819-40fc-b7c2-cefbbda847f9 погашено ровно один раз, principal d65a1331-d513-4f16-b70f-df6180b5a3c0 имеет одну активную роль MEMBER, устройство 149d1145-4ee4-4865-8a83-876cd4f87d7d и сессия по одной записи. Ошибка завершения instrumentation остаётся ошибкой оснастки и не объявляется успешным тестом.

DEF-017 закрыт после 4/4 UI тестов APK6 и реальных отрицательных и положительного переходов. NAV-03 пока частичен: отдельный ответ API404 не снят.

ENV-07 закрыт на APK6: `ENV07-proof.json` и пять исходных XML подтверждают пять нижних разделов и ровно один правильный выбранный раздел после каждого перехода.

Следующая независимая группа PRT-03: установленный TypeBox проверил фактические схемы всех 13 инструментов, 72 варианта успешно, `prt03-schema-boundaries.json`. Проверены неизвестные поля и поля личности, UUID, даты, версии, длины, пустое UPDATE и повторяющиеся корни. Свободная строка фильтра LIST_TASKS соответствует исходному `ListTasksIntent`; ошибочное ожидание перечисления не объявлено дефектом. Полный OpenClaw dispatcher в этом запуске не вызывался, поэтому отказ до HTTP остаётся непроверенным и PRT-03 остаётся частичным.

## Отзыв семьи сессий и повтор выхода

AUTH-15 закрыт: `auth-family-boundaries-oracle.txt` и `auth15-old-access-probe.json` подтверждают повтор proof 409, отзыв всей семьи поколений 1–6 и отказ старого и последнего access 401. Ожидание HTTP исправлено по действующему Gateway `internal/httpapi/server.go:402–403`: ErrRefreshReplay отображается в 409 CONFLICT; `identity/postgres.go:169–179` отзывает семью. Это уточнение исходного ошибочного ожидания 401 именно для повторного refresh, а не изменение защиты.

AUTH-17 закрыт отдельным изолированным API сценарием `auth17-logout-repeat-oracle.txt`: активная сессия подтверждена защищённым запросом 200, первый logout204, повтор того же старого bearer401; семья неактивна, устройство и членство неизменны, дополнительных эффектов нет. Это другая официальная тестовая семья, не повтор мобильного AUTH-16.

Локальная исполняемая часть REM-05 проверена под assistant_runtime на PG18: `rem05-list-boundaries.sql/.log`, чужие actor/chat не получают задач, недопустимые/пустые/null состояния дают 22023, каноническая функция ограничивает лимит диапазоном 1–50. Контрольный хеш задач не изменён, транзакция отменена. Отображение SQL-ошибки через полный инструмент и HTTP этим запуском не проверено.

TASK-08 закрыт по `task08-12-prod-oracle.txt`: отдельное изменение title сохраняет все прочие поля, единственные версия/аудит/outbox. Связанные 09–12/15 остаются частичными там, где исходный критерий требует UI или календарь; API-проверка не подменяет их. TASK-16 требует все исходящие терминальные цели, backend пока подтвердил два представителя и получил запрос оставшихся.

TASK-16 закрыт дополненным `task15-16-prod-oracle.txt` (SHA335E81…BF67C): все восемь исходящих переходов из DONE/CANCELLED отклонены 409 TASK_TRANSITION_INVALID, неизвестное состояние400. Обе задачи сохранили v2 и completedAt-инвариант; новых аудитов/outbox/idempotency нет. Уже исполненные два варианта не повторялись.

## Пауза по просьбе пользователя — 10 сентября 2026

Проверки, сборки и сеть остановлены. Каноническая точка продолжения: [PROJECT_HANDOFF.md]([[PROJECT_HANDOFF]]). Срез94 PASS /2 FAIL /202 NOT_RUN, всего298. APK7 8/8 и реальные Agent/Inbox empty положительные; реальные решения font2 ещё не сведены, ENV08 пока FAIL. TASK13 UI не начат, local access-loss regression красный сохранён. TaskCore DEF016 production исправлен, plugin deployment после трёх откатов BLOCKED; попытка4 запрещена. Документация сохранена локально, push/deploy не выполнялись.

## Возобновление и независимые проверки

ENV-08 / DEF-018 закрыты на APK7. Реальные ENV08-font2-inbox-initial.xml, ENV08-font2-inbox-decisions.png/xml подтверждают PIN при масштабе 2 и полностью доступные кнопки подтверждения/отклонения. ENV08-font2-detail-top/mid/actions/result.xml и actions.png подтверждают доступ через прокрутку ко всем полям, видимости, сохранению, статусам, результату и документам. Вместе с batch7-agent-keyboard и batch7-inbox-scrolled это покрывает исходные auth/detail/inbox/agent. Проверены изображения, а не только координаты. Масштаб возвращён к 1.0; решений и изменений задачи не выполняли.

TASK-13: независимый task-access-loss-family-final.log — 4/4, ошибок и пропусков 0, настоящий PG18 и Flyway V10. Получатель получает TASK DELETE v2 без payload, cursor585→588; повтор страницы идентичен, следующая пуста. Новый участник после потери доступа ID не получает. PRIVATE→TEAM восстанавливает UPSERT без старого TASK DELETE. OWNER, ACCEPTED assignment и TASK_READ grant сохраняют доступ. Два исходных отказа расширенного теста были ошибкой ожидания: штатное CALENDAR_ITEM DELETE при отсутствии срока не означает удаление TASK; oracle ограничен правильным entityType. Production и мобильная очистка пока не проверены.

REM-17: реальный jsCode обеих materializer workflow проверен без Telegram, rem17-digest-boundaries.cjs/json. Из восьми вариантов шесть PASS, два FAIL: 25 отложенных напоминаний с длинными заголовками приводят к обрезанию текста до 4000 и исчезновению строки «Ещё 5 напоминаний», хотя все 25 остаются в members. Группа лимитов/сводок собирается перед единым исправлением. Полный REM-17 пока FAIL.

Срез после этого обновления: 298 = 95 PASS + 2 FAIL + 201 NOT_RUN. TASK-13 и REM-17 — FAIL. DEF-014 deployment BLOCKED по-прежнему не разрешает четвёртую попытку.

### Завершённая граница семейства REM-17

`rem17-digest-boundaries.cjs/json`: выполнен реальный код всех четырёх workflow из текущих templates, 16 вариантов, 12 PASS и 4 FAIL. Ограничение 4000 символов и максимум 5 сообщений взяты из существующего кода. При 25 заголовках по 600 символов обрезка удаляет строку количества объединённых элементов во всех четырёх вариантах; идентификаторы членов сохраняются ровно по одному. Короткие сводки и обычное переполнение проходят. Нужен общий бюджет строк с сохранением обязательной сводки, без увеличения потока сообщений.

`rem17-rate-concurrency.json`, `rem17-rate-mixed-concurrency.json`, `rem17-rate-storm-concurrency.json`: настоящий PG18, роль assistant_runtime, две перекрывающиеся транзакции. Подтверждены 6 авторизаций при лимите5 для обычных доставок, 6 при общем лимите5 для delivery+confirmation и 21 при storm20. Шестая/21-я авторизация не ожидала фиксации первой транзакции. Отправок провайдеру 0. Это дефект общей сериализации счётчика получателя; функция считает и обычные, и confirmation timestamps, но не блокирует общую policy row. Локальные fixtures имеют точный ledger; реальные напоминания не затрагивались.

Группа передана backend одним отдельным пакетом после TASK13: DEF020 — бюджет сводки; DEF021 — общий rate/storm для двух authorize функций. Требуются проверка обоих направлений, независимость разных получателей, сохранение quiet00–06, cadence30m, TTL и retry_after. До проверки кандидата reminder deployment не выполняется; ограничение трёх попыток plugin остаётся без изменений. Числовой срез не изменился: 95 PASS / 2 FAIL / 201 NOT_RUN.

PRT-07: новые синтетические проверки санитаризации выявили DEF022; кандидат и независимая приёмка описаны в журнале дефектов. Текущий срез 298 = 95 PASS + 3 FAIL + 200 NOT_RUN; среди последних47 частичных,153 без привязанного результата. Четыре прежних PRT07 проверки не повторены.

DEF022 независимо принят backend после добавления канонического поля errorCode; commit ca601c3 содержит только два согласованных plugin-файла. def022-local-tests-reviewed.log 15/15, без пропусков. Pack и deploy не выполнялись, PRT07 целиком не закрыт.

REM17 независимая проверка уточнила вход до реальной границы заголовка300: rem17-digest-legal-baseline.json подтверждает исходный дефект с250символами. У первого исправления новая граница: допустимый заголовок с250 внутренними переводами строки превышает итоговые4000 из-за split по пользовательскому тексту; rem17-digest-legal-review.json — 4 отказа из20. Требуется структурированный бюджет элементов, а не разбор уже склеенного текста. Это тот же DEF020 до выпуска, не отдельная новая функция.

Исходные mixed/storm отчёты гонок были перезаписаны исполнителем повторной проверки. Фактически наблюдённые красные значения сохранены в rem17-rate-red-observation-summary.json с явной ссылкой на происхождение из tool outputs; файл не выдаётся за исходный полный журнал. Первый исходный rem17-rate-concurrency.json сохранён. Дальнейшие результаты должны иметь отдельные fixed/временные суффиксы.

## Порядок фиксации транзакций и курсор синхронизации

Один согласованный локальный Maven-прогон завершён: `task-access-commit-order.log`, 2 теста, 1 PASS / 1 FAIL, ошибок и пропусков 0. Принятие назначения после потери TEAM-доступа правильно возвращает TASK UPSERT с той же версией2 и более новым cursor. Это подтверждает необходимость курсорной, а не строго версионной защиты в APK8.

Второй тест подтвердил DEF023. Первая настоящая транзакция меняет TEAM→PRIVATE, но удерживает COMMIT после вызова сервиса. Другая TEAM-задача фиксируется, получатель синхронизируется и продвигает cursor894→899. После фиксации первой транзакции её задача недоступна прямым GET, но следующая sync899→899 возвращает changes[]. Событие DELETE было получено из sequence до COMMIT и оказалось позади курсора. Тест `delayedVisibilityCommitMustNotFallBehindAnotherTaskCursor` сохранён в ReadToolContractIntegrationTest; его нельзя исключать ради зелёного набора. Данные только локальной qa_read_tools, задача c221302a-ad87-405c-8b46-6afe0c9aff36. Нужна защита порядка публикации общего feed; повторно запускать production-операции для демонстрации не требуется.

APK8 независимо одобрен на уровне кода и локальных тестов: TaskAccessLoss6/6 + DocumentCacheAuthorization16/16, без пропусков. Удаление связано с точной задачей, поздние task/document/calendar/focus не восстанавливают удалённое, marker переживает перезапуск и допускает то же taskversion при более новом cursor. Реальная эмуляторная приёмка отдельно; DEF023 не позволяет объявить TASK13 полностью закрытым только по последовательному сценарию.

### Продолжение 11 сентября: независимая приёмка двух пакетов

DEF023: проверен ранний транзакционный advisory lock рабочего пространства. Он берётся до строк задач, назначений, согласований и записей идемпотентности во всех существующих путях, создающих change_log. Обычная синхронизация без изменений использует отдельный режим IdempotencyService без этой блокировки. Переходы task run не создают change_log и не получают новый lock, поэтому существующий порядок блокировки dispatcher не инвертирован. Схема БД не изменяется.

`task-access-commit-order-fixed.log`: 3/3 PASS, ошибок и пропусков 0. При удерживаемой транзакции следующий writer того же пространства ждёт; другая область и чтение через фактический IdempotencyService завершаются в пределах 3 секунд. После COMMIT cursor912→912→919 включает DELETE915 перед более поздней задачей. После ROLLBACK cursor932→932→939 не содержит ложного DELETE, TEAM-доступ сохранён. Возврат доступа через назначение с той же версией задачи также проходит. Добавлен один тест двух одновременных пакетов изменений с обратным порядком задач; он передан backend для общего затронутого регресса и пока отдельно не засчитан. Production DEF023 и совместная приёмка APK8 ещё ожидаются.

REM17: независимая приёмка локального пакета DEF020/021 завершена. `rem17-digest-legal-fixed.json` — 24/24 PASS на четырёх фактических workflow: обычное переполнение, короткая/длинная сводка, допустимые многострочные заголовки, смешанная очередь и backlog100 при25 включённых элементах. Сохраняются максимум5 сообщений, предел4000, однократное включение каждого member и итоговый счётчик. Последняя граница теперь сообщает5 дополнительно объединённых и75 оставшихся в очереди, не выдавая ожидающие сообщения за отправленные. Первые4 отказа нового счётчика были ошибкой кодировки русского текста в тесте; после замены на Unicode escape тот же продукт прошёл проверку.

`rem17-rate-delivery-fixed.json`, `rem17-rate-mixed-fixed.json`, `rem17-rate-storm-fixed.json` сохранены из проверенного запуска23:43: разрешены ровно5/5/20, конкурирующая авторизация ждёт COMMIT и отклоняется, providerCalls=0. Ночью эти проверки повторно не запускались, чтобы тихие часы не подменили проверяемую причину отказа. Проверены совпадающие тела двух SQL-функций в005 и014; изменение блокирует только строку политики соответствующего получателя. Периодичность, тихие часы, TTL, доставка и повторы не менялись. Развёртывание этого пакета ещё не подтверждено; REM17 остаётся FAIL до требуемой проверки production.

Текущий полный срез не изменён:298 =95 PASS +3 FAIL +200 NOT_RUN; среди NOT_RUN47 частично проверены и153 без привязанного результата. TASK13 и REM17 не закрываются по одной локальной сборке; PRT07 остаётся ограничен недоступным развёртыванием plugin.

APK8: независимое чтение сохранённых Room-снимков `TASK13-apk8-member-metadata-settled.db` и `TASK13-apk8-member-private5.db` подтверждает последовательную потерю доступа. До: задача a8a50115-bed8-40c3-9129-2e69ffe28834 TEAMv4, метаданные документа6c8d601a-0bdf-41f0-a842-ed1243aa7ff7, cursor325. После OWNER PRIVATEv5: задача и её метаданные отсутствуют, две другие задачи сохранены, outbox0, общий курсор и долговечная отметка удаления330. `TASK13-apk8-member-private5-settled.xml` фиксирует возврат с открытой детали в список. Это частичная приёмка DEF019; тело документа получателю TEAM изначально не разрешено, поэтому удаление ранее доступного тела на production не заявляется. ASSIGNED/возврат через принятое назначение и выпуск DEF023 остаются отдельными условиями полного TASK13.

### Независимый сетевой остаток

AGT03: перед отрицательной API-проверкой проверена существующая отдельная backend QA-сессия. GET401; после согласования единственного владельца ровно один штатный proof refresh завершился challenge401, до ротации и изменения файла. Backend подтвердил причину: прежний AUTH15 намеренно отозвал всю эту семью. Повтор, новый redeem и использование AVD-сессии не выполнялись. `agt03-negative-api.py` подготовлен, но поведение ещё не проверено. Для оставшихся API-проверок требуется одна новая официальная изолированная сессия на весь пакет.

Вместо ожидания выполнен независимый NET01: `net01-all-routes.py/json` использует26 защищённых методов/путей существующего контракта, исключая три публичных маршрута авторизации. Все запросы отправлены на production без Bearer с query-параметром, без автоматического перехода по redirect, параллелизм4. Результат26/26: HTTP401, `AUTH_REQUIRED`, непустой traceId; ни404, ни успешной операции. Секреты и эмулятор не использовались. NET01 полностью PASS. Новый срез298 =96 PASS +3 FAIL +199 NOT_RUN, из последних46 частично проверены,153 без привязанного существенного результата.

TASK13: последовательный возврат доступа независимо подтверждён `task13-postrelease-regain-oracle.txt` и чтением `TASK13-regain-document.db`. Задача ASSIGNEDv6, назначение8cbb60dc-8fbb-4505-8284-53fe0b7d790a ACCEPTEDv2, ровно одно назначение, сервер разрешает task и document. Принятие вернуло TASK UPSERT с той же версией6 и cursor346, превышающим сохранённую отметку удаления330; общий курсор348. `TASK13-member-regained-detail.xml` показывает доступную деталь. Свежие HTTP-запросы после принятия восстановили метаданные и тело документа, по одной строке в Room; `TASK13-member-doc-regain-list.xml` и `TASK13-member-doc-regain-body.xml`. Это не доказательство отзыва ранее разрешённого тела: такая операция не реализована. Полное закрытие TASK13 удерживается выпуском и проверкой DEF023.

NET02: `net02-raw-paths.py/json`,12/12 production-проверок без Bearer. Недопустимые PUT/DELETE, вложенные admin, двойные разделители, буквальные точки, кодированные `%2e`/`%2f` с разным регистром возвращают404 без Location. Использован http.client, не нормализующий заданный путь и не переходящий по redirect. Прямой счётчик обращений upstream не измерен, ветка с действующим Bearer ожидает выделенной QA-сессии; поэтому полный NET02 остаётся NOT_RUN с частичным доказательством. Числовой срез не меняется.

Официальная изолированная API-сессия создана один раз для всего пакета: workspace8012ec66-e376-400d-9c05-6e871ba2a64e, principalc5a0451a-0381-4f1f-969d-25cba71cca6f, devicefc360776-8d81-4a2d-addf-2d6c841c868c, invitation21c876e3-3230-4c6b-b6ae-e7d329454044. Каталог `TOOLS-5a5af623/api-batch-20260911`, QA — единственный владелец ротации. Существующий helper успешно сохранил новую сессию, затем завершился отказом доступа к ledger; повтор redeem не выполнялся. Проверка сохранённого доступа дала200, backend точечно исправил ACL ledger и записал идентификаторы. Секреты в отчёты не выводились.

AGT03 полностью PASS. `agt03-negative-api.json`: авторизованный production-вход отклонил malformed/numeric task UUID и blank/20001 text с400. Для точной conversationcf0573b7-c556-4526-ad73-9cc9420fd917 до и после: runs0,dispatches0,outbox0; для четырёх operationId записей идемпотентности0. Сопоставлены прежние реальные UI-артефакты `AGT03-before/after.db/json`, `AGT03-20001-rejected.xml`: очередь19 и сообщения2 неизменны. UI-прогоны не повторялись. Новый срез298 =97 PASS +3 FAIL +198 NOT_RUN; среди последних45 частичных и153 без привязанного существенного результата.

NET02: дополнительно `net02-authenticated.json`12/12 PASS на той же группе сырого пути, теперь с действующей выделенной QA-сессией и предварительным GET200. Прежняя группа без Bearer не повторялась. Прямой счётчик upstream остаётся неизмеренным; полный статус пока не повышен.

TASK09–12 полностью PASS после независимого сопоставления XML, Room и `task09-fields-prod-oracle.txt`. Точная задача483dad28-0a38-4bc4-a8eb-e286916b5b48: description с ё,€,emoji и переводом строки сохранён побайтно вv2 и виден после повторного открытия; очищен вv3, прежний текст скрыт и показано пустое состояние. Допустимый срок с исходным смещением+14 вv4 сохранён как10:30Z и показан13:30МСК в календаре; очисткаv5 удаляет его из календаря. Невалидный текст срока даёт ошибку RFC3339, очередь36→36, версия остаётся5. Сервер подтверждает CREATE1/UPDATE4, неизменные PLANNED/PRIVATE. Доказательства: `TASK09-description-reopened.xml`, `TASK10-clear-section-hidden.xml`, `TASK11-calendar-with-due.xml`, `TASK11-calendar-after-clear.xml`, `TASK12-invalid-due-rejected.xml` и соответствующие db. Новая сборка не требовалась. Текущий срез298 =101 PASS +3 FAIL +194 NOT_RUN, среди последних41 частичный и153 без существенного результата.

SYNC02 и SYNC03 полностью PASS. Независимое чтение шести сохранённых Room-снимков подтверждает PENDING с тем же operationId и payload после принудительного завершения приложения и повторного входа без сети; после сети обе операции APPLIED. `sync02-03-prod-oracle.txt`: statuse351414c-a3aa-4ccb-87ff-c20e1f24c56e применён ровно один раз вv6, resultba0d81ba-cbf5-4a5c-b174-262529219561 — вv7. UTF8 результата с ё/emoji побайтно совпадает с сохранённым офлайн-payload; audit CREATE1/UPDATE6 соответствует четырём прежним изменениям полей и двум новым. Артефакты `SYNC02-restarted-offline-*`, `SYNC03-restored-result-draft.xml`, `SYNC03-restart-offline.db`, снимки after-network.

NET04/06: первая оснастка ошибочно отправляла PATCH с application/json, получив415 и один таймаут. Данные точной задачи после него осталисьv1/audit1/changes2/outbox1; такие ответы не считаются дефектами продукта. Исправлено на предусмотренный контрактом application/merge-patch+json. Новая сессия к продолжению истекла через15 минут; один штатный proof refresh новой семьи дал201/200/200. Прежняя отозванная семья не использовалась. Не повторены два уже пройденных отсутствующих/невалидных ключа.

`net04-06-fixed.json`:9/10 PASS. NET06 полностью подтверждён: отсутствующий/некавыченный/weak/нулевой/нечисловой If-Match отклонены400 без эффектов, допустимая версия применяется200, устаревшая409 без эффектов. NET04 FAIL: два различных Idempotency-Key в одном запросе приняты200 с изменениемv1→v2 вместо400, DEF024. Следующие варианты неоднозначных заголовков собираются на этой же QA-фикстуре в единый пакет, без изменений gateway по одному случаю. Текущий срез298 =104 PASS +4 FAIL +190 NOT_RUN; из последних38 частичных и152 без существенного результата.

DEF024: семейство собрано полностью в `net-header-family.json` —12 дополнительных случаев,6 PASS/6 FAIL. Ошибочно приняты200 с эффектами одинаковые Idempotency-Key, разные значения при разном регистре имени, valid→invalid; аналогично одинаковые Authorization, valid→invalid и вариант регистра имени. В обратном порядке invalid→valid запросы отклонены; ключ со списком через запятую и все три дублированных If-Match отклонены400. Авторизационные значения в отчёте скрыты. Все эффекты принадлежат одной явно созданной QA-задаче, текущая версия9. Один общий пакет передан владельцу gateway; отдельные исправления по каждому случаю не выпускались.

REM17: независимо принят frozen helperC30F1461AB4E20A2CA9E99DB854778CF7191A3D79C7D8D62BD233D02C4367A11. Проверяет пять SHA, реальные тела/владельца/SECURITY DEFINER/ACL функций, четыре workflow, состояния активности, неизменность домена; отрицательная проверка выполняется под assistant_runtime с ROLLBACK. При таймауте docker exec фиксируется UNKNOWN_OUTCOME без конкурирующего автоматического восстановления.

`rem17-deploy.log` и `rem17-prod-oracle.json` подтверждают один успешный выпускa15ea8d: domainPreserved=true, активностьt/t/f/f, N8N running/healthy/restart0, обе функции ownerassistant_owner и EXECUTE только owner/runtime. Обе отрицательные проверки вернули LEASE_INVALID, providerCalls0, ROLLBACK. Локальные положительные гонки5/5/20 и реальные исходники совпадают с выпущенными, но отрицательный production-тест не подменяет положительную production-проверку тихих часов/сводки/лимита. Полный REM17 пока не закрыт.

DEF023: `deploy-task-feed-order.sh` независимо принят по точной разнице с ранее проверенным выпуском, новых SQL-миграций нет. `task-feed-order-deploy.log` подтверждает ee5e19b, imagebd9554daa32b976e2c42a97990df7574a4829252913bb38b2c9919634770795a, healthy/restart0/readinessUP. Независимая гонка COMMIT/ROLLBACK была локальной; последовательный TASK13 эмуляторный путь — production. Эти разные доказательства не называются одной production-гонкой.

## 11 сентября: действующая граница синхронизации и сохранность очереди

TASK13 полностью PASS. `task-feed-live-fence.json`: на точном QA-пространстве перед началом нет активных операций; краткая блокировка задержала штатный PATCH, но read-only sync вернулся HTTP200 за 0,174 с без изменения курсора373. Блокировка освобождена в finally, задача c9202ea5… получила ровно v10 и по одному audit/outbox/feed. Это проверка выпущенной границы; локальная гонка порядка COMMIT и реальные мобильные потеря/возврат доступа описаны отдельно выше.

NET05 PASS: `net05-replay-live-fence.json` повторяет только уже завершённую операцию: тот же ключ и тело дают тот же HTTP200/v10, другое тело —409 IDEMPOTENCY_KEY_REUSE. Счётчики задачи, audit, outbox и feed не изменились; запись идемпотентности одна.

SYNC06 PASS по уже существующему доказательству, без повторного изменения Inbox: `mobile-20260910/batch3-INB-offline-proof.json` сохраняет PENDING e8866014… и оптимистичный READ после перезапуска без сети; после восстановления сети APPLIED/READ v2. `batch3-generic-read-oracle.txt`: одна запись audit, HTTP200. Новый элемент5a77… не трогали.

## 11 сентября: варианты APK и полная цепочка офлайн-операций

ENV10 PASS: одна сборка release+qa и обе проверки LintVital успешны. `mobile-20260910/ENV10-variant-proof.json` и manifest-файлы подтверждают backup=false, отсутствие экспортированного QA-компонента, debuggable=false для release/qa. Независимо пересчитаны SHA-256 unsigned APK release e7c15e3b…4438a5 и qa c2be99ba…6bab97; в самих DEX диагностические маркеры присутствуют только в qa, отсутствуют в release/debug. Подпись и установка не выполнялись.

SYNC05 PASS: `SYNC05-restart-offline.db` сохраняет операцию fee678b9… с expectedVersion10 и точный порядок07,A,08 после остановки процесса. `SYNC05-after-network.db` и `sync05-focus-prod-oracle.txt` подтверждают APPLIED v11 и единственную запись audit.

SYNC01 и SYNC07 PASS по общей реальной цепочке: `SYNC07-chain-after-restart.db` содержит четыре PENDING-операции CREATE/status/result/focus для локального ac44b49e…. После сети `SYNC07-after-network.db` содержит четыре APPLIED, зависимости переписаны на канонический6bf22116…, задача IN_PROGRESS v3, Фокус v12, локальной копии нет. `sync07-chain-prod-oracle-fixed.txt` подтверждает CREATE1/UPDATE2, версии1→2→3, точный результат Unicode и канонический ID в Фокусе. UI локальной карточки, итоговой задачи и Фокуса сохранён. Отдельные граничные заголовки TASK02 не выдаются за проверенные до синхронизации.

Срез после этих четырёх закрытий: 298 =111 PASS +3 FAIL +184 NOT_RUN;37 частичных,147 без привязанного существенного результата. Новая находка ENV04 пока собирается отдельным семейством; её регистрация последует после проверки доказательств.

## 11 сентября: два новых подтверждённых дефекта, без отдельных выпусков

Первый реальный Go-прогон в существующем Docker buildstage исполнил ранее заблокированный EX008: `gateway-def024-build-test.log` подтверждает синтетический302 с внешним Location. DEF025 включён в тот же пакет Gateway, что и DEF024. Ошибка тестового пути link-nonce вместо link-nonces — отдельно исправляемая ошибка теста, не дефект продукта.

Мобильный ENV04: независимая сверка подтверждает точный ввод44 символов и пустой EditText после rotation1/PIN в том же разделе. Зарегистрирован DEF026; остальные экраны проверяются до общего исправления. Причина исчезновения черновика и все варианты обрезки ещё не установлены.

Текущий срез: 298 =111 PASS +5 FAIL +182 NOT_RUN;35 частичных и147 без привязанного существенного результата. Изменение счётчиков отражает находки в ранее непроверенных частях, а не отмену прежних полных PASS.

Следующий общий Gateway gate подтвердил DEF027/EX007: прежняя неисполненная проверка выявила исходные X-Forwarded/X-Action в upstream. Кандидат удаляет фактические ключи этих семейств, Forwarded и X-Poruchik независимо от регистра перед восстановлением доверенной идентичности. Независимый тест расширен теми же заголовками в двух регистрах, включая Timestamp/Body-SHA256. Всё остаётся в одной серверной партии DEF024/025/027; нового выпуска не было. Текущий срез298 =111 PASS +6 FAIL +181 NOT_RUN;34 частичных,147 без существенного результата.

## 11 сентября: единая приёмка локального пакета Gateway

`gateway-header-redirect-sanitize-build.log`: фактический Docker `go test -v ./...` успешен. Один TestPostgresIdentityLifecycleAndReplay пропущен из-за отсутствия тестового подключения PostgreSQL; это не общий результат «без пропусков».

NET02 полностью PASS: ранее сохранены24 production-запроса (12 путей с Bearer и без); TestQAUnsupportedRawPathsNeverReachUpstream теперь подтверждает те же24 варианта HTTP404/noLocation/upstream0 и валидный контроль upstream1. EX006 полностью PASS по TestQAProxyFidelityAndReuse: точные body/query/correlation, удаление Bearer/Server, повторное соединение. EX007 и EX008 — локальные кейсы матрицы — PASS после фактической проверки удаления двух регистров spoof-заголовков и нейтрального502 без Location/canary. Производственный выпуск исправлений этих дефектов пока не заявляется.

DEF024 локальные10 вариантов неоднозначных заголовков и2 локальных link-маршрута PASS. NET04 остаётся FAIL до production-проверки после выпуска. EX005 частично PASS по limit/+1 и последующему healthy запросу; неверный Content-Length и медленный body остаются непроверенными.

Срез298 =115 PASS +4 FAIL +179 NOT_RUN;33 частичных,146 без привязанного существенного результата. Общий Gateway пакет передан для одного выпуска после независимого review сценария развёртывания.

## 11 сентября: production-приёмка общего Gateway пакета

`gateway-header-batch-deploy.log`: проверенный backup, immutable image1fabb863…76ad46, gateway healthy/restart0, обе сети, readiness и AUTH_REQUIRED401; TaskCore не пересоздавался. Один штатный refresh текущей QA-семьи выполнен после завершения выпуска (201/200/200), повторного redeem не было.

`net-header-family-retest.json`:13/13 production-вариантов PASS на прежней задаче c9202ea5…. Одинаковые/разные/разнорегистровые/невалидные дубли ключа и Authorization отклонены; прежняя защита If-Match сохранена. Для каждой попытки версия10, title, audit, outbox и feed до/после идентичны. `net-header-valid-control.json`: один контроль положительного одиночного заголовка через replay уже завершённого PATCH вернул200/v10 без новых эффектов. Это проверка изменённого валидатора, не новая доменная операция.

NET04/DEF024 PASS. DEF025/027 приняты по синтетическим локальным оракулам и выпуску того же проверенного immutable image; внешнюю перенаправляющую цель и подмену служебных заголовков на production не инсценировали. Все три исправления вышли одной партией. Срез298 =116 PASS +3 FAIL +179 NOT_RUN;33 частичных,146 без привязанного существенного результата. Открытые FAIL: ENV04, REM17, PRT07.

## 11 сентября: завершены оставшиеся границы Gateway

Gateway пакет сохранён commit360c21a. NET03 PASS: `net03-trusted-context.json` использует только GET и текущую QA-сессию. При подставленных X-Poruchik principal/workspace/role/scopes/device в каноническом и нижнем регистре своя PRIVATE c9202ea5… остаётся200, чужая точная QA PRIVATE483dad28… остаётся404:4/4. Владелец и пространство чужой задачи подтверждены read-only SQL. Вместе с локальным TestProxyDerivesIdentityAndRejectsSpoofing это подтверждает серверное происхождение identity; никаких задач, прав или сессий другой учётной записи не изменяли.

EX005 PASS: `body_transport_test.go`, независимо принят по исходнику и `gateway-body-boundaries.log`. Четыре реальных TCP-варианта: нечисловой Content-Length, конфликтующие значения, преждевременный EOF, медленное тело. Использованы действующие ReadTimeout30с/WriteTimeout35с; медленный запрос завершился за30,03с, ошибка ограничена размером4096, upstream не принял тело, после каждого варианта допустимый запрос204 успешен. Прежние ровно-limit/+1 остаются действительными. Продукт не менялся, новый выпуск не нужен. Один unrelated identity PostgreSQL test по-прежнему SKIP и не скрывается.

Срез298 =118 PASS +3 FAIL +177 NOT_RUN;31 частичный,146 без привязанного существенного результата. DEF026 мобильный исходник прошёл независимый review и передан для одной APK9 с проверкой реального поворота/PIN; сам дефект ещё не закрыт.

NET11 PASS: `net11-external-boundary.json` сохраняет4 публичных обращения `/internal/*` с действующим mobileBearer:404, без Location. Проверки HTTP внешних18791/8080 завершаются RemoteDisconnected; Docker публикует Gateway только127.0.0.1:18791, у TaskCore bindings отсутствуют. Серверный `ss -ltn` показывает только127.0.0.1:18791 и отсутствие listener8080. TCP connect сам по себе завершался успешно, поэтому TCP refused не утверждается и причина сетевого accept/close не выдумывается. Критерий недоступности внутреннего HTTP подтверждён вместе с фактическим bind.

Для NET09/10 после нулевых read-only счётчиков backend создана одна официальная link-nonce текущего изолированного QA principal, HTTP201/operation19acfd3c…. Секретный ответ сохранён в файле с ACL только текущего владельца до записи содержимого, metadata и ledger обновлены. Backend единственный потребитель для synthetic user/chat860911000000000001; реальных отправок и пользовательских связей не будет. Повторное создание не требуется. Срез119 PASS/3 FAIL/176 NOT_RUN,30 частичных,146 без существенного результата.

NET09/10: исходная link-nonce истекла в22:39UTC до независимого review после прерывания; backend подтвердил отсутствие consume/HMAC-вызовов. Старый код не используется повторно. Перед единственной заменой требуется полностью готовый сценарий с проверкой exactowner/workspace и remainingTTL>=120с до одноразового marker/операций. Это ограничение оснастки, не дефект продукта. Основная работа не приостановлена: APK9 восстановил44символа после настоящего rotation/PIN, но реальная IME перекрыла composer — тот же DEF026, очередная дельта уже прошла source review; закрытие ждёт actual screenshot.

## 11 сентября: окончательная приёмка ENV04/DEF026

Final APK9 `batch9-ime-debug.apk`, SHA273856396ED6C14706EA69E5E8B23987ECD418349E7B3E2F7DEAD1A03D26D93A установлен с сохранением данных только на emulator5562;5560 остаётся на APK8. Новые6 и прежние8 Compose UI-проверок прошли14/14; они не моделировали настоящую IME, поэтому отдельно проверена реальная клавиатура после уточняющей дельты.

Независимо просмотрены `APK9-ime-landscape.png` и `APK9-ime-portrait.png`: обе кнопки полностью над клавиатурой. `APK9-ime-final-proof.json`: точные44 символа сохранены во всех четырёх XML, после закрытия IME панели возвращаются и переход Календарь работает; Room3/0/0, schema6, отправки не было. Agent и Focus сохраняют черновик/выбор после настоящего rotation→PIN. Пустой Focus прокручивается до действия.

Остальные разделы имеют действительные доказательства APK8, их код этой партией не менялся: `ENV04-task-before-rotation.xml`/`ENV04-task-landscape-editor.xml` сохраняют несохранённый заголовок и выбранную задачу; `ENV04-calendar-before.xml`/`ENV04-calendar-landscape.xml` — раздел и ту же карточку; `ENV04-inbox-landscape.xml`/`ENV04-inbox-portrait-return.xml` — раздел, фильтры и карточку после возврата. Нестандартный фильтр Inbox или точный scroll-offset отдельно не заявляются.

ENV04 полностью PASS, DEF026 RETEST_PASS. Срез298 =120 PASS +2 FAIL +176 NOT_RUN;30 частичных,146 без привязанного существенного результата. Открыты REM17 и PRT07. Для NET09/10 final TTL-сценарий независимо принят backend; один свежий nonce201/operation28d5ea94… передан единственному потребителю сразу после создания, без повторного redeem.

## 11 сентября: production HMAC/nonce — NET09 и NET10 PASS

После независимой приёмки final TTL/ownership preflight выполнен ровно один `net09-10-internal.py`; `net09-10-production.jsonl` содержит9 отрицательных и3 последовательных положительных/повторных сценария. Отсутствующие/неверные key/signature/timestamp (включая прошлое/будущее)/bodyhash/nonce дают401, превышенный body400; service_nonces/link/binding/audit/tasks не меняются.

Валидный вызов200 создал только синтетическую связь изолированного principal c5a0451a… с user/chat860911000000000001 и consumed link; serviceNonces1. Точный nonce replay401 SERVICE_REPLAY_REJECTED без эффектов. Новый nonce с тем же одноразовым link возвращает409 CONFLICT: serviceNonces2, binding по-прежнему один. Это фактический одноразовый BindTelegram-контракт, а не выдуманная повторная привязка. Audit62/tasks8 неизменны, providerCalls0. Операция77ef5a84… и точный synthetic tuple записаны в API ledger; реальные пользовательские связи не затрагивались. Прежняя истёкшая nonce не использовалась.

Срез298 =122 PASS +2 FAIL +174 NOT_RUN;28 частичных,146 без привязанного существенного результата. NET09/10 не требуют нового выпуска продукта.

## 11 сентября: нормализация SYNC12 и окончание TASK03

SYNC12 первоначально ошибочно требовал4xx для чужого/отмотанного числового курсора. Независимый backend-арбитраж сверил `contracts/mobile.openapi.yaml` (synchronize, cursor, accessible changes), `MobileController.sync` (Long.parseLong) и `TaskCoreService.sync` (sequence lower-bound плюс workspace/current ACL). Привязанный к principal токен и серверное хранение клиентского курсора не реализованы и не обещаны контрактом. Приёмка нормализована с сохранением происхождения; новая функция не добавлялась.

`sync12-cursor-boundaries.json` actual5/5: malformed/overflow400 без событий;0/число330 другого пространства/rewind0 дают200 и32/20/32 события. Каждое TASK/parent/recipient сопоставлено с авторизованным набором из DB; хеш задач, audit и feed не менялись. Новые request idempotency записи ожидаемы и не считаются доменными изменениями. SYNC12 PASS.

TASK03 PASS: `TASK03-ui-boundaries-proof.json` подтверждает disabled родитель кнопки Создать при пустой строке/трёх пробелах и отсутствие новых операций. При вводе301 символа UI штатно ограничил значение300, поэтому кнопка стала доступна для уже допустимой длины; отправка300 не повторялась. Исходный inputprobe FAIL означает ожидаемое усечение, а не успешный exact301. После Cancel Room18/43/6 полностью совпал с baseline. Вместе с прежними API blank/301 reject с нулевыми эффектами составной кейс закрыт.

Срез298 =124 PASS +2 FAIL +172 NOT_RUN;26 частичных,146 без привязанного существенного результата.

## 11 сентября: TGL01/03 и уборка синтетической связи

TGL01 PASS по существующей официальной create nonce201, доверенной HMAC consume200 из NET09/10 и `tgl01-03-binding-lifecycle.json`: GET link-status200 возвращает linked=true и точный synthetic telegramUserId860911000000000001. До любых изменений read-only SQL подтвердил единственную связь нашего principal c5a…/workspace8012… с теми же user/chat. Реального Telegram-обмена не было.

TGL03 PASS: официальный DELETE link204, повтор с тем же Idempotency-Key204; GET status200 linked=false без user/linkedAt. Точная строка связи помечена revoked, после повтора неизменна включая revokedAt; хеш всех чужих связей, задач нашего пространства и число audit не изменились. Ledger помечает эту synthetic-связь REVOKED с точным operation/evidence. Это очистка только созданной QA-связи, а не пользователя проекта.

Срез298 =126 PASS +2 FAIL +170 NOT_RUN;26 частичных,144 без привязанного существенного результата. FOC02 готовится следующей самостоятельной API-группой; до передачи владения session новые вызовы не выполняются.

TASK05 полностью PASS: прежняя QA483dad28… изменена одним UI Save v7→8. `TASK05-calendar.xml` показывает следующий московский день02:30, `TASK05-moscow-detail.xml` явно сохраняет «Срок: 2026-09-12 02:30» и исходный редактор2026-09-11T23:30:00Z. `task05-day-boundary-prod-oracle-fixed.txt`: exactDue=true, одна TASK change v8, TASK_UPDATE6→7, CREATE1. Новых задач/напоминаний не создавали. UI/SQL независимо сверены; срез127 PASS/2 FAIL/169 NOT_RUN,25 частичных,144 без существенного результата.

## 11 сентября: сведение сохранённых FOC02, CAL01, NAV06 и INB01

После восстановления исполнения повторных HTTP-запросов не выполняли. FOC02 закрыт по `foc02-api-boundaries.json`: 7/7, пустой и список20 приняты200,21/duplicate отклонены400 FOCUS_INVALID, чужая/отсутствующая задача404. Все четыре отрицательных запроса сохраняют полный снимок focus/tasks/audit/feed/outbox. Исходный пустой фокус восстановлен; положительные сохранения дают ровно одну версию, audit и change. Для публичного API использованы2 существующие авторизованные задачи и18 официально созданных PRIVATE QA-задач; точные UUID и operationId добавлены в ограниченный ledger. Это проверка границ публичного API, не утверждение о визуальном выборе20 элементов.

CAL01 закрыт: `cal01-read-boundaries.jsonl` содержит3/3 production-проверки включённой начальной границы, исключённой конечной и нескольких дней; точные UUID/UTC совпадают с DB, домен неизменен. `TASK05-calendar.xml` подтверждает UI-порядок02:30→12:00 в одном московском дне и последующие дни. Контракт API не обещает сортировку по сроку; возрастающий порядок относится к CalendarScreen. Это уточнение области oracle, а не новая функция.

NAV06 закрыт совокупно с прежними реальными signed-out/authenticated malformed UI-проверками. `NAV06-complete-observer.log`: 1/1, четыре ошибочных маршрута ведут домой; после штатного startup API=0, synchronize=0, schedule=0. Положительный контроль явно вызывает detail после обнуления счётчика. Это отсутствие запросов от ошибочного маршрута, не нулевая штатная фоновая синхронизация. Изменён только тестовый APK.

INB01 закрыт: `INB01-filters-room-proof.json` и all/decisions/errors XML подтверждают12 существующих событий,9 решений и0 ошибок. Фильтр решений исключает3 обычных события; пустой фильтр ошибок соответствует Room; возврат Все восстанавливает список. Данные до/после совпадают. Состояния READ/RESOLVED, sequence и единственное durable mark-read уже подтверждены предыдущими API/SYNC06/INB07; их не повторяли.

Срез298 =131 PASS +2 FAIL +165 NOT_RUN;21 частичный кейс,144 без привязанного существенного результата. Полная приёмка не завершена. REM17 положительная production-проверка и PRT07 остаются FAIL; запрет четвёртой попытки plugin сохраняется.

## 11 сентября, после 07:30 МСК: TASK20, INB08 и лимиты REM17

TASK20 PASS: `task20-spoofed-read-surfaces.json` содержит только четыре недостающих GET с подставленными чужими principal/workspace/role/scopes: задачи, календарь, фокус и Inbox возвращают200 без UUID/заголовка PRIVATE483dad28… другого пространства. Снимок задач и audit неизменен. Прежние `member-acl-batch.json`/DB oracle подтверждают same-workspace MEMBER list/calendar/focus/inbox empty и GET404; NET03 отдельно подтверждает spoofed GET404. Сессия обновлена один раз штатным challenge/refresh, новых приглашений/задач не создавали.

INB08 PASS: `INB08-member-isolation-proof.json` и actual member UI после ссылок подтверждают отсутствие чужого event5a77…/заголовка, прежние собственные Inbox и задачи не менялись. TEAM-задача9300… была законно доступна до проверки и сохранена. Исходное ожидание отсутствия этой задачи в первом assertion было ошибочным oracle, не дефектом продукта. Реализованный inbox-link открывает список; отдельный event-detail не заявляется.

REM17: final script SHA0ca50ea2b05af15b42d81e4dea44e3f7c38a282278a96e170d4b3acc54fa9d0c независимо принят после добавления наблюдаемой `pg_stat_activity.wait_event_type=Lock` и `pg_blocking_pids` с точным holder до COMMIT. По одному запуску delivery/mixed/storm после06:00:5/5/20 авторизаций, превышение отклонено RECIPIENT_RATE_LIMIT/RECIPIENT_STORM_LIMIT, lockObserved=true, вызовов провайдера0. `rem17-prod-{delivery,mixed,storm}.log` и точные JSON-ledger сохранены. `rem17-prod-cleanup.json`:6+6+21 задач отменены,0 dispatchable/0 pending confirmation, все3 синтетические политики отключены, собственных соединений0. Общие materialize/claim, периодичность, тихие часы и пользовательские политики не менялись.

DEF020/021 исправлены:24 локальные проверки реальных шаблонов, совпадение выпущенного кода и три production-проверки лимитов. Это не полный REM16 send и не фактический quiet00–06→digest; REM17 остаётся FAIL до завершения его отдельного полного сценария. Срез298 =133 PASS +2 FAIL +163 NOT_RUN;19 частичных,144 без существенного результата. Следующий независимый backend-остаток REM05 mapping, без повторения уже проверенной SQL-изоляции.


NAV04 нормализован после независимого арбитража и принятого решения координатора. Исходные `docs/ARCHITECTURE.md:79–82` обещают точную задачу/диалог и сохранение маршрута через unlock. `PushEnvelope` имеет Task/Agent destinations; `FcmPushDispatcher` направляет INBOX_EVENT_CREATED к task/agent. Только новая QA Lead1/объединённая строка предполагала автоматический выбор inbox subject. Ручной `poruchik://inbox/{subject}` реализует открытие списка, а не отдельной детали события: `initialInboxSubjectId` пока не используется. Автофокус subject отмечен NOT_IMPLEMENTED вне текущего продуктового объёма; исправление новой функции не добавляется.

Фактический NAV04 PASS: `NAV04-main-inbox.xml` после PIN показывает Входящие; после прокрутки `NAV04-main-target-card.xml` содержит нужное событие; его явная кнопка открывает точную TEAM-задачу E2E_UI_REBASE_BATCH1 (`NAV04-main-related-task.xml`). Данных не меняли. Требования к точному Task/Agent назначению FCM остаются прежними и не заменяются этой проверкой списка. Срез134 PASS/2 FAIL/162 NOT_RUN;19 частичных,143 без привязанного существенного результата. Следующий мобильный набор NAV07 — порядок двух действительных намерений без отправки сообщений.


NAV07 PASS: `NAV07-last-intent-proof.json` связывает быстрые valid task483…→conversationf37… на основном AVD; после PIN/rotation/возврата остаются четыре точных сообщения второго назначения, старое task-намерение не возвращается. `NAV07-delayed-completion.log`1/1 дополняет actual UI задержанным завершением sync и SavedStateRestore; Agent/selectorB остаётся, outbox0. Никаких отправок сообщений или изменений продукта.

NET07 FAIL / DEF028: публичный PATCH неизвестного поля даёт200/v11, audit10→11/outbox10→11/feed20→22; numeric title123 даёт200/v12/title"123", audit/outbox ещё+1/feed+2. Четыре остальные JSON-границы400 без эффектов. `net07-json-boundaries.json` содержит exact operation IDs/before/after; исправление одной семьёй, без смешения с typed intent DEF016. Единственная корректная реставрация baseline title завершена200/v13 после известного отказа415 тестового helper. Срез135 PASS/3 FAIL/160 NOT_RUN;19 частичных,141 без существенного результата.


## Дедупликация до возобновления тестов

Новых тестов/HTTP/мутаций не выполняли. Все298 исторических ID машинно назначены ровно одному из16 пакетов в существующей матрице; для каждого описаны общий шаг, несколько критериев, старый proof, уникальный остаток, ресурс и зависимость. Два строгих aliases APP03→INB05 и INB07→SYNC06 не исполняются повторно. DOC13/AGT08 NOT_IMPLEMENTED исключены без PASS;294 представляющих строки требований, а не294 запуска. Backend/mobile независимо приняли план; policy/version loss REM18, production/SDK слои и разрушительные OS-предусловия сохранены.

Одной read-only сверкой ранее сохранённой actual cold-Agent FCM цепи закрыты PUSH07/AGT14/E2E04: provider delivery1 +kill beforefinal +neutralnotification/PIN/exactconversation +одинаковый messageID RUNNING→terminal. Артефакты и остатки upgrade/EX027 перечислены в исполнительном плане. Срез138 PASS/3 FAIL/157 NOT_RUN; новая зелёная отметка дана доказанной старой цепи, не обещанию выполнить её позже. До следующей команды координатора новые проверки не начинаются.


Поправка независимого timeline-review до возобновления тестов: предварительное сведение PUSH07/E2E04 снято. Receipt1789045827291 исходит от Android, kill13:10:45.933Z — host; калибровки нет. Нельзя доказать dead-at-receive ни подписью raw JSON «before final», ни обратным сравнением некалиброванных часов. Исходные артефакты не редактируются. Сохраняются доказанные cold tap/PIN/exacthistory и AGT14 same-message update; отдельный строгий cold-delivery и полный background-stage остаются. EX027 имеет только старый task-route послеpurge остаток; upgrade имеет только pending states/signed artifact остаток. Новых проверок не было. Окончательный срез дедуп-аудита136 PASS/3 FAIL/159 NOT_RUN,2 полных алиаса,2 целых NOT_IMPLEMENTED вне исполнения;294 представляющих требования,134 закрыты/160 незакрыты. Все298ID сохранены и назначены16 пакетам с общими действиями и уникальными слоями. Backend/mobile coverage-review принят с указанными оговорками.

## Первый объединённый пакет очереди после дедупликации

Начало04:57:54Z, targeted XML timestamp05:00:51Z, checkpoint05:01:47Z:3мин53с до отчёта. Gradle BUILD SUCCESS19с;4/4 tests,0skip/0error, выполнение тестов0.193с. Это измерение локального пакета, не скорость оставшихся UI/FCM сценариев.

`queue-unique-batch.log/.xml` и SyncCoordinatorTest: три новых уникальных проверки —101 операций проходят100+1 в исходном порядке без повторов; missing/foreign/duplicate results не подтверждают очередь, не применяют changes/focus и не двигают cursor; permanent REJECTED не отправляется снова при следующей synchronization. Четвёртый существующий тест этого класса выполнен как затронутый helper-regress. Все прежние60 тестов повторно не запускались. Mobile независимо принял source-review в локальной границе.

Это MemoryStore/оркестрация: настоящий Room/restart/UI не выдаются за PASS. Для SYNC08/10 добавлены точные частичные доказательства; SYNC22 дополнен no-resubmit local proof. Недостающие Room/restart/UI включаются одной следующей очередной цепью после текущей task-consumer проверки, без повторения этих local assertions. Product и production не менялись; Gradle освобождён.


## Пауза перед отключением сети — 11 сентября 2026

По просьбе пользователя все новые команды остановлены. Каноническая точка сохранена в верхнем разделе PROJECT_HANDOFF.md и двух checkpoint работников. REM05 исправленный прогон 8/8 с finally-очисткой завершён; DEF028 21 уникальный локальный тест PASS и package exit 0, финальный QA gate pending, deploy не начинался; очередь 4/4 в локальной границе, не Room/restart. Мобильная цепь остановлена на BLOCKED v9 задачи 483dad28…; PLANNED/DONE/Cancel ещё не выполнялись, Room main18/45/6. Активных QA-команд и неизвестных исходов нет; AVD/локальные сервисы оставлены. Матрица 136/3/159 и весь дедуп-план 16 пакетов сохранены без необоснованного повышения статусов.

## Работа возобновлена — 11 сентября 2026, после сохранённой паузы

Пользователь явно продолжил работу в этом же контексте. Сохранённые 16 пакетов используются без повторной дедупликации и без повторения незатронутых успешных проверок. Короткая проверка процессов не обнаружила активных Java/Maven/SSH QA-команд; обычные службы не останавливались.

QA завершил финальную независимую приёмку DEF028: строгий mapper ограничен публичными DTO, полноценная sync Mutation отвергается именно из-за строкового expectedVersion, реальная MVC/core/PostgreSQL цепь подтверждает отсутствие побочных эффектов ошибочных PATCH и единственный эффект допустимого Unicode/null PATCH с replay. Сохранённые журналы подтверждают 21 уникальный тест PASS, 0 пропусков, package exit0; новых прогонов не было. Backend получил разрешение на scoped commit трёх файлов и один проверенный выпуск TaskCore. До проверки production NET07 и DEF028 остаются открытыми.

REM05 полностью сведён: rem05-http-retest-result.json даёт8/8 через установленный action client и настоящий n8n HTTP, providerCalls0, domainUnchanged=true; exact ledger task2279661b-ebc3-4e95-b48e-3887c6037b59/run7bf23dc6-e891-47c9-8edd-fa28d57e9f3f. Cleanup: cancelled, policyEnabled=false, dispatchable0, pending0. Сохранённые SQL-проверки дополняют этот слой. Повторные create/list/cleanup не запускались.

Текущий срез: **298 = 137 PASS + 3 FAIL + 158 NOT_RUN**. Среди NOT_RUN25 частичных и133 без привязанного существенного результата. Следующий независимый остаток QA — уникальные Room/restart/response-lost проверки очереди совместно с mobile после его общей task consumer цепи; локальные4/4 не повторяются. API-сессия на время public canary передана backend исключительно, без нового invite/redeem. Plugin остаётся BLOCKED после трёх попыток, четвёртая запрещена; физический телефон не используется.

### Итог после выпуска DEF028 и независимого пакета Room

TaskCore DEF028 принят на production: commit7417e95, образ071afd97704f66a14fa80098742bf6638a8a5b04b8b2d4cec481b6982fd7d26f, тегmvp0-public-json-20260911-0705. `task-public-json-deploy.log`: healthy/restart0/readinessUP и обе точные сети; backup SHA5b77e05b67720258a72fefb6e6f6b58d0dcc9efc77e4a95386b85a736a8ae694. `def028-public-canary.json`:10/10,8 отрицательных400 без изменений, один допустимый PATCHv13→14 и точный replay без второго эффекта. Первая попытка canary остановилась на SSH baseline до мутаций, завершённая refresh не повторялась. API-сессия возвращена QA. NET07/DEF028 закрыты.

`QueueRoomRecoveryTest.kt`: три новых файловых Room-проверки PASS —101 операция сохраняет остаток после reopen; malformed result не меняет долговечную очередь/курсор/данные; lost reply после fake apply→reopen→один replay и окончательный REJECTED без повторной отправки. Это реальные Room/SQLite и контролируемый транспорт, не Android process-death и не production backend. Первый пакет26,3с; осмысленные дополнительные проверки по обнаруженному семейству7–8с каждая. MemoryStore4/4 не повторялся.

DEF029/SYNC13 открыт: три связанных красных теста показывают устаревший Focus. Поздний ответ GETv2 откатывает сохранённыйv3; старый FOCUS feed UPSERT/DELETE откатывает новый HTTP-снимок, в том числе пустой; GET, начатый до локальной правки, заменяет видимый выбор старым при сохранённой exact PENDING операции. Task и общий cursor остаются новыми. Доказательства `queue-room-recovery.xml`, `queue-room-focus-delayed-http.log`, `queue-room-focus-family.xml/.log`. Семейство передано mobile для одной правки/сборки после consumer; продукт QA не менял.

Актуальный срез: **298 =138 PASS +3 FAIL +157 NOT_RUN**; FAIL SYNC13, REM17, PRT07. Уникальные API/UI/OS слои и внешние ограничения остаются; весь продукт ещё не принят. API-сессию, текущие task IDs и эмуляторы не пересоздавать.

SYNC10 закрыт после независимого source-review mobile: malformed missing/foreign/duplicate responses проверены на настоящем файловом Room с повторным открытием; операция, cursor и Focus неизменны, malicious TaskChange не применён. Это ровно заявленный oracle fake transport only/Room; отдельный AVD повтор не добавляет критерия и не выполняется. Срез139 PASS/3 FAIL/156 NOT_RUN, частичных24, остальных132. SYNC08/09/22 сохраняют свои отдельные production/OS/UI остатки.
### Сведение готовых общих проверок — 11 сентября, после DEF029 Room gate

Актуальный срез: **298 = 146 PASS + 3 FAIL + 149 NOT_RUN**. Новые PASS: SYNC14, EX027, DOC03/04/07, REM16/18. Это сведения разных обязательных проверок общих действий, а не семь новых сквозных прогонов.

- **SYNC14:** существующий `RoomPushEventStore` сохраняет новый eventId с accepted=false при равной/меньшей версии; сохранность события не означает повторное уведомление. Этот контракт уже зафиксирован `PoruchikDatabaseTest`. `PushSchedulerPrivacyTest.staleAndSameVersionsDoNotRefreshButNewerVersionDoes` в `mobile-20260910/batch5-worker-family.log` проверяет настоящий Android worker: Room-запись есть, work IDs и время уведомления неизменны, новая версия запускает обновление. В `PUSH-approval-proof.json` два настоящих события e5b5a09c…/e626a8fb… одной APPROVAL2a7cb1af…v2 сохранены с accepted1/0. Повторный push не отправлялся.
- **EX027:** `mobile-20260910/EX027-old-route-proof.json` и XML/Room подтверждают исчезновение task/cache/calendar после TEAM→PRIVATEv2 и безопасный выход старого маршрута в список без содержимого/бесконечной загрузки. Задача4bdf8af7… восстановлена TEAMv3; операцииfd6f02d2…/d46b10e8… APPLIED. Прежние TASK13 purge и DEF007 document reopen повторно не исполнялись. TASK13 accepted assignment сохранён.
- **Документы:** `document-negative-http-result.json` —14/14 production HTTP: path/title/content/checksum/ACL, каждый отказ сохраняет точные документы, версии, audit/change/outbox. DOC03 нормализован по реальному V9: ведущий `/` допустим для логического пути; запрет абсолютного файлового пути не выдумывается. DOC04 использует прежний root/child+3 negative rollback из `MVP0_BACKEND_QA_20260908.md:52`, неизменённый V9 commit7d509f8; parent/cycle — DB-граница, соответствующего поля write DTO нет. DOC07 stale/missing checksum409 без эффектов. Для DOC05 остаются гранично допустимые размеры; искусственного максимума глубины нет. DOC08 HTTP-входы проверены, политика отображения media/reference — отдельная клиентская граница.
- **REM16/18:** `reminder-isolated-chain.json` —4/4 на существующем PostgreSQL18 с настоящими SQL001–014 и JavaScript узлами канонического workflow. Две доставки объединены в один вызов fake provider и успешно завершены с provider message ID; три смены policy/version после claim отвергаются до отправки. realProviderCalls0, fakeProviderCalls1, rollbackVerified9tables=true. Вместе с сохранёнными EX045–047 retry/dead_letter/uncertain это закрывает исходную API-ИЗО/fake границу REM16/18. REM17 реальные часы quiet→digest и EX049 настоящий restart n8n этим не подтверждаются.

DEF029/FOC04: после единой связанной правки `queue-room-focus-conflict-final.xml` даёт8/8 PASS, пропусков0 (Gradle18с, собственно тесты3,905с). Поздний HTTP/feed не откатывает highwater; PENDING и CONFLICT сохраняют локальный выбор после reopen. Два отказа enqueue — ABORT и IGNORE — не скрывают конфликт и не заменяют выбор; явный retry атомарно создаёт одну долговечную операцию и затем APPLIED. Это Room/контролируемый транспорт; окончательная проверка production/API/UI выполняется на одной APK10, SYNC13 пока FAIL до её завершения.
### Общая цепь потребителей задачи сведена без повторного исполнения

TASK04, TASK15 и CAL03 PASS. Срез **298 = 149 PASS + 3 FAIL + 146 NOT_RUN**. Предыдущая production state-machine и версии/аудит дополнены готовыми Android XML/Room доказательствами: `CONSUMER-filter-active-baseline`, `CONSUMER-focus-in-progress`, `CONSUMER-calendar-GMT`, `CONSUMER-blocked-{active,all,detail,focus,calendar}`, `CONSUMER-planned-*`, `CONSUMER-done-*`, `CONSUMER-cancel-*`. PLANNED/IN_PROGRESS/BLOCKED входят в активные; DONE доступен в готовых и общем списке; CANCELLED — в общем, отсутствует в активных/готовых. Изменённые Focus/Calendar представлены соответствующими сохранёнными снимками.

`task-consumer-done-oracle.jsonl`: задача483dad28…v10→11, операцияc4a495f5… и удаление Focusb066d42b… приняты по одному разу, Focus14. `task-consumer-cancel-oracle.jsonl`: d53619db…v1→2, операция267c98f1… и Focusb8483718… приняты по одному разу, Focus15. DONE остаётся в календаре с отметкой готовности, CANCELLED удаляется. Прежние `TASK11-calendar-with-due/after-clear.xml`, due-set/due-cleared Room и task09-fields-prod-oracle подтверждают установку/изменение/очистку срока без повтора. FOC03 пока частичен: терминальные переходы полностью подтверждены, отдельный видимый Focus после потери доступа ещё не привязан; store purge6/6 не выдаётся за отсутствующий UI-снимок.
DOC08 PASS: production HTTP14/14 уже покрыл входные title/content/checksum ограничения. Сохранённый `def029-offline-regression.xml` (27/27) содержит отказ text/html и неверного checksum без порчи проверенного кеша. Единственный недостающий reference oracle добавлен в существующий `DocumentCacheAuthorizationTest`: чужой documentId и taskId дают DOCUMENT_SCOPE_MISMATCH, metadata/content и отсутствие чужой записи подтверждены после файлового Room reopen. `document-reference-boundary.xml`:1/1, без ошибок/пропусков; Gradle7с, тест2,421с. Новый APK не нужен. Это клиентская проверка ответа плюс HTTP проверка входа; несуществующие media/reference поля write DTO не придуманы. Актуальный срез **298 = 150 PASS + 3 FAIL + 145 NOT_RUN**.
### DEF029 закрыт одной APK10 и общим Focus-сценарием

SYNC13 и FOC04 PASS; **298 = 152 PASS + 2 FAIL + 144 NOT_RUN**, FAIL REM17/PRT07. APK10 SHA256C4717801313A4D1A149BBBA6CE724C1E8515F01CA534962DCD1449F49CCD1E57, прежний signerb567…aca0. `APK10-update-proof.json`: schema6, пять таблиц сохранены точно, PENDING1/CONFLICT2,18tasks/52outbox/6messages, локальный PIN сохранён. Это успешное обновление debug-кандидата; исходные ENV02/EX018 release и pending CREATE/UPDATE границы автоматически не закрываются.

`FOC04-APK10-proof.json`, conflict/reopened/retry XML и `foc04-final-oracle.jsonl`: независимый официальный клиент A того же владельца применил ea6441b2…v15→16; Android B817337c8… получил CONFLICT, сохранил исходный payload и видимые9300f7f9…/6bf22116…. Извещение о конфликте и выбор пережили настоящий force-stop/PIN. Версия16 получена авторитетным GET; currentVersion в первоначальном конфликте отсутствовала, его не выдумываем. Явный UI retry02cf2f12…expected16 принят один раз какAPPLIED17, исходная операция SUPERSEDED с прежним payload. A/retry имеют поодной idempotency/audit записи; прежний конфликт не создал доменный эффект. Это не трассировка каждого HTTP-повтора, а доказательство точных состояний/эффектов; запрет автоматического повторения terminal дополнительно подтверждают локальные тесты. Оба AVD теперь APK10, main18/53/6, Focus17, сеть восстановлена. Телефон не использовался.
DOC01 PASS без повторной записи: `task13-document-create.py/-result.json` — штатный HMAC HTTP /internal/v1/documents,201, root document6c8d601a…v1; pathqa/task13-access.md, parent=null. Тот же документ в `TASK13-regain-document.db`: metadata и кешированный Markdown с точным checksum84c93511… совпадают с запросом; `TASK13-member-doc-regain-list/body.xml` показывает дерево и текст после синхронизации. Имя root.md исходного кейса — пример, корень определяется parent=null, а не буквальным именем.

APP04/PROP06 PASS: `public-decision-races.json` впервые исполнил именно публичные /v1/.../decision, прежний live canary был внутренним /internal/v1/intents/execute. Для каждой из трёх пар два настоящих HTTP-запроса наблюдались ожидающими общий PostgreSQL lock до COMMIT; один200/один типизированный409, terminalv2/audit1/inboxRESOLVEDv2, повтор победителя без дополнительных эффектов. Approval1d722870…APPROVED породил ровно один APPROVAL_DECIDED; proposal38b40ade…REJECTED не создал задачу. ASN07 race также успешен, осталось свести единственный read-only TASK-feed oracle для победившего DECLINE. Terminal SYSTEM rune8e56f36… — только явно отмеченная SQL-фикстура для FK proposal, не проверка создания/запуска агента; outbox0/dispatch0/provider0. Новых пользовательских задач нет. Актуально **298 = 155 PASS + 2 FAIL + 141 NOT_RUN**.
ASN07 PASS: сохранённый public race DECLINE дополнен `public-assignment-decline-feed-oracle.json`: TASK feed exactc920…=0 в интервале после07:47:36.232222Z setup до07:47:42.185602Z decision. Это read-only запрос, новая гонка не выполнялась. Актуальный срез **298 = 156 PASS + 2 FAIL + 140 NOT_RUN**.

Уточнённая экспертная оценка доступного остатка после этих закрытий: примерно9–16 суммарных активных человеко-часов; при трёх исполнителях6–10 активных календарных часов без нового дефекта, рабочий резерв8–14. Критический путь — AVD условия Android/сессии/очередь/FCM и заключительный релиз; backend документы/HTTP/эксплуатация идут параллельно. Это оценка по уникальным оставшимся границам, не умножение140 исторических ID. Внешний plugin BLOCKED3, ожидание реального quiet-окна и физическая акустика не включены, срок их закрытия не обещается. Уверенность средняя или ниже из-за стабильности AVD и ещё не исполненных системных условий; предыдущие10–17/14–23 более не являются текущей оценкой.
### Устранён пробел холодной FCM-доставки; документные границы приняты

PUSH07/E2E04 PASS. `PUSH07-dead-delivery-proof.json`, исходный347-строчный PID trace и `PUSH07-process-start.txt` дают согласованную цепь на одних Android часах: последний отсутствующий PID в1789113546.783549; запуск8080 для FirebaseInstanceIdReceiver в1789113546.879; Room приём event0a8fcb75… в1789113547.516. stopped=false записан до запроса, предыдущие samples без процесса. Реальный TASK_RUN_CHANGED относится к runb1077272…v2 и точной conversationf37f27c9…. RUNNING подходит исходному критерию, terminal специально не дожидались для kill. Прежние нейтральное уведомление→cold tap→PIN→та же conversation→авторитетная история из PUSH-cold-agent-* сохраняются; эти действия не повторялись. Старый неоднозначный trace не исправлен задним числом, его пробел закрыт новым точным доказательством.

Producer — один штатный SERVICE_OPENCLAW /internal/v1/task-runs, не синтетическое outbox-событие и не проверка public command. `push07-final-oracle.json`: SUCCEEDEDv3, «Проверка завершена.», два штатных событияRUNNING/terminal доставлены поодной попытке; других доменных аудитов0. `push07-session-tool-oracle.json` прочитал настоящую session59405d33…:6 записей, toolCalls0/toolResults0. Пустой result.toolInvocations не использован как доказательство, поскольку relay заполняет его константой. Telegram-сообщений нет.

DOC05/06 PASS: `document-positive-result.json` — root1b0f2b3a… с path500/title300/Markdown1_000_000 UTF16 принят201v1, SHA содержимого точен; Unicode update69b7f55e… дал201v2 и поодному новому audit/change/outbox, exact replay не изменил состояние. Прежние превышения14/14 дополняют допустимые границы; максимума глубины нет в исходном контракте. Четыре новые QA-документа и три явные SQL parent links сохраняются до DOC02 public list/UI приёмки, task6bf22116…v3 не менялся. API родительского редактирования не изобретён, cleanup пока не исполнялся.

Актуально **298 = 160 PASS + 2 FAIL + 136 NOT_RUN**;19 частичных,117 без существенного результата в частичном срезе.16 общих пакетов сохраняются. Закрытые cold/consumer/Focus/HTTP сценарии повторять не нужно.
DOC11 PASS по уже принятому def029-offline-regression.xml27/27: test document content rejects media and checksum mismatch while retaining verified cache отказывает text/html/неверномуchecksum и сохраняет точный прежний Markdown. Это тот же общий шаг DOC08, отдельного повтора нет. Текущий срез **298 = 161 PASS + 2 FAIL + 135 NOT_RUN**.

### Реализованный документный пакет завершён

DOC02/12/14 PASS. Все13 реализованных DOC-кейсов приняты, DOC13 официальный revoke остаётся вне реализации и не считается PASS. Актуально **298 = 164 PASS + 2 FAIL + 132 NOT_RUN**;18 частичных,114 остальных в частичном срезе.

- DOC02: штатный public refreshTree получил новую metadata fixture; `DOC-tree-ui-proof.json`/`DOC-tree-first.xml` дают root→A→A child→Z, с вложенными отступами x90/137/184/137; Z создан раньше A. Серверная сортировка дополнена ранее принятым `TaskCoreIntegrationTest.documentListUsesStableDepthFirstOrder` (b-root,z-root,a-child), неизменённый core.listDocuments используется контроллером. Родительские links заданы SQL только как setup: write DTO parent не имеет. Новый raw HTTP response order отдельно не записывался; серверный integration-оракул и реальный public→UI путь явно различены.
- DOC14: тот же проверенный rootv2 открыт offline, поля записи отсутствуют, content/hash совпадают, outbox53 неизменен; некешированный A даёт NETWORK_UNAVAILABLE. Это проверка режима только чтения, не новый документный сценарий.
- DOC12: same-workspace memberd65 UI безопасно отверг неизвестную ему задачу/маршрут без title/path/body/кеша; SQL can_read всех4docs=false/list0. Отдельно `doc12-outsider-http.json` даёт2/2 настоящих authenticated public GET списка и тела существующего TASK13/doc6c8d… от outsiderc5 другого workspace:404 NOT_FOUND, без title/path/content, без изменений task/document/audit/change/outbox. Использована существующая identity со штатным refresh, новых приглашений/устройств нет. Это не SQL подмена HTTP.
- Очистка: `document-positive-cleanup-result.json` — exact4 rows удалены, remaining0, audit5/change5/outbox5 сохранены. `DOC-cleanup-ui-proof.json` и empty-list XML: штатный refreshTree очистил metadata/cache всех4; задача6bf…v3, Focus17 и клиентский outbox53 неизменны. Сохранённые пользовательские/другие QA документы не затронуты.

Следующие общие действия уже назначены: mobile EX054 offline FCM→EX050 force-stop semantics на одной существующей QA-задаче, по одному событию на каждое разное OS-предусловие; backend EX017 wrong-role/body-spoof по actual internal allowlists, без повторов HMAC/NET01 и сегодняшних положительных вызовов. Snapshots backendworkspace8012 отделены от mobileworkspace332bed, чтобы независимые действия могли идти параллельно.
### FCM offline / force-stop — 11 сентября, пакет завершён

EX054/EX050 приняты по FCM-OFFSTOP-proof.json и mobile-os-final-audit.json. Два разных события одной существующей задачи, два разных системных предусловия, две единственные серверные мутации. В force-stop72,35с нет приёма, после обычного запуска состояние восстановлено через HTTP; это не повторная FCM-доставка. Состояние18/53/7, Focus17 сохранено, сеть и приложение восстановлены. PUSH15 сведен по прежним4 FCM integration и production-доказательствам без повторного теста. Срез167/2/129 из298; подробные границы в канонической матрице.

### Изолированный FCM HTTP и service roles — пакет завершён

PUSH16/EX051: один новый метод actual dispatcher+existing PostgreSQL при fake HTTP,1/1 без пропусков,8,909с Maven/4,099с test, реальных отправок0 и rollback подтверждён. Существующие4 FCM integration не повторялись. EX017 закрыт11 wrong-role/spoof отказами и4 недостающими allowed-role допускающими доменную проверку запросами без эффектов. VOI04 конечная ошибка при отсутствии recognizer и восстановление пакета приняты. Актуальный срез171/2/125 из298;16 пакетов проверены на полноту без повторной дедупликации.

### AUTH14: завершён оставшийся контролируемый повтор401

Два прежних теста не повторялись; новый auth14-second401.xml1/1 проверяет новый credential, неизменность запроса/idempotency и отсутствие цикла при втором401. Gradle4с/test0,150с, продукт без изменений. Срез172/2/124,16 пакетов и298 уникальных ID сохранены.

### DEF030/031: собраны два воспроизводимых UI дефекта

AUTH03 paddedinvite→экран без действий: первая инструментальная попытка,1,839с,0сетевых запросов/состояние сохранено. SYNC17 legacycompare→готоваяv2, но старыйtitle: второй уточняющий прогон подтвердил завершённыйGET и5с неизменности. Одна продуктовая auth/queue пачка начата mobile; тесты/доказательства в канонической матрице. Срез172/4/122,17 partial.

### NET08: два ограниченных прогона и DEF032

Первый потерял raw ответ, второй с16 concurrent сохранил57GET200/61GETplain429. Read-only подтвердил наш source-IP proxy60/60s доGateway120, а не внешнийegress. DEF032 — отсутствиеcode/trace согласно исходному общемуerrorконтракту; квоты не меняются. Gatewayграницы/identityisolation остаютсянепроверенными. Нагрузочныхпопыток2/3, третьей сейчас нет. Срез172/5/121; подробныеартефакты вматрице.

### Повторная критика необходимости и готовые сведения — 11 сентября

APK11 принят без дополнительного прогона: `mobile-20260910/APK11-final-proof.json` связывает два исправленных теста2/2 PASS за8,775с с exact APK SHA256`3B5CEF0A…351A318C` и signer`b567…aca0`. На обоих AVD сохранены PIN и все14 Room-таблиц при install; после launch изменился только sync timestamp. AUTH03/DEF030 и SYNC17/DEF031 переведены в PASS. Это debug update и не закрывает ENV02/EX018 same-cert release upgrade.

EX023 принят композицией `mobile-20260910/EX023-boundary-proof.json` и `ex023-mobile-refresh-gateway.json`: exact production origin/allowlist, forbidden host1/1, `adb reverse` пуст, а main refresh в согласованном device-window дал POST`/v1/sync`200 и GET`/v1/focus`200 с точными correlation IDs. Packet capture не запускался и не требуется принятому destination oracle.

TGL02 завершён одним официальным expiry-oracle. `tgl02-expiry-result.json`/`.log`: nonce создан201 один раз; initial timeout восстановлен exact idempotent replay с тем же expires`09:18:54.172913Z`; consume`09:21:36.836Z` с valid HMAC дал `401 AUTH_INVALID`+trace, service nonce0→1. Link nonce до и после не consumed/revoked, bindings/tasks/audit hashes неизменны, provider calls0. Wrong bot/expected actor не являются полями consume API; replay/HMAC/untrusted context уже покрыты NET09/10, TGL01/03, PRT02/EX065. TGL04 закрыт без нового HTTP mini-chain по контракту status/revoke без target principal, scoped lifecycle proof и NET03.

Существующие доказательства также закрыли ENV06, PRT04–06, VOI02/06/07 и OPS08; точные ссылки внесены в строки матрицы. Текущий срез **298 =185 PASS +3 FAIL +110 NOT_RUN**; FAIL NET08, REM17, PRT07;14 частичных и96 остальных NOT_RUN.

Исполнительный план облегчён. EX001/EX059/OPS10 дают один финальный artifact gate. NET12/EX022/NET14 ограничены тремя различающимися ветвями: pre-send, неопределённый read-timeout и offline→restore. EX025 оставляет один low-storage/write-failure oracle; EX058 — одного владельца состояния на класс ошибки. EX049 actual n8n restart, EX057 OEM/work-profile и EX060 rollback конкретного кандидата не выполняются как обязательные активные тесты и не получают PASS по отмене; durable lease recovery EX049 подтверждён SQL-клиентами, но startup/in-flight process crash остаётся непроверенным.

Mobile production SYNC04/15/19 уже получил два настоящих CONFLICT, force-stop/PIN, сохранённый TITLE draft, final explicit retry expected7→APPLIED8 и сохранённый server DESCRIPTION. `sync19-final-v8-oracle.json` подтверждает три принятые операции и audit5→6→7→8 без лишнего эффекта. Полный PASS удерживается до единственного UI cleanup и singleton-финала.

Оценка доступного остатка после сокращений:4–7 календарных часов без новых дефектов,6–10 с резервом при параллельной работе. Backend DEF032 source recovery→minimal candidate→дифференциальный регресс требует ориентировочно3–5 активных часов; runtime не меняется до review. Mobile cleanup занимает1–2 часа, затем same-cert release gate ещё1–2 часа при наличии baseline/подписи. В оценку не входят внешние ожидания: plugin BLOCKED3, реальное quiet-окно REM17 и акустика VOI01/08.

### SYNC04/15/19: единая production conflict-цепь завершена

`mobile-20260910/SYNC19-final-proof.json`, `sync19-final-v8-oracle.json` и `sync19-cleanup-final-oracle.json` независимо приняты. Android B сохранил TITLE-only intent с expected5; server actor A изменил DESCRIPTIONv5→6, поэтому первый sync дал настоящий CONFLICT6 без эффекта. После compare A изменил DESCRIPTIONv6→7, explicit retry Android дал второй настоящий CONFLICT7. Force-stop/PIN/reopen сохранил тот же draft/dirty и блокировку Save до нового compare; окончательный explicit retry применил только TITLE как APPLIEDv8, не потеряв server DESCRIPTION phase2.

Backend oracle содержит ровно три принятые операции v5→6→7→8 и audit5→6→7→8. Оба conflict operation records после supersede отсутствуют; бесконечного автоматического retry нет. Единственный UI cleanup `1ef2ece2…` дал один HTTP200/APPLIEDv9 и audit8→9, восстановил исходные title/description и точный baseline business hash`2404e4b8…`; документов0. На Android все прежние53 outbox rows неизменны, итог18tasks/57outbox/7agent messages, PENDING/CONFLICT0, Focus17 и Agent неизменны; сеть и recognizer восстановлены.

SYNC04, SYNC15 и SYNC19 переведены в PASS; DEF001 закрыт. Дополнительная критика не нашла отдельного продуктового oracle во втором AVD-writer: конфликт и merge исполняет Android B, а независимый официальный API actor A создаёт ту же авторитетную версию, при этом Android mutation/Room и изоляция второго AVD уже проверены отдельно. Поэтому E2E05 сведен той же цепью без нового прогона. Срез после сведения: **298 =189 PASS +3 FAIL +106 NOT_RUN**;14 частичных и92 остальных NOT_RUN. Conflict-цепь повторять нельзя.

### EX025: сокращённый write-failure oracle выявил DEF033

Единственная оставшаяся уникальная ветвь EX025 исполнена без AVD fill и без повторения font/display/locale/Doze. `mobile-20260910/EX025-create-write-before.log`:1/1 FAIL на установленном APK11. Controlled trigger в уникальной test Room вызвал `SQLiteConstraintException qa-internal-storage-detail` в `TaskDao_Impl.upsertAll`→`RoomTaskRepository.createOffline`; исключение вышло необработанным до ожидаемого сообщения интерфейса. Реальная Room приложения и production не затронуты.

DEF033 зарегистрирован. Минимальный критерий: CREATE transaction возвращает санитизированный Failure; dialog/title не закрываются и не очищаются до Success; Room/outbox/placeholder остаются без изменения, retry доступен. Старые Focus ABORT/IGNORE и task retry insertion rollback не повторять; они подтверждают атомарность соседних путей, но не UI fail-safe CREATE. EX025 переведён из NOT_RUN в FAIL. Срез после регистрации: **298 =189 PASS +4 FAIL +105 NOT_RUN**.

INB06/EX021 закрыты одним сокращённым actual AVD online-only oracle без нового fixture/test APK. `mobile-20260910/EX021-online-only-proof.json`: при отсутствии default network нажатие существующего approval action дало конечный `NETWORK_UNAVAILABLE`; все14 Room tables точны, outbox/domain mutation0. После восстановления Wi-Fi/default network явная кнопка повторного решения доступна; online решение не отправлялось. Общий путь `InboxScreen→OnlineApprovalRepository.decideOnline→MobileApi` при ошибке не ставит offline mutation. Backend/API запреты и отсутствие mobile document write/tool/Telegram paths уже доказаны отдельно; cross-product типов не выполнялся.

Текущий срез **298 =191 PASS +4 FAIL +103 NOT_RUN**; FAIL NET08, EX025, REM17, PRT07;14 частичных и89 остальных NOT_RUN.

### EX025/DEF033: исправленный CREATE write-failure принят на APK12

Минимальная правка независимо принята без расширения матрицы: repository transaction возвращает санитизированный `TASK_CREATE_SAVE_FAILED`, повторно бросает cancellation и вызывает уведомление об очереди только после commit; UI сохраняет dialog/title до Success и блокирует повторное нажатие. Тот же ранее красный `MissingTaskRecoveryUiTest` на `APK12-create-write-ui.log` прошёл1/1 за9,129с. Controlled trigger дал конечную санитизированную ошибку, сохранил draft и доступный retry; baseline task/outbox остался неизменным через второй file Room handle. После удаления trigger один явный retry создал ровно1 CREATE PENDING; realNetwork0, test Room очищена.

`mobile-20260910/APK12-update-proof.json` связывает результат с APK SHA256`75f9572b22e22c6ad4897e61a3ac9f3516fb1667850860d2021270c3d06d3b4a` и прежним signer`b567…aca0`. Обе AVD после update сохранили PIN и точные14 Room-таблиц: main18/57/7, member3/0/0; обе открыли Tasks. EX025 переведён в PASS, DEF033 — RETEST_PASS. Debug update не закрывает ENV02/EX018 same-cert release upgrade. Новый срез: **298 =192 PASS +3 FAIL +103 NOT_RUN**; FAIL NET08, REM17, PRT07.

### DEF032: production-формат 429 исправлен, NET08 удержан

Восстановленный исходник `server/public-http-proxy/main.go` и тесты независимо приняты. Изолированный network-none differential сравнил старый ELF SHA`f6b0a764…1eea9` и кандидат:21/21 HTTP-сценарий, единственная дельта — plain429→bounded Problem JSON. Сохранены path/query/method/body1MiB±1, XFF/XProto, таймауты, `DefaultTransport`, sliding60/IP/60s, independent IP и spoof same IP; `TestSlidingLimit`/`TestRateProblem` PASS. Две Go1.25.1/CGO0 сборки совпали SHA`71b15b10…79846d9`. Точные параметры контейнера, hashes и ответы сохранены в `def032-execution.json` и `def032-comparison.json`.

One-shot выпуск commit`689b2c8` завершился `RELEASE_VERIFIED` в`09:39:42Z`: изменён только `/usr/local/libexec/poruchik-public-http-proxy`, live SHA совпал с кандидатом, unit SHA`37f2e3…09621` не изменился, service active и readiness через port80 с query прошли. Старый бинарник и unit сохранены в `/opt/poruchik/backups/def032-release-20260911`; rollback не понадобился. Одна loopback canary до первого отказа дала59 health200 и затем429 `application/json`/`RATE_LIMITED`, traceId`17d6…72e9`, равный `X-Correlation-Id`. Доказательства: `def032-release-result.json`, `def032-canary-result.json`.

DEF032 переведён в RETEST_PASS. NET08 остаётся FAIL: эта canary и локальный independent-IP regression не доказывают, что другой production source не затронут после насыщения первого. Точный остаток — один узкий source-isolation oracle; Gateway120, новый общий burst и изменение лимитов не требуются. Счёт не меняется: **298 =192 PASS +3 FAIL +103 NOT_RUN**.

### Семантика остатка NET08/REM17/PRT07 и signed artifact gate

NET08 закрыт без нового runtime-прогона. Gateway `server.go:64–67,377` создаёт authenticated limiter120/min и выбирает key строго как `principal:`+успешно аутентифицированный principalId; `limiter.go` хранит отдельный bucket на key. Сохранённые `TestLimiterWindowAndIsolation` и `TestLimiterConcurrentBound` подтверждают N-bound, concurrency и независимый key b. `def032-comparison.json` отдельно доказывает independent TCP source200 и невозможность spoof сбросить насыщенный same-source bucket; production canary подтверждает активный front boundary и исправленный429. Прямой published121 законно недостижим через front60 и не является новым продуктовым oracle.

REM17 переведён из FAIL в NOT_RUN. Исторические дефекты digest/overflow и rate сохранены в журнале, но исправления подтверждены24 local checks и тремя production limit modes5/5/20. Воспроизводимого неисправленного поведения сейчас нет; остаётся только фактически не наблюдавшийся quiet00–06→digest. PRT07 остаётся единственным FAIL: исправленный candidate существует локально, но live plugin всё ещё воспроизводит старое поведение; три попытки deploy исчерпаны, четвёртая не выполняется.

`final-release-artifact-gate.json` подтверждает один подписанный release APK SHA`9c5ad4a20d195d80e63ce7d7ed610da162244c319b18cdf0f797ccb33465aefd`, v2/v3, reference certificate`4ca92b…224f`,16K alignment и exact624 ZIP entries относительно reviewed unsigned; использовано одно password window. Это готовая часть общего EX001/EX059/OPS10 gate. Строки пока NOT_RUN: ещё нужны общий component/source manifest, diff/secret/SBOM/locks scan. ENV02/EX018 отдельно BLOCKED_EXTERNAL_AVD_NETWORK3 и подписью не закрываются.

Новый канонический срез: **298 =193 PASS +1 FAIL +104 NOT_RUN**;15 частичных и89 без существенной привязки. Распределение NOT_RUN: release/artifacts9; Android/navigation7; enrollment8; session/revoke7; task/focus/calendar7; queue/network recovery8; assignment/decision/ACL16; documents1; agent/plugin16; voice2; FCM8; reminder tools10; scheduler2; operations3; HTTP0; Telegram0. Доступные сокращённые функциональные группы остаются, поэтому ETA полного доступного остатка сохраняется4–7 календарных часов без новых дефектов и6–10 с резервом; внешние plugin/quiet/acoustic/release-AVD условия не включены.

### EX019: один наблюдаемый in-flight double-action выявил DEF034

Последовательные ADB taps не были выданы за concurrency: local Room save завершается быстрее доступного UI snapshot. Вместо новой инфраструктуры использован один существующий ComponentActivity/test host и actual Room repository; latch удержал возврат после реального commit, пока второй pointer tap и ровно одна recreation произошли внутри той же незавершённой UI-операции. `mobile-20260910/EX019-held-save-before.log`: targeted test FAIL за5,684с; `EX019-held-save-state.log`: `heldReply=true,calls=2,pending=2`. Draft после recreation сохранился, но второй UPDATE уже создан.

DEF034 зарегистрирован. Минимальная правка — общий edit in-flight flag, установленный до coroutine launch/первого suspend; control disabled/repeated click no-op, reset в finally. Повторяется только этот test, соседние TASK10/rotation/Room/API singleton проверки не повторяются. EX019 переведён из NOT_RUN в FAIL. Срез: **298 =193 PASS +2 FAIL +103 NOT_RUN**;15 частичных и88 без существенной привязки.

DEF034 закрыт тем же одним targeted test без нового сценария. `APK13-double-save-ui.log`:1/1 PASS за5,542с; первый ответ удержан после actual Room commit, но repository calls1/pending1, второй pointer tap no-op. Одна actual recreation произошла до release, draft сохранился, после завершения Save снова enabled. Продуктовая дельта ограничена `TasksScreen.savingTask`: синхронный guard до coroutine launch, disabled control и reset в finally. Этот общий finally покрывает success/failure/cancellation; существующие recovery proofs уже подтверждают доступность retry, поэтому отдельный зеркальный failure method не запускался. EX019 PASS, DEF034 RETEST_PASS. Срез: **298 =194 PASS +1 FAIL +103 NOT_RUN**.

ENV03 закрыт одним offline negative-install пакетом на существующем non-debug release AVD5564. `mobile-20260910/ENV03-final-proof.json`: APK13/debug с другим signer отклонён как `UPDATE_INCOMPATIBLE`; готовый старый release01.09 v1 с тем же certificate`4ca…224f` отклонён как `VERSION_DOWNGRADE` поверх установленного release v2. Installed SHA, UID, version и install times до/после точны; network/clear/uninstall0. Raw Room не заявлен, потому что отрицательные установки не заменили пакет. ENV02/EX018 этим не закрыты. Срез: **298 =195 PASS +1 FAIL +102 NOT_RUN**;15 частичных и87 без существенной привязки.

ENV09 выполнен одним semantics inspection без включения TalkBack и без системных/продуктовых мутаций. На задачах, календаре, inbox и доступном сохранённом voice-review слое видимые controls проверены с учётом descendants/clipping. `mobile-20260910/ENV09-task-delegation.xml` выявил две delegation CheckBox: text/content-desc пусты, clickable/focusable/checkable=true, NAF=true; расположенные рядом подписи — отдельные sibling Text и не именуют checkable nodes. Остальные осмотренные поверхности не дали безымянных видимых controls.

DEF035 зарегистрирован. Минимальная правка — связать две существующие русские подписи с checkable semantics без изменения действий; повторить только task delegation dump. ENV09 переведён из NOT_RUN в FAIL. Срез: **298 =195 PASS +2 FAIL +101 NOT_RUN**;15 частичных и86 без существенной привязки.

OPS06 впервые проверен на фактической потере lease: прежний `expiredLeaseRetriesAndMovesToDlqAfterBoundedAttempts` всегда вызывал fail() и не моделировал исчезновение worker. `ops06-lost-lease-red.log` показал, что после истечения пятого lease delivery остаётся LEASED attempts5, run RUNNING, а шестой claim возвращает null. Production не затронут, test transaction откатан; зарегистрирован DEF036.

Candidate ограничен `TaskRunDispatchService.claim`: существующий locked query также выбирает только expired exhausted LEASED, затем с текущим leaseToken вызывает существующий atomic fail-path без выдачи шестого lease. `ops06-dispatch-batch-green.log`:3/3 PASS/0 skip на actual PostgreSQL — новый loss path и две затронутые регрессии accepted/explicit-fail. Активный fifth lease не тронут; четыре stale tokens и late complete rejected; после expiry DLQ/FAILED attempts5/runv3, replay fail/claim без дублей, change events2/tasks0, rollback. Maven package exit0; frozen JAR SHA`7196a556d3c928b3eeb14b48fb251d40ac7dd80c038fe81aa4abf9cfb56b89ac`. Source/tests gate PASS, production release pending; до него OPS06 остаётся FAIL. Срез: **298 =195 PASS +3 FAIL +100 NOT_RUN**;15 частичных и85 без существенной привязки.

DEF035 закрыт одной целевой проверкой на APK14. `mobile-20260910/APK14-delegation.xml` подтверждает у обеих delegation CheckBox точные русские `content-desc`, отсутствие NAF и сохранность прежних checkable/checked/clickable/enabled/focusable состояний; остальные поверхности ENV09 не повторялись. Сохранённый voice review сводится с прежними UI3PASS и source semantics; акустический ввод остаётся отдельным остатком VOI01/08. `APK14-final-proof.json`: SHA`8c9cd70944ebbb8fe73b32f1a2299a933c25c08ac8ab1b753f7654f417cf9fda`, прежний signer, обе AVD сохранили точные14 Room-таблиц, production-мутаций0. ENV09 PASS, DEF035 RETEST_PASS.

Для EX019 дополнительно выполнена только ранее отсутствовавшая Failure→retry ветвь общего guard. `APK14-edit-retry-ui.log`:1/1 PASS за4,419с — первая контролируемая Failure сохранила draft и0 PENDING, затем один явный retry создал ровно1 PENDING. Held-save/recreation тест не повторялся. Актуальный срез: **298 =196 PASS +2 FAIL +100 NOT_RUN**; FAIL OPS06/DEF036 и PRT07;15 частичных и85 без существенной привязки.

TASK06 и TASK07 закрыты сведением уже снятых слоёв без новой seed/mutation. Для TASK06 `APK14-detail.xml`, main Room и `task06-existing-detail-oracle.json` совпали по title/status/due/visibility/description/result/version exact task9300…v6. Для TASK07 `CONSUMER-offline-detail.xml` и сохранённые baseline DB/JSON показывают task483d…v8 с основными полями, конечным `NETWORK_UNAVAILABLE` и явным сообщением «Нет связи…», без ложной пустой карточки и при outbox delta0; сеть была восстановлена в сохранённом checkpoint.

FOC03 закрыт минимальным отсутствовавшим UI-consumer oracle в композиции с уже принятыми production EX027 purge и прежними DONE/CANCELLED UI+API singleton. `NavigationFocusUiTest.accessLossPurgesVisibleFocusAndStaysEmptyAfterDatabaseReopen` на отдельной fileRoom и настоящих repositories/RoomSyncStore дал1/1 PASS за2,712с: focus visible до DELETE change, затем конечный empty без stale, после close/reopen также empty, PENDING0. Первая попытка оснастки проверяла UI до асинхронной Room invalidation; bounded `waitUntil` исправил ожидание, продукт не менялся. Доказательства `FOC03-access-purge-ui-final.log` и `FOC03-access-purge-state.log`. Актуальный срез: **298 =199 PASS +2 FAIL +97 NOT_RUN**; FAIL OPS06/DEF036 и PRT07;14 частичных и83 без существенной привязки.

DEF036/OPS06 принят на production. Выпуск commit`993c4f2` заменил только TaskCore на image`be1a9a9d…b9b09` с JAR`7196a556…b56b89ac`; matching exhausted-lease predicate был0 в preflight, повторно0 после validated dump непосредственно перед записью и0 после. Container healthy, readinessUP, exact две сети и config неизменны; global claim и production fixture не запускались. Dump SHA`9d4288ee…5862e0`, rollback не понадобился. Доказательства `def036-release.log`, `def036-release-exit.json`; DEF036 RETEST_PASS, OPS06 PASS.

AUTH04 закрыт одним public POST уже существующего expired/unconsumed invite2d32…. Fresh valid ES256/SPKI получил401 `AUTH_INVALID`+trace; hashes invitation, memberships, principals, devices и sessions до/после совпали, новых enrollment/provider0. `auth04-expired-invite-result.json`. Это отдельная invitation redeem граница, TGL02 её не заменяет.

NAV02 закрыт одной official PRIVATE QA fixture task6c01…v1. На warm activity deeplink дал конечную последовательность loading→точный detail; первый recovery sync и target GET200 коррелированы по времени. Реальный FCM пришёл в том же окне и вызвал второй штатный sync, поэтому общий HTTP-total не выдан за число recovery. Reopen дал sync0 и один обычный detail GET, что соответствует текущему контракту полного detail; cache не очищался. Fixture штатно переведена CANCELLEDv2 одной операцией, прежние18 задач и8 таблиц сохранились точно. `NAV02-runtime-proof.json`, video/frames/XML, Room и Gateway windows.

CAL02 закрыт без повторной смены настроек: `CONSUMER-timezone-proof.json`, Calendar GMT/LA XML и baseline Room/API показывают GMT/auto1→America/Los_Angeles→GMT/auto1, одинаковые тексты и порядок; due23:30Z отображается02:30МСК через границу суток без double shift. Актуальный срез: **298 =203 PASS +1 FAIL +94 NOT_RUN**; единственный FAIL PRT07;14 частичных и80 без существенной привязки.

REM14 закрыт одним isolated actual SDK dispatcher run без provider/network. `rem14-dispatcher-result.json`: первый callback вернул handled=true; domain confirm, invoke, failed edit, clear и success reply вызваны ровно по одному разу. Replay того же dedupeId дал duplicate=true, все счётчики неизменны. Exact SDK/core hashes и bounded network-none/read-only command сохранены. Локальный console UnicodeEncodeError возник только после записанных result/exit0 и не является продуктовым отказом; тест не повторялся.

TASK02 закрыт композицией существующих доказательств без новой задачи или запроса. UI XML подтверждают точные title длиной1 и300; Room содержит canonical tasks5b59…/cf03… и APPLIED create operations. `task02-existing-correlation.json` связывает обе операции с canonical entityId/version1, exact title hashes и единственными create audit/outbox/idempotency. Общий SYNC07 proof отдельно подтверждает immediate local PENDING placeholder→canonical remap/APPLIED без сохранения локальной копии. Актуальный срез: **298 =205 PASS +1 FAIL +92 NOT_RUN**; единственный FAIL PRT07;12 частичных и80 без существенной привязки.

AUTH05 и EX009 закрыты одним narrow actual-PG тестом `TestPostgresEnrollmentReplayOnly`,1/1 PASS,0 skip,0,09с. Same requestId/body вернул exact credential/token/expiry; changed SSH body дал ErrConflict, новый requestId после consume и repeated redeem — ErrConsumed. HTTP source mapping нормализует оба состояния к409 `CONFLICT`; специальные `INVITATION_ALREADY_USED`/`REQUEST_ID_REUSE` не являются public contract. Member/device/session/SSH credential созданы по одному, audit0 неизменно, disposable workspace cleanup0. `auth05-ex009-enrollment-local.log`, result JSON; commit`e2a5453` содержит только этот test file.

SYNC22 подтверждён FAIL в одном targeted existing-host тесте за3,313с. Actual SyncCoordinator сохранил fileRoom REJECTED `PERMISSION_DENIED`; reopen не отправил операцию повторно, cursor31 применил позднюю task2. Normal UI при первом показе и после reopen вывел только общий summary «0 отправлено,1 получено» без безопасного кода. `SYNC22-rejected-ui-before.log` и state; production0. Зарегистрирован DEF037. Минимальная правка ограничена read-only notice из existing outbox, нормализацией errorCode и локальным acknowledgement watermark; retry/schema/protocol не добавляются. Актуальный срез: **298 =207 PASS +2 FAIL +89 NOT_RUN**; FAIL PRT07 и SYNC22/DEF037;11 частичных и78 без существенной привязки.

AUTH06/EX011 закрыты двумя узкими существующими harness tests,2 methods PASS,0 skip. Actual HTTP malformed/P384 key дал401 `AUTH_INVALID`, wrong algorithm400 `DEVICE_KEY_INVALID`, unknown field400 `REQUEST_INVALID`, spy storeCalls0. Actual-PG tamper invitationId/token отклонён при domain delta0 и cleanup workspace/invite/device/session0. Строгие P-256 crypto/replay tests не повторялись. Исходное EX011 описание исправлено: wire не содержит клиентских role/workspace и RSA signature; server provisioning авторитетен, tamper protection проверена на реальных invitation/token/SSH credential полях. `auth06-ex011-result.json`/local log; commit`117b5d8` только два test files.

DEF037 закрыт на APK15 тем же одним targeted методом за6,038с. До отказа notice отсутствует; после REJECTED он сохраняется через reopen, операция не отправляется повторно, cursor31 и позднее изменение применяются. Acknowledgement watermark `(createdAt,operationId)` per Room namespace скрывает старые после reopen, будущий отказ появляется; invalid raw code отображается как `OPERATION_REJECTED`, payload hidden. Ack меняет только preferences, outbox/state неизменны. `APK15-rejected-ui.log`/state и `APK15-final-proof.json`: SHA`09072131…bfee1d`, прежний signer, обе AVD14таблиц exact, PIN сохранён, normal Tasks без false warning. DEF037 RETEST_PASS, SYNC22 PASS. Актуальный срез: **298 =210 PASS +1 FAIL +87 NOT_RUN**; единственный FAIL PRT07;11 частичных и76 без существенной привязки.

EX012/013 закрыты двумя узкими gap tests,2 methods PASS,0 skip. Correct P1363 signature после exact challenge expiry дала actual-PG unauthorized при неизменных session/device/challenge hashes, generation/last_seen/used_at и cleanup0. HTTP unknown field/trailing JSON дали400 `REQUEST_INVALID`, RotateSession0. Сохранённый production auth-family proof уже покрывает wrong device/token/signature/key, нерасходование challenge негативами и replay409→family revoke; эти ветви не повторялись. EX013 нормализован к реальному refresh DTO и raw64 P1363: alg/timestamp/nonce/bodyHash/DER не являются его wire contract.

EX014 закрыт тем же сохранённым production family oracle: generations1..6/five parents, valid refresh after negatives, latest protected200, затем replay409 и old/latest access401 после family revoke. Новых rotation/probe нет. Актуальный срез: **298 =213 PASS +1 FAIL +84 NOT_RUN**; единственный FAIL PRT07;11 частичных и73 без существенной привязки.

EX061 и refresh-ветвь AUTH18 закрыты одной natural-expiry mobile цепью. Offline manual sync сохранил PENDING и показал конечное санитизированное сообщение без секрета. Session generation41 естественно истёк; reconnect дал challenge201/refresh200/sync200 и generation42, op8d62… APPLIED/audit ровно один раз. Первый UI tap до готовности сети честно отмечен как offline; второй manual sync0/0, требование одного tap не заявляется. Cleanup op7ae5… APPLIED/audit singleton восстановил исходный title/descriptionnull/docs0; Focus/docs/assignments/calendar/agent exact, сеть восстановлена. `EX061-runtime-proof.json`, `ex061-natural-expiry-oracle.json`, Gateway log и `ex061-cleanup-oracle.json`.

EX015 выявил DEF038 в одном actual-PG repro. Official RevokeDevice(A) отозвал device/SSH/session/challenge и protected auth стал401, но push registration A осталась enabled, actual FCM recipient SQL и READY candidate продолжили выбирать A. Device B сохранил enabled/recipient/candidate/auth; provider0, cleanup0. `ex015-revoke-push-red.log`. Fix ограничен atomic disable registrations и A READY/RETRY deliveries, terminalization affected events без иных nonterminal deliveries, active-device filters и pre-provider guard. SENDING, прошедшая guard до observed revoke, остаётся честной in-flight границей. Актуальный срез: **298 =215 PASS +2 FAIL +81 NOT_RUN**; FAIL PRT07 и EX015/DEF038;11 частичных и70 без существенной привязки.

EX056 закрыт одним actual OS notification route и точным ACL restore. Event71cd…/PendingIntent a16d… указывал task4bdf…; tap после PRIVATE дал PIN gate, после unlock — конечный список доступных задач без target, Room target purged, outbox0, backend memberCanRead=false. Непрерывного видео после PIN нет, поэтому transient frame-by-frame не заявляется; lock gate/source и final purge дают достаточную композицию, ACL ради видео не повторялся. Restore TEAM6 вернул memberCanRead=true одной canonical record/audit5→6; Room outcome REPLAYED отмечен честно. `EX056-runtime-proof.json`, `ex056-restored-oracle.json`.

EX055 закрыт без нового biometric destination цикла: AUTH10/11 уже дали actual fingerprint success/failure/cancel, EX056 доказал exact locked PendingIntent/PIN route, а AuthGate использует один linkedTarget path после любого successful Authenticated и не переходит к target после failure/cancel. Физическая биометрия не заявляется.

SYNC08 закрыт композицией без production101. FileRoom test сохранил101 ordered operations, применил первые100, после reopen сохранил tail1 и завершил exact ID order/cursor101. Production SYNC07 доказал тот же coordinator на CREATE→2UPDATE с canonical singleton. Текущий SyncCoordinator отправляет `listOf(next)`, поэтому каждый HTTP body фактически1≤100; искусственный100+1 production trace не является отдельным риском. Актуальный срез: **298 =218 PASS +2 FAIL +78 NOT_RUN**; FAIL PRT07 и EX015/DEF038;10 частичных и68 без существенной привязки.

SYNC09 закрыт композицией существующих доказательств без новой production-мутации. Actual op `37385…` прошёл PENDING → клиентский `SocketException/TIMEOUT` → APPLIED с outcome REPLAYED; backend подтвердил единственные canonical mutation и audit. Gateway window содержит успешные sync, но не op ID, поэтому точная граница потери ответа после server apply и повторного открытия взята из уже пройденного файлового `QueueRoomRecoveryTest`. Новый срез: **298 =219 PASS +2 FAIL +77 NOT_RUN**;9 частичных и68 без существенной привязки.

SYNC21 закрыт композицией существующих production/Room доказательств и одним адресным файловым тестом. Сохранённые consumer и access-loss цепи покрывают UPSERT всех пяти сущностей и DELETE TASK/DOCUMENT/CALENDAR. Новый `QueueRoomRecoveryTest` 1/1 за2,535с покрыл только отсутствовавшие INBOX_EVENT/ASSIGNMENT DELETE: malformed завершающее изменение полностью откатило обе строки и cursors после reopen; затем valid DELETE2 удалил только targets, sentinels сохранились. Product/AVD/production не менялись, старые зелёные ветви не повторялись. Новый срез: **298 =220 PASS +2 FAIL +76 NOT_RUN**;9 частичных и67 без существенной привязки.

NET12/EX022/NET14 закрыты сокращённой композицией без новых сетевых переключений. EX061 дал actual AVD no-network с конечной безопасной категорией, сохранённым PENDING и последующим singleton; EX056 — actual TIMEOUT и REPLAYED/canonical1; EX20 — WorkManager SUCCEEDED/runAttempt1/EXPONENTIAL10000 и canonical1. Реальный локальный `OkHttpMobileApiTest` покрывает CONNECT_FAILED/ECONNREFUSED, RESPONSE_HEADERS_START/READ_TIMEOUT и sanitization. Debug telemetry sink отключён, поэтому AVD phase/host и физический failed-request count не заявляются; буквальный DNS/IPv4/IPv6 cross-product не выполнялся как повтор эквивалентных продуктовых исходов. Новый срез: **298 =223 PASS +2 FAIL +73 NOT_RUN**;8 частичных и65 без существенной привязки.

SYNC11 закрыт без нового pull. Actual MEMBER Room snapshots дают последовательность cursor449→486→491→493 с изменениями TEAM→PRIVATE→TEAM; `sync12-cursor-boundaries.json` подтверждает ordered changes и monotonic opaque cursors на двух исходных позициях. Existing transport test подтверждает encoding opaque cursor, а delayed older-sync и SYNC21 rollback — no-rewind и commit cursor только после применения batch. Literal full-page-cap boundary не выдаётся за отдельное доказательство и не является дополнительным критерием строки. Новый срез: **298 =224 PASS +2 FAIL +72 NOT_RUN**;8 частичных и64 без существенной привязки.

DEF038/EX015 закрыт. Actual-PG red→green подтвердил: отзыв A отключает registration, исключает A из recipient/candidate/claim/pre-send; A-only delivery завершается DLQ, смешанное A+B событие остаётся для B, attempts и replay exact. Первый release attempt остановился на ложном ENV_CHANGED из-за порядка Docker Env; read-only inspect подтвердил оба exact OLD runtime healthy. Отдельный attempt2 с semantic env-map завершился `RELEASE_VERIFIED`: live JAR/ELF и оба image exact, healthy/readiness/restarts0, env/networks/config и pre/prewrite/post counts неизменны. Provider canary не выполнялся; SENDING, прошедшая guard до observed revoke, может завершиться. Новый срез: **298 =225 PASS +1 FAIL +72 NOT_RUN**; единственный FAIL PRT07.

ASN01, APP01/02 и PROP01–05 закрыты сведением без новых запросов. `assignment-tool-local.log` и production assignment lifecycle подтверждают единственный PENDING assignment с selected roots/replay/audit1 и отсутствие member access до ACCEPT. Approval/proposal local-PG tests дают exact capabilities/invalid delta0, PENDING/inbox, ACCEPT/REJECT/DELEGATE, relationships/audits и replay; production public decision race подтверждает route, одного победителя и отсутствие повторных эффектов. Plugin/provider этим строкам не приписываются. Новый срез: **298 =233 PASS +1 FAIL +64 NOT_RUN**;8 частичных и56 без существенной привязки.

ACL02 закрыт одним адресным actual-PG методом1/1 без provider/production: accepted assignment даёт `can_read_document` root/child=true, sibling=false, list exact2, exact root/child content и sibling API404; транзакционный rollback оставил workspace/tasks/docs/assignments/audit/outbox0. Снятие inherited access берётся из уже выполненной TASK13 access-loss цепи; отдельный revoke не повторялся. Новый срез: **298 =234 PASS +1 FAIL +63 NOT_RUN**;8 частичных и55 без существенной привязки.

EX062 выявил DEF039 одним isolated UI/auth red test1/1 за3,526с без сети и рабочих AVD. После protected401 и definitive `AUTH_INVALID` на refresh challenge upload остаётся0 и Room/session/pending/key exact, но `AuthState` остаётся `Authenticated`, кешированная задача видима, explicit «Вставить приглашение» отсутствует. Требуемая правка — durable terminal recovery только для definitive auth failure, блокирование protected UI/calls и явное переподключение без автоматического wipe пользовательских данных. Новый срез: **298 =234 PASS +2 FAIL +62 NOT_RUN**.

ASN02 закрыт одним узким actual-PG методом1/1 без повторения старых assignment/ACL тестов. После member cursor394 принятие assignment с due опубликовало точную последовательность ASSIGNMENT395(v2)→TASK396(v1)→CALENDAR397(v1), next398; все changes UPSERT/non-null, due instant и member access exact. Транзакционный rollback оставил workspace/tasks/assignments/changes/audit/outbox0. Новый срез: **298 =235 PASS +2 FAIL +61 NOT_RUN**;8 частичных и53 без существенной привязки.

PUSH03/04/EX016 закрыты одним combined MVC→service→actual-PG методом1/1: семь invalid/spoof DTO rejected400 до service, same-key PUT semantic replay и DB exact, changed body409, DELETE204/204 оставляет enabled false и оба device active, foreign B registration exact; rollback cleanup0. Timestamp offset→Z сравнивается как тот же instant, остальные поля exact. EX052 закрыт композицией PUSH16 actual dispatcher one-event/three-device/three-row isolation/retry-no-duplicate и DEF038 active same-principal guards. Provider/production не повторялись. Новый срез: **298 =239 PASS +2 FAIL +57 NOT_RUN**;8 частичных и49 без существенной привязки.

DEF039/EX062 закрыт после независимого source review и одного targeted APK16 retest. Compact unit1/1 отличает IOException/503/initial401→successful refresh от definitive challenge/refresh401; только последнее ставит durable recovery и блокирует будущие protected calls. UI1/1 за6,084с подтвердил reopen, hidden cache, cancel-preserves, explicit confirmed wipe1 и failed enrollment без возврата cached access. `APK16-final-proof.json`: обе установки имеют SHA`eb2b4547…e244a`, прежний signer, exact14 Room tables/schema6 и рабочий PIN. E2E06 live server-revoke→Android связка остаётся отдельной BLOCKED_FIXTURE границей. Новый срез: **298 =240 PASS +1 FAIL +57 NOT_RUN**; единственный FAIL PRT07.

PUSH12 закрыт существующим actual Android worker replay (`batch5-worker-family.log`) и durable Room/action/scheduler guards: повтор того же event не меняет sync IDs или notification postTime, accepted row одна. REM12 закрыт actual SDK dispatcher duplicate replay + SQL exact token replay. REM13 дополнил единственный gap actual local-PG `reminders_resolve_callback`4/4: malformed/wrong owner/chat/expired C-token fail closed; domain exact, только допустимое expiry state, rollback/provider0. Новый срез: **298 =243 PASS +1 FAIL +54 NOT_RUN**;8 частичных и46 без существенной привязки.

TASK01 закрыт одним адресным UI-прогоном `MissingTaskRecoveryUiTest.emptyTaskListOffersTwoEntriesToOneCreateActionWithoutWrites`, PASS1/1 за3,128с. Actual `TasksScreen` показывает «Пока пусто» и ровно две видимые/доступные «+ Задача»; каждое нажатие даёт один callback, task/outbox0. `TASK01-final-proof.json` подтверждает сохранность14 Room-таблиц рабочего MEMBER. Изменился только test-файл, APK16, unsigned release и product source не изменены. Новый срез: **298 =244 PASS +1 FAIL +53 NOT_RUN**;8 частичных и45 без полного применимого доказательства.

Final16 unsigned SHA`4d7c34b89fff04e4422c9af75b314493742b2d1a676cd2fa44936772817ab562` прошёл независимую часть общего artifact gate: exact product source manifest, scoped diff, lock/integrity/component declarations, ZIP duplicate/suspicious-name0 и production secret findings0. `PRIVATE KEY` marker из DEX сопоставлен с буквальной проверкой `InviteBootstrap.kt`, которая отвергает закрытый ключ; fixtures находятся только в test/androidTest. N8N gate подтвердил runtime-scope diff0, package0.2.3/typebox1.1.38 integrity, verifier116 machine+5 docs и production secret findings0. Отдельного Android/N8N image SBOM нет; этот форматный пробел зафиксирован, но exact manifests и lock/component provenance достаточны для одного окна подписи. Статус artifact-строк не повышается до проверки подписанного результата.


### Проверка подписанного final16 завершена

Подписанный final16 подтверждён `final16-release-artifact-gate.json`: SHA256 `6218012f6751d088a210c27a1e7898c522fbd1ca28ff92eea25990e29eecd142`, подписи v2/v3 и прежний сертификат `4ca92be4b6682276bae07820b7c725aed5f7ab9af48c9df7c1627b7ecc63224f`, выравнивание16К. Все624 исходные ZIP-записи совпали с проверенным unsigned; добавлены только3 записи подписи. Исходники продукта/сборки совпали с замороженным манифестом; известная дополнительная правка относится только к тесту TASK01. Совместно с ранее принятыми сборками, проверками исходников/зависимостей/секретов и серверным манифестом это закрывает EX001/EX002/EX059/OPS10. Новых сборок или повторов функциональных проверок не было. Отдельный SBOM образов Android/N8N не создан; ограничение формата сохранено. ENV02/EX018 — обновление настоящего release с данными — этой проверкой не закрывается.

Канонический срез: **298 =248 PASS +1 FAIL +49 NOT_RUN**;6 частичных и43 без полного применимого доказательства. Плагин не установлен повторно, телефон не использовался.


### Сведение остатка и доступная серверная пачка

Независимое сведение `evidence-six-closure.md` закрыло OPS09: точная очистка4 документов, остаток0, audit/outbox/change5/5/5 сохранены; Android metadata/cache0, исходная задача/очередь сохранены. Это приёмка завершённой группы, не удаление всех исторических фикстур. ASN05/06 и ACL01 имеют частичное покрытие, ASN08/09 требуют фактической ветви переназначения. Их прежнее обозначение как «только композиция» исправлено: есть доступная локальная PostgreSQL/controller работа, без нового production-прогона.

Срез: **298 =249 PASS +1 FAIL +48 NOT_RUN**;9 частичных и39 без полного применимого доказательства.


### DEF040: принята локальная пачка, выпуск ожидается

Одна адресная PostgreSQL/controller пачка закрыла ASN05/06 и ACL01: отсутствующий корень/неверное решение отвергнуты без эффектов; изменённый запрос с тем же ключом назначения/решения даёт409 IDEMPOTENCY_KEY_REUSE; OWNER не изменяет личную задачу MEMBER и не запускает на ней агента. ASN08/09 выявили два связанных обхода DEF040: принятый получатель мог назначить задачу дальше при canRedelegate=false либо передать корень вне исходной ветки. `redelegation-gap-red.log`:2 FAIL из-за созданного назначения вместо отказа. Общая правка TaskCoreService в коммите56c9bca прошла независимое ревью и `assignment-acl-gaps-final.log`:5/5 PASS,0 пропусков,3,367с; проверены исходная и суженная дочерняя ветки, запрет родителя/соседней ветки, пять откатов с остатком workspace/principals0 и providerCalls0. `assignment-key-code-final.log`: адресное уточнение точного409-кода,1/1 PASS. ASN08/09 остаются FAIL до проверенного выпуска TaskCore; Android final16 не изменён.

Срез: **298 =252 PASS +3 FAIL +43 NOT_RUN**;6 частичных и37 без полного применимого доказательства.


### DEF040: один выпуск завершён

DEF040 принят на production одним выпуском: `def040-release-result.json` — RELEASE_VERIFIED, точный образ `ae5b94b2767800bcb316ee1c66ffbc7aa3e51a2580ff0645ca0fa3986dffc602` и JAR `f17669b7e2f19de225145186f5bfb1751b1e6a8abd80907aec469a6c3837963a`, healthy/readiness UP/restart0, прежние сети, смысловое равенство Env, неизменные конфигурация и структура таблицы. Проверенная резервная копия SHA256 `b9be762a760afbb2a51ad43371d9dfb7a998d4af25d32c24e93aa4231574a4f8`, откат не понадобился. Выполненный скрипт SHA256 `8fc80bb93e5eaafa03c09028ee27fa14e11be804d5a7ff3c0ba5e1a6f69f7755`: после независимого review изменён только поясняющий docstring. Новых назначений и вызовов провайдера процедура выпуска не создавала; приёмка сочетает действительные5 PostgreSQL/controller проверок с подтверждённой идентичностью работающего артефакта. Обновлена только запись TaskCore в общем манифесте. Подписанный Android final16 и остальные компоненты сохранены.

Итог: **298 =254 PASS +1 FAIL +43 NOT_RUN**;6 частичных и37 без полного применимого доказательства. PRT07 остаётся отдельным блокером, его выпуск не выполнялся.

### PRT07: исправленный плагин установлен и принят

Одна ограниченная попытка выпуска завершилась успешно. `prt07-resume-release.json` подтверждает архив `1d22bd9fa230546247465bd94336a2b80ead71b61bc2919e3efc3d46467f6044`, исходник `c04e4eb02b719e122b913bf1d9a525d840332f4c031b8c6ef40d2ce56a39b0b1`, выбранное поколение `g-0e24a05d32ee9e19`, исправные OpenClaw/relay и точную новую привязку сетевого пространства. Хеши конфигурации, DeepSeek и прежней осиротевшей записи совпали до/после. Резервная копия сохранена, откат не понадобился. Передача теста через файловую систему контейнера была отклонена до исполнения; тот же код выполнен через стандартный ввод без изменения контейнера и без повторного выпуска.

`prt07-installed-canary-stdin-result.json` подтвердил два исправленных отказа установленной фабрики: `PORUCHIK_HTTP_503:UNKNOWN` и `PORUCHIK_TRANSPORT_FAILED`, без произвольного текста и ложного успеха, реальная сеть0. Прежние проверки корреляции, ложного успеха, размера и тайм-аута переиспользованы. PRT07 закрыт. `prt01-installed-sdk-result.json` отдельно подтвердил настоящий SDK регистрации и фабрику установленного инструмента, HMAC, доверенные principal/workspace, одинаковые тело/operationId/correlationId повторного вызова и отказ подмены identity до HTTP. Сохранённые23/23 покрывают общую ветвь всех13 инструментов и смену доверенного run. Это синтетический доверенный контекст с перехваченным транспортом, не новый пользовательский запрос модели.

PRT01 закрыт; REM14 повторять не требуется, поскольку его фактический SDK-тест использовал точный core.js нового архива. PRT03 остаётся частичным:72 проверки TypeBox не заменяют отказ внешней границы SDK до HTTP. Остальные ранее названные «21 зависимая строка» не повышены автоматически. Настоящие подтверждения Telegram и отдельные доменные/интерфейсные границы учитываются по собственным доказательствам.

Текущий срез: **298 =256 PASS +0 FAIL +42 NOT_RUN**. Это отсутствие известных открытых дефектов в принятом объёме, а не полная приёмка MVP0. Подписанный final16 сохранён, телефон не использовался.

### Сведение PRT03 и истории агента

PRT03 закрыт без повторения72 границ: `prt03-installed-validator-result.json` — один некорректный аргумент отвергнут фактической функцией `validateToolArguments` установленного SDK до HTTP; точная связь с вызовом перед execute в agent proxy проверена по исходнику и хешу. Реальная сеть0.

AGT10/11 закрыты сохранённым production-результатом `MVP0-QA-20260909-TOOLS-cb0038ca/agent-history-live.json`: две точные страницы, новый запуск первым, конечный cursor null, чужая история пустая, чужой cursor и limit101 отвергнуты. Создание101 запусков лишь ради формы исключено; отдельная нагрузка не заявляется. AGT12 дополнил один существующий `mobileAgentRunHistoryIsOwnedBoundedPaginatedAndRedacted`: `agent-history-redaction-local-final.log` — actual PostgreSQL1/1 PASS,0 пропусков,3,379с. Проверены отсутствие seeded command/provider/session в публичном DTO и точный разрешённый состав четырёх записей audit metadata. Тест получил только транзакционный откат и проверку metadata;69 workspaces/22 runs/264 audit/192 outbox до/после совпали, production/provider0. Подпись APK и серверный выпуск не повторялись.

Текущий срез: **298 =260 PASS +0 FAIL +38 NOT_RUN**;5 частичных и33 без полного применимого доказательства. Прежняя группа «21 зависит от плагина» больше не используется как блокирующее условие: часть закрыта, настоящие Telegram-подтверждения остаются внешними, оставшиеся конкретные доменные границы проверяются отдельной компактной пачкой. Общая приёмка ещё не завершена.

AGT15 закрыт без нового прогона: полностью совпадает с уже принятыми EX028/PRT04. Сохранённые `create-tool-boundaries-final.log` и `derived-create-server-branch-final.log` подтверждают обе ветви, аудит и идемпотентность. Реализованный подписанный серверный API AGENT_DERIVED создаёт только предложение; автоматический producer этой ветви отсутствует и не заявляется. Текущий срез: **298 =261 PASS +0 FAIL +37 NOT_RUN**. Из прежних21 строк после закрытых PRT01/03, AGT10–12/15 остаются15: восемь доменных/интерфейсных границ (`AGT02/04/05/06/07/09/16`, `EX026`) и семь сценариев напоминаний (`REM01–04/06–08`). Последние требуют собственной проверки, включая настоящие Telegram-кнопки; установленный архив сам их не закрывает.

### Компактная пачка оставшихся доменных границ агента

Приняты AGT02/04/05/09 и EX026. `agent-cancel-scope-local-final.log` —2/2 PASS: чужая область отвергнута, отменённый RUNNING не получает новую аренду и не принимает позднее завершение/выход из конечного состояния. `agent-approval-mismatch-local-final.log` —1/1 PASS: подтверждённое согласование другой цели/возможности отвергнуто без эффектов; положительная ветвь и повтор переиспользованы. `agent-taskbound-wait-local-final.log` —1/1 PASS: точная привязка к задаче и последовательность ожидания/возобновления до версии5, ответ истории доступен, задача неизменна. Общие Android submit/history/terminal доказательства не повторялись. Все локальные транзакции откатились; исходные69 workspaces/22 runs/264 audit/192 outbox сохранены. Изменения только тестовые, production/APK не менялись.

AGT06 и AGT16 не закрыты этими результатами. Отсутствие новой аренды после отмены не доказывает запрет нового инструмента уже выполняющемуся агенту. `agent-false-success-local-final.log` подтверждает три отказа Action Executor502, но не доказывает итоговый статус run после ошибки инструмента: relay отдельно формирует результат. Требуются только эти конкретные границы, не повтор всей пачки.

Текущий срез: **298 =266 PASS +0 FAIL +32 NOT_RUN**. Непроверенные границы не объявлены ни успешными, ни подтверждёнными дефектами.

REM08 принят по исторической реальной цепи без новых сообщений и кнопок: `rem-historical-evidence.md`, серверный отчёт8–9 сентября, телефонный журнал и два позднейших чтения подтверждают настоящую отмену задачи040ce… в CANCELLEDv3, отсутствие будущих активных доставок и сохранность двух исходных напоминаний. REM01/02/04/06/07 имеют реальные положительные действия, но отдельные точные промежуточные состояния ещё не связаны; REM03 настоящего отказа кнопкой ❌ не имеет. Повтор всей цепи не назначается ради отсутствующей ссылки.

Текущий срез: **298 =267 PASS +0 FAIL +31 NOT_RUN**. Из них13 имеют частичные доказательства и18 пока не имеют полного применимого подтверждения. Подписанный APK и production не изменялись этой проверкой.

### Историческая цепь напоминаний сведена; выявлены две отдельные границы агента

REM01/02/06/07 приняты по настоящим действиям пользователя и одному чтению сохранённых серверных состояний `rem-historical-live-readonly.json`. Предложение create создано08 сентября17:08:05Z, задача/v1 появились только при применении17:14:54Z; occurrencev1 один. Предложение переноса создано09 сентября04:12:05Z приexpectedVersion1, v2 появился только при применении06:02:56Z; срок11 сентября16:45 Europe/Moscow, старый occurrence superseded. Счётчики и версии той же040ce… сохранены. Указанные старые номера audit отдельно не найдены; вместо них использованы предметные события148–152, pending/result и версии. REM04 принят одним настоящим установленным reminder_list→n8n чтением: cancelled/limit50,6 записей с каноническими состояниями, точные task_id/current_version/public_handle; `rem04-installed-list-result.json`. Новых Telegram-сообщений или нажатий не было. REM03 настоящего отказа ❌ ещё не имеет.

Два узких отрицательных теста подтвердили новые дефекты. DEF041/AGT06: настоящий клиент с контекстом уже отменённого запуска создаёт новую задачу через контроллер/PostgreSQL; `agent-cancel-effect-red.log` — ожидался отказ, действие принято, тестовая транзакция откатилась. DEF042/AGT16: настоящий код relay при управляемом CLI-ответе после ошибки настоящего клиента403 формирует SUCCEEDED/toolInvocations=[]; `agent-relay-false-success-red.log`, provider/network0. Это проверка границы CLI с подставленным ответом, не обращение к настоящей модели. Отдельные server lease guards и Action Executor502 не закрывают эти два дефекта.

Принят минимальный связанный план: доверенный runId в подписанном намерении и атомарная проверка состояния перед новым эффектом; учёт фактических результатов инструментов для честного конечного статуса relay. Успешный реальный повтор того же действия должен закрывать временную ошибку, старые human/Telegram запросы и идемпотентность сохраняются. Исправления собираются одной пачкой.

Текущий срез: **298 =272 PASS +2 FAIL +24 NOT_RUN**. Открыты DEF041/042; полный MVP0 не принят. Подписанный final16 не изменён.

AGT07 закрыт последним отдельным интерфейсным доказательством: `agt07-ui-result.log`,1/1 PASS за2,264с. Подставлен только ответ API; настоящий OfflineRepository, отдельная файловая Room и AgentScreen показали «Ошибка агента: DISPATCH_RETRIES_EXHAUSTED», без ложного успешного/принятого ответа; сохранена одна непользовательская строка. Основные db/shm/wal совпали по SHA до/после. Реальные5 попыток/DLQ/FAILED уже подтверждены старым production-результатом и OPS06, повторять их не потребовалось. Изменён только тест; продукт, подписанный final16 и телефон не затронуты.

Текущий срез: **298 =273 PASS +2 FAIL +23 NOT_RUN**. Открыты DEF041/042; они исправляются общей пачкой. Среди NOT_RUN5 частичных и18 остальных.

REM03 принят после настоящего нажатия ❌ пользователем. Одно QA-предложение pending679e5d48… доставлено сообщением96; доменное событие156 `pending_cancelled` в18:30:15Z связано с тем же pending/actor/chat. `rem03-after.json` подтверждает cancelled, taskId/appliedTaskId null, tasks0/occurrences0, точное сохранение всех8 исходных напоминаний (active2, hash258cda…69ad1). `rem03-callback-audit.json`: nextConfirmationAt/leaseOwner null. Отдельной записи tool_audit по этим корреляциям не найдено; положительное доказательство — доменное событие, состояние и настоящее нажатие, а не выдуманный аудит. Повторного предложения, отправки или подставного callback не было.

Локальная связанная пачка DEF041/042 прошла независимое ревью и адресные проверки: actual PostgreSQL4/4 с обоими порядками гонки отмены/эффекта; настоящий Linux Action Executor4 верхних теста и3 подслучая; plugin15/15; настоящий relay с контролируемыми границами8 тестов, включая незавершённую запись, ошибку записи журнала, успешный повтор, изменённые аргументы и текстовый ответ без инструмента. Сохранённый n8n workflow дополнительно проверен двумя точными примерами сохранности тела и подписанных заголовков, не изменялся. Дефекты остаются открыты до проверенного общего выпуска; APK final16 не пересобирается.

Текущий срез: **298 =274 PASS +2 FAIL +22 NOT_RUN**. Пакет пяти инструментов напоминаний/подтверждений завершён16/16; ночное окно REM17 относится к отдельному пакету и не закрыто. Среди NOT_RUN5 частичных и17 остальных.

Уточнён E2E03 только чтением: прежняя зависимость от установки плагина снята. Автоматическая ветвь «текст/голос→агент сам создаёт предложение AGENT_DERIVED» не реализована: `openclaw/plugin/dist/poruchik.js` формирует `origin:HUMAN_COMMAND`. Документированный подписанный серверный API AGENT_DERIVED реализован и уже принят AGT15/EX028/PRT04; он не подменяет отсутствующего автоматического производителя. Поэтому E2E03 остаётся NOT_RUN с точной пометкой частично NOT_IMPLEMENTED, без новой функции или повторного общего прогона. Числа274 PASS/2 FAIL/22 NOT_RUN не изменились; остальные причины отсутствия проверок не переименованы во «внешние».


### DEF041/042 — выпуск остановлен, 11 сентября 2026
Адресные локальные проверки исправлений успешны, но производственная приёмка не завершена. Единственная попытка сценария d0e721c4…e017 остановилась на конфликте имени Action Executor: его существующий проект Compose — poruchik-wave4, тогда как процедура использовала poruchik. Полный откат остановился на том же шаге. Проверка def041042-failure-readonly.json подтвердила: TaskCore работает на кандидате, Action Executor остался прежним; прежние плагин/relay, конфигурация, DeepSeek и исторический запуск сохранены, службы исправны, четыре счётчика активной работы равны нулю. Подготовлено независимым обзором минимальное восстановление только TaskCore к прежнему образу. До его итогового подтверждения восстановление не объявляется завершённым. Текущие 274 PASS /2 FAIL /22 NOT_RUN сохранены. Телефон и подписанный APK16 не менялись.


Минимальное восстановление завершено и независимо принято: def041042-restore-core-only-result.json, BASELINE_RESTORED, код выхода0. TaskCore возвращён к образу ae5b94…/JAR f17669…, Action Executor сохранил прежний образ и тот же контейнер. Прежние архив плагина1d22… и relayc049…, настройки/DeepSeek, исторический запуск и сетевое пространство проверены; службы healthy, активная работа0. Исправляется только процедура выпуска: проект Compose закрепляется отдельно для каждого сервиса, уже восстановленные компоненты при откате пропускаются. Продуктовые исправления и локальные успешные доказательства сохранены; 274 PASS /2 FAIL /22 NOT_RUN до производственной приёмки.


Исправленная попытка2 (1d9112…ca8ee) остановилась на защитной проверке BACKEND_ENV_CHANGED; откат также остановлен этим контролем. Контрольные проверки функциональности после установки не запускались. До точной сверки окружения и восстановления смешанное состояние не считается принятым; статусы 274/2/22 сохранены. Повторных попыток автоматически нет.


Второе восстановление независимо принято: def041042-restore-exact-baseline-result.json, EXACT_BASELINE_RESTORED, код выхода0. Причина отличия окружения — единственная добавленная PORUCHIK_RELEASE; существующие значения не менялись. Использован точный исходный Compose Action Executor, окружение возвращено к исходному SHA abbe2e…d135; оба серверных образа, плагин/relay/config и здоровое состояние полностью прежние, активная работа0. Потенциальная следующая процедура проходит сравнение разрешённой конфигурации каждого сервиса до запуска; нового выпуска пока нет. Срез275 PASS /2 FAIL /21 NOT_RUN.


### DEF041/042 — производственная повторная проверка успешна
Принят final script48b182…2a47b и def041042-final-final-readonly.json: точные новые Core/Executor, архив5c594… из7 файлов, relay4c2a…, здоровые службы/сеть/неизменные настройки и DeepSeek; активная работа0. def041042-final-api-run.json: подписанный CREATE с отсутствующим trustedRunId→403 RUN_CONTEXT_INVALID, задачи/аудит неизменны; прежний GET_FOCUS→200 APPLIED, только штатный audit+1. def041042-final-module-run.json: фактические установленные relay/plugin8/8 PASS, сеть/провайдер0. Вместе с четырьмя actual PostgreSQL проверками отмены/идентичности/двух порядков блокировки, Linux forwarding и совместимостью старого request hash это закрывает AGT06/DEF041 и AGT16/DEF042. Удалённый abort уже запущенного провайдера не обещается. Первые два процедурных отказа и точные восстановления сохранены в истории; общий регресс не повторялся. Срез277 PASS /0 FAIL /21 NOT_RUN. Подписанный APK16 не менялся.


EX024 принят композицией прежних InviteBootstrap/gateway negative/replay/direct-HTTP доказательств и нового actual AndroidKeyStore oracle: mobile-20260910/EX024-actual-keystore-proof.md, instrumentation status0. Отдельный QA alias: EC256, privateKey.encoded=null, ECDSA sign/verify, destroy/absent; штатные alias presence before/after совпали, приложение не переустанавливалось. Основная Room не открывалась; отдельный хеш Room этим тестом не заявляется. EX010 остаётся NOT_RUN: три выполнения метода завершились до подтверждённой recreation, сначала невалидная fixture, затем неустановленная синхронизация UI; отдельный class-not-found запуск метод не исполнял. Продуктовый дефект не доказан, четвёртый запуск не назначен. Срез278 PASS /0 FAIL /20 NOT_RUN.


PUSH01 принят без нового вызова Firebase: mobile-20260910/PUSH-baseline-proof.json фиксирует QA_Stable35/GMS/разрешение и token=pending→registration=start→complete в12:14:18Z; batch3-push-readiness.txt подтверждает в тот же момент включённую регистрацию ANDROID/0.2.0 и прежний хеш токена. Реальное получение уведомления/Room и успешные доставки приняты ранее. Отдельная трассировка происхождения callback не добавляет пользовательского критерия. PUSH02 — фактическая замена токена — остаётся уникальным непроверенным случаем. Срез279 PASS /0 FAIL /19 NOT_RUN.


### Точный остаток после серверной приёмки
19 NOT_RUN не являются19 дефектами и не относятся все к телефону. DOC13/AGT08/E2E03 содержат не реализованные возможности. E2E01/02 и EX057/058 — ранее исключённые широкие составные проверки, без присвоения PASS. VOI01/08 — настоящая русская акустика, REM17 — реальное ночное окно00–06. ENV01 требует отсутствующего API26; ENV02/EX018 — подписанное обновление с ожидающими операциями, ранее трижды ограниченное сетью release AVD. E2E06 — живой отзыв сессии с последующей реакцией Android, отдельно от принятого локального auth/UI доказательства. OPS07/EX049/EX053 — восстановление очередей после остановки процесса; установка при пустой очереди это не доказывает. EX010 остановлен после трёх неуспешных исполнений тестового сценария до проверки recreation. PUSH02 имеет отдельный ограниченный план фактической смены QA токена и его регистрации, ещё не выполненный. Создание новых возможностей ради этих строк не планируется; доступные уникальные проверки оцениваются отдельно от исключённых дублей.


Уточнение EX010 без нового запуска: общий first-assert failure не доказывает единую первичную причину трёх попыток. Первые две fixture содержали невалидное приглашение; третья исправила приглашение, но fake publicKey=qa-public-key не является допустимой Base64 и default publicKeySha256 вызывается до controlled exchange. Поэтому missing wait остаётся гипотезой, а не установленной причиной. Лимит нельзя механически считать исчерпанным по сумме разных fixture ошибок. Предложен один минимальный корректный oracle после source-проверки identity и ожидаемого поведения восстановления, с сохранением диагностической причины до UI assert; разрешение запуска остаётся у координатора.


PUSH02 принят: ровно одна actual Firebase SDK ротация на QA_Stable35, новый хеш de5775…8c8f0 зарегистрирован для того же enabled QA устройства; остальные регистрации неизменны (push02-registration-comparison.json). PUSH02-local-recovery-proof.md подтверждает pending=false после одного завершения регистрации без повторной ротации; workspace/principal/device, PIN, штатные alias и счётчики Room сохранены. Whole-session assertion исходного теста был избыточным: bearer/refresh/expiry штатно обновились, sessions43→44; это не продуктовый дефект. Повторная доставка не требовалась для критерия регистрации, действительные PUSH18 доказательства не повторялись. Срез280 PASS /0 FAIL /18 NOT_RUN.


EX010 принят после одной обоснованной адресной попытки с исправленными fixture: EX010-final-instrumentation.txt — OK1/1,3.702с; EX010-final-logcat.txt — actual recreation1, bootstrapCalls2, безопасная причина ошибки и явный retry, sessionNull/enrollmentEmpty/network0. Повтор transport после recreation разрешён исходным контрактом при отсутствии успешного двойного redeem и частичного входа. Первые три first-assert отказа не были тремя подтверждениями одной первичной причины: исправлены формат приглашения и fake identity hash до controlled exchange; missing-wait не выдаётся за установленный дефект продукта. Широкий cross-product каждой фазы/ориентации не нужен. Продукт/подписанный APK16 не изменены. Срез281 PASS /0 FAIL /17 NOT_RUN.


### Текущий остаток17
Уникальные пользовательские границы: ENV01 (запуск на минимальном API26; такого SDK/AVD сейчас нет), ENV02/EX018 (одна цепь подписанного обновления с ожидающими CREATE/UPDATE; прежний release AVD ограничен сетью после трёх попыток), VOI01/08 (одна проверка настоящей русской речи через микрофон/OEM), REM17 (ночное окно00–06), E2E06 (серверный отзыв отдельной QA-сессии с реакцией Android; локальный auth/UI уже проверен, живая связка отсутствует). Для E2E06 пригоден существующий AVD с изолированной QA-сессией, телефон не обязателен. Физический телефон полезен для настоящей речи и совмещённого release-update в тестовом профиле; основной профиль пользователя не затрагивается.
Отдельно не выполнены операционные OPS07/EX049/EX053 (остановка процесса с ожидающей очередью; idle-релизы не подменяют такой сценарий). EX049 ранее исключён из обязательных активных прогонов, SQL-восстановление аренды уже проверено. DOC13/AGT08/E2E03 содержат не реализованные возможности. E2E01/02 и EX057/058 — ранее исключённые широкие составные проверки, не получившие PASS. Эти категории нельзя объединять в «нужен телефон». Все обнаруженные продуктовые исправления текущей пачки закончены и установлены; оставшиеся изменения этой адресной Android-пачки относятся к тестам. Полная приёмка17 непроверенных строк не заявляется.


### Эксплуатационный остаток без дополнительного перезапуска
OPS07 и EX053 исключены из обязательного активного прогона MVP0 как составные эксплуатационные повторения; оба сохраняют NOT_RUN. Дословная цепь «ожидающая запись→фактический перезапуск процесса→обработка» не выполнена, idle-релизы за неё не выдаются. Функциональные границы OPS07 подтверждают ops06-dispatch-batch-green.log (actual PostgreSQL3/3: потеря аренды, новый worker, отклонение старого lease, ограниченный терминальный переход) и проверенные установки relay с рабочим опросом. Для EX053 batch3-backend-local-gate.txt/FcmPushPublisherIntegrationTest4/4 подтверждает единственную delivery при повторном seed, отказ повторного claim и публикацию только после принятия; сохранённые реальные доставки и перезапуски проверяли смежные стороны отдельно. Эти доказательства не объявляются одним непрерывным restart-сценарием. Дополнительный production restart и новый локальный стенд не назначены. EX049 ранее исключён по тому же принципу с сохранённым SQL lease-recovery доказательством. Срез281 PASS /0 FAIL /17 NOT_RUN неизменён.


ENV01/02 приняты на отдельном API26 AVD: API26-fixture-provenance.md и API26-ENV02-normal-samecert-proof.md. Старый подписанный SHA d1257…5e820 обновлён install-r до final16 SHA621801…d142 при том же сертификате4ca92…224f; firstInstallTime сохранён. Final16 запускается после force-stop, требует прежний PIN и показывает собственную PRIVATE задачу (оба before/after XML проверены). Это обычное release-обновление и минимальный API26 startup; побайтовое равенство Room/внутренних UUID не заявляется. EX018 pendingCREATE/UPDATE остаётся NOT_RUN: три штатных Android network-control способа не отключили сеть, четвёртый не назначен. Это ограничение среды, не дефект final16. Срез283 PASS /0 FAIL /15 NOT_RUN; E2E06 живой отзыв ещё ожидает выполнения.


E2E06 принят: API26-E2E06-live-owner-member-revoke-proof.md, официальный OWNER→MEMBER DELETE204 ровно1 раз (api26-official-member-revoke-result.json). Target device/session/SSH отозваны, push отключён, другие устройства/задачи/аудит неизменны. Три actual final16 XML подтверждают: собственная PRIVATE задача видима до отзыва, скрыта после реального sync и после force-stop/reopen; показано «Нужно подключиться заново», локальные данные сохранены и отправка остановлена. Это настоящий серверный отзыв→Android recovery, без подмены logout/SQL и без silentwipe. Прежний OWNER-fixture blocker был ограничением выбранной тестовой роли и снят официальным OWNER→MEMBER сценарием. Подписанный final16 не пересобирался. Срез284 PASS /0 FAIL /14 NOT_RUN.


### Готовность после адресной приёмки API26
Итог284 PASS /0 FAIL /14 NOT_RUN. Не завершены три уникальных сценария (четыре строки): EX018 — подписанное обновление с ожидающими CREATE/UPDATE при реально отключённой сети (API26 network-control ограничен после трёх способов); VOI01/08 — настоящая русская речь через микрофон; REM17 — фактическое ночное окно00–06. DOC13/AGT08/E2E03 — три не реализованные/частично не реализованные возможности. OPS07/EX049/EX053/E2E01/E2E02/EX057/EX058 — семь исключённых составных или эксплуатационных прогонов, сохранённых как NOT_RUN без ложного PASS. Эти14 строк не являются14 открытыми дефектами; полной приёмки всех сценариев пока нет.
Обычное signed-update, минимальный API26, FCM регистрация/ротация и официальный отзыв сессии с защищённым Android recovery уже проверены. Следующий физический телефон нужен только для акустики и, при доступном надёжном offline, совмещённой pending-update проверки; ночное окно отдельно. Все выявленные продуктовые исправления установлены; последняя Android работа меняла только тесты/собственные fixtures. Минимальная правка OWNER helper (TCP readiness/checkpoint) принадлежит серверному исполнителю; она не меняет продуктовый APK/серверный протокол. Signed final16: C:/BackUp/Poruchik-Signing/Poruchik-MVP0-final16-release-signed-20260911.apk, SHA2566218012f6751d088a210c27a1e7898c522fbd1ca28ff92eea25990e29eecd142.
