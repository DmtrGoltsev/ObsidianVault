# Частичное покрытие текущего прогона

Канонические статусы не изменяются из-за частичных доказательств. После принятого сведения готовых доказательств подтверждены 5 частично выполненных кейсов среди 14 NOT_RUN; REM17 добавлен сюда после исправления прежних дефектов, потому что actual quiet-window остаётся непроверенным. Остальные 9 не повышены без полного применимого доказательства; это не утверждение, что старые тесты их никогда не затрагивали. SYNC04/15/19 и E2E05 закрыты одной production HTTP+Android UI/Room+restart цепью и exact cleanup; второй AVD-writer не создаёт отдельного продуктового oracle. INB06/EX021 закрыты одним actual AVD online-only guard и существующими typed/backend доказательствами; cross-product действий не выполнялся. EX025/DEF033 закрыт тем же единственным сокращённым CREATE write-error UI oracle на APK12: санитизированная ошибка, draft/retry и rollback подтверждены; широкие варианты не добавлялись. EX019/DEF034 закрыт одним actual-Room+latch test на APK13 source; последовательные ADB taps не использовались как concurrency proof. VOI02/06/07 переведены в PASS композицией UI, общего production text-submit и source/storage oracle; новой акустической или сетевой проверки не было. EX006–008 закрыты после реального Go-прогона в существующей Docker-сборке; Windows-ограничение не считается успешным тестом. EX005 закрыт дополнительной реальной TCP-проверкой Content-Length и медленного тела. NET01 и AGT03 закрыты11 сентября после полного оставшегося production-набора. REM05 закрыт после сведения сохранённого production HTTP 8/8 с предыдущим SQL-доказательством; повторных запросов не было.

| Кейс | Выполнено и точный остаток |
|---|---|
| DOC-13 | NOT_IMPLEMENTED: официального отзыва assignment/grant нет. Исходный сценарий не является ни FAIL продукта, ни PASS; реализованные403/404 и cache invalidation проверены отдельно в DEF007. |
| EX-049 | SQL claim/reclaim с новыми клиентами PASS; настоящий restart worker не выполнен. |
| REM-17 | Исправленные digest/rate ветви подтверждены; фактическое quiet-window 00:00–06:00 МСК ещё не наблюдалось. |
| AGT-08 | NOT_IMPLEMENTED: отдельного retry-existing-run API/UI нет; обычный submit и transport lease retry не подменяют эту функцию. |
| EX-018 | APK9 before/after JSON одинаков,43op:38APPLIED/2CONFLICT/3SUPERSEDED; PENDING0. Остаток pending CREATE/UPDATE при signed release update объединяется с финальной установкой. |


Дополнение Room-пакета: для SYNC08/09/10/22 теперь есть настоящая файловая Room/reopen граница (QueueRoomRecoveryTest3PASS), а не только MemoryStore. Production и Android process-death этой проверкой не подменяются; перед новым AVD прогоном свести существующие API/kill-доказательства и оставить только уникальный остаток. SYNC13 переведён вFAIL по DEF029, остальные строки от частичного результата автоматически не повышались.


### Готовность после адресной приёмки API26
Итог284 PASS /0 FAIL /14 NOT_RUN. Не завершены три уникальных сценария (четыре строки): EX018 — подписанное обновление с ожидающими CREATE/UPDATE при реально отключённой сети (API26 network-control ограничен после трёх способов); VOI01/08 — настоящая русская речь через микрофон; REM17 — фактическое ночное окно00–06. DOC13/AGT08/E2E03 — три не реализованные/частично не реализованные возможности. OPS07/EX049/EX053/E2E01/E2E02/EX057/EX058 — семь исключённых составных или эксплуатационных прогонов, сохранённых как NOT_RUN без ложного PASS. Эти14 строк не являются14 открытыми дефектами; полной приёмки всех сценариев пока нет.
Обычное signed-update, минимальный API26, FCM регистрация/ротация и официальный отзыв сессии с защищённым Android recovery уже проверены. Следующий физический телефон нужен только для акустики и, при доступном надёжном offline, совмещённой pending-update проверки; ночное окно отдельно. Все выявленные продуктовые исправления установлены; последняя Android работа меняла только тесты/собственные fixtures. Минимальная правка OWNER helper (TCP readiness/checkpoint) принадлежит серверному исполнителю; она не меняет продуктовый APK/серверный протокол. Signed final16: C:/BackUp/Poruchik-Signing/Poruchik-MVP0-final16-release-signed-20260911.apk, SHA2566218012f6751d088a210c27a1e7898c522fbd1ca28ff92eea25990e29eecd142.
