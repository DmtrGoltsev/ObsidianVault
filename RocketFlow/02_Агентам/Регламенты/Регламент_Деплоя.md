---
id: "reg-deploy"
тип: "регламент"
статус: "активно"
проект: "RocketFlow"
владелец: "rocketflow-team"
создано: "2026-05-31"
обновлено: "2026-08-22"
уверенность: "высокая"
источники: ["docs/60-hexcore-prod-runbook.md", "docs/68-scroll-and-priority-retirement-delivery.md"]
доказательства: ["Док_Prod_Deploy_State", "Док_V21_Scroll_Priority_20260822"]
теги: ["регламент", "деплой", "production"]
---

# Регламент Деплоя

## Цель

Стандартизировать процедуру развёртывания RocketFlow на production-сервер [[HexCore]].

## Область применения

Деплой backend jar и web статики на HexCore (45.10.110.42). Docker/GHCR deploy не является закрытым production-фактом и остаётся open gate.

## Основной production deploy

Основной путь production deploy для RocketFlow — GitHub Actions workflow `.github/workflows/backend-hexcore-prod-deploy.yml`.

- Auto deploy запускается только при push в ветки, имя которых содержит `release`.
- Workflow дополнительно защищен guard `contains(github.ref_name, 'release')`.
- GitHub environment: `production`.
- Concurrency включен.
- Existing HexCore SSH/SCP/promote/health steps сохраняются.
- `MVP2` больше не является auto-deploy веткой без `release` в имени.
- Required deploy secrets by name: `HEXCORE_PROD_SSH_HOST`, `HEXCORE_PROD_SSH_USER`, `HEXCORE_PROD_SSH_PRIVATE_KEY`, optional `HEXCORE_PROD_SSH_PORT`.

Примечание: финальный production deploy через этот путь подтвержден 2026-06-19; см. [[CI_CD_Production_Status_20260619]].

## Fallback / alternative deploy

Direct SSH/SCP upload to HexCore остается fallback/alternative способом, когда GitHub Actions недоступен или требуется ручное восстановление. Fallback не должен обходить readiness gates: backup expectation, secrets/env readiness, target ref/method и smoke plan должны быть явно подтверждены.

## Процедура

### Backend (systemd)

1. Собрать backend jar через CI (`backend-verify` workflow)
2. Остановить systemd unit: `sudo systemctl stop rocketflow-backend`
3. Скопировать jar на HexCore
4. Запустить unit: `sudo systemctl start rocketflow-backend`
5. Проверить статус: `sudo systemctl status rocketflow-backend`
6. Smoke check: `curl http://localhost:8080/actuator/health`

### Web (Nginx)

1. Собрать production build: `npm run build` (через `web-verify`)
2. Скопировать статику в Nginx docroot
3. Проверить доступность web через HTTPS

### Откат

- Восстановить предыдущую версию jar и перезапустить systemd unit

## Участники

- [[Агент_DevOps]] — исполнитель деплоя
- [[Агент_Бэкенд]] — предоставляет jar
- [[Агент_Веб]] — предоставляет статику
- [[Агент_QA]] — smoke-валидация после деплоя

## Триггеры запуска

- Ручной: workflow_dispatch (`backend-hexcore-prod-deploy.yml`)
- Автоматический: push только в ветку, имя которой содержит `release`

## Известные blockers/risks

- Production status 2026-06-19: **DEPLOYED / PASS** по GitHub Actions release branch evidence.
- Residual risk: independent DB read via local SSH principal unavailable; DB evidence comes from deploy logs.
- Docker/GHCR deploy не закрыт и остается open gate.
- Secret values не документируются; допустимы только имена required secrets.

## Критерии успеха

- `/actuator/health` возвращает UP
- Web доступен по HTTPS
- Основные endpoints отвечают 200

## Эскалация

Отказ деплоя → откат → диагностика логов (`journalctl -u rocketflow-backend`) → [[Агент_DevOps]] → [[Оркестратор]].

## Связанные заметки

- [[Источник_Продакшен_Runbook]] — runbook-источник
- [[HexCore]] — целевой сервер
- [[Регламент_CI_CD]] — связанный регламент
- [[Схема_Развертывания]] — визуальная схема
- [[Док_Prod_Deploy_State]] — factual deploy state

## Final CI/CD local preparation status (2026-06-14)

Status: PASS for local CI/CD preparation. This does not claim that a new production deploy was executed.

- User confirmed RocketFlow is production.
- Read-only HexCore inventory confirmed `rocketflow-backend.service`, `/rocket/ -> /var/www/rocketflow-web/current`, `/rocket-api/ -> 127.0.0.1:8080/api/`, backend symlink `/opt/rocketflow/current/rocketflow-backend.jar`, DB `rocketflow_prod`, Flyway 18 rows, health OK.
- Local workflow/docs prepared: hardened `.github/workflows/backend-hexcore-prod-deploy.yml`, new GHCR package and rollback workflows, `docs/production/rocketflow-*`, policy/runbook updated.
- Historical 2026-06-14 preparation state: release push was package-only and production deploy dispatch-only; superseded by 2026-06-19 release-branch deploy evidence.
- Final review: all 7 workflow YAML files parse; no raw `${{ inputs.* }}` in `run` or `script`; release push no prod mutation; dispatch gates present; no hardcoded secret values.
- Historical note: no production deploy was executed during the 2026-06-14 preparation update; superseded by final deploy evidence on 2026-06-19.
- Residual approvals: GitHub production environment/secrets/required reviewers, first production workflow run, deploy/restart/migration/rollback/GHCR publish approvals, DB backup proof; DB rollback out of scope.
- Evidence: [[CI_CD_Production_Status_20260614]].

## Final production CI/CD state (2026-06-19)

Status: PASS. GitHub Actions release-branch production deploy is now the primary path.

- Release branch: `release/rocketflow-prod-ci-cd-ec377a7`; release commit `4dbf10b0d693ea9f160993fe15199bc0047bb2ea`.
- Final deploy run: `https://github.com/DmtrGoltsev/RocketFlow/actions/runs/27803394498`, success; package/deploy success.
- Production symlinks: backend `rocketflow-backend-sha-4dbf10b0d693.jar`; web `rocketflow-web-sha-4dbf10b0d693`.
- Flyway rows `18 -> 18`; app Flyway lifecycle no migration necessary.
- Trigger rule: branch name must contain `release`; push/cherry-pick/merge desired commit into `release/<project>-prod-...`, then watch GitHub Actions.
- Manual `workflow_dispatch` remains available.
- Direct SSH/SCP remains documented fallback, not primary.
- GitHub `production` environment and environment secrets are configured; secret values must never be stored in docs.
- Verify after deploy: `http://45.10.110.42/rocket/` and `http://45.10.110.42/rocket-api/health`.
- Rollback: use project rollback workflow where available; DB rollback is not automated and requires manual restore from backups after approval.
- Evidence: [[CI_CD_Production_Status_20260619]].

## Calendar/Weekly Focus rollout (2026-08-10)

Backend/web rollout завершён: source `910c061de4af9395d9bb682624bd966b2977a738`, release `sha-910c061de4af`, Flyway V20 (`20/20`). Focus cadence и Web Push остаются disabled; authenticated production smoke остаётся открытым gate. Evidence: [[Док_Production_Rollout_20260810]], [[Док_Prod_Deploy_State]].

## V21 candidate rollout gate (2026-08-22)

- Current V21 worktree не был commit/push/deploy в этой задаче; production остаётся V20.
- Перед rollout обязательны reviewed commit/release ref, backup/rollback owner и production configuration validation.
- Workflow preflight должен подтвердить Flyway `>=20`, manifest target ровно `21`, а после совместного backend/web promotion — Flyway `>=21`, Hibernate validation, health и authenticated compatibility smoke.
- Web совместим с V20 и V21, поэтому продвигается совместно с backend; Android выпускается отдельно после зелёного server/web gate.
- Application rollback является fail-closed forward-schema rollback: readable manifest обязателен, `flyway_history_min_rows` должен быть integer `>=20` и `<=PRE_FLYWAY_COUNT`; отсутствующие, строковые, boolean и out-of-range значения отклоняются. Post Flyway остаётся `>=20` и `>=pre`. V20 binary с minimum 20 допустим на V21 schema; DB downgrade/repair/restore не выполняется workflow.
- Contract checks: accepted `7/7`, invalid `4/4`, YAML/Bash PASS; `actionlint`/`shellcheck` недоступны.
- Android production artifact по-прежнему требует отдельного release/signing/configuration evidence.
- Evidence: [[Док_V21_Scroll_Priority_20260822]], [[ADR_Отказ_От_Приоритета_Задач]], [[Док_Prod_Deploy_State]].
